{
  "name": "decision-labs",
  "private": true,
  "type": "module",
  "description": "Four independent, offline decision workbenches.",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node scripts/serve.mjs",
    "test": "node --test tests/launcher.test.mjs tests/standalone-modules.test.mjs && node scripts/run-apps.mjs test",
    "check": "node --check scripts/serve.mjs && node --check scripts/run-apps.mjs && node scripts/run-apps.mjs check",
    "build:standalone": "node scripts/run-apps.mjs build:standalone"
  },
  "license": "MIT"
}
