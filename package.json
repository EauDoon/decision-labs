{
  "name": "partnership-breakpoint",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "description": "A transparent browser workbench for participant-level partnership exit thresholds.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build:standalone": "node scripts/build-standalone.mjs",
    "launch": "node scripts/launch.mjs",
    "start": "node scripts/dev-server.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node scripts/check.mjs && node --check scripts/launch.mjs && node --check scripts/build-standalone.mjs && node scripts/build-standalone.mjs --check"
  }
}
