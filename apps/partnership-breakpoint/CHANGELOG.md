# Changelog

## 1.5.24

- Keeps the 1.5.0 constraint review and 1.5.1/1.5.2/1.5.3/1.5.4/1.5.5/1.5.6/1.5.7/1.5.8/1.5.9/1.5.10/1.5.11/1.5.12/1.5.13/1.5.14/1.5.15/1.5.16/1.5.17/1.5.18/1.5.19/1.5.20/1.5.21/1.5.22/1.5.23 review UI: `createPartnershipReviewPacket`, `replayPartnershipReviewPacket`, `PARTNERSHIP_REVIEW_TOOLS`, `analyzePartnershipReview`, waterfall SVG, copy waterfall, persisted hide holders, Talent/agent/platform, ledger all-hold filter, Three-party joint venture, Podcast host and network, first-breakpoint snapshot, Community hall split, Festival stall split, Pop-up cinema split, Community radio split, School concert split, Sports carnival split, Netball carnival, Swimming carnival split, Athletics carnival split, Cricket carnival split, Tennis carnival split, Basketball carnival split, Volleyball carnival split, Rugby carnival split, Hockey carnival split, Baseball carnival split, Softball carnival split, Lacrosse carnival split, Water polo carnival split, and keys `f` `w` `l` `b` `c` `p` `t` `d` `k` `h` `a` `m` `v` `i` `o` `j` `q` `x` `y` `z` `,` `.` `/` `;` `[` `]` `'` `<` `>` `:` `-` `=` `"` `_` `{` `}` `+` `|` `~` `!` `@` `(` `)` `#` `*` `&` `%` `$` `^` backtick `5` `6` `7` `8` `9` `0` `1` `2` `3` `4` `PageUp` `PageDown` `Home` `End` `ArrowUp` `Insert` `ArrowDown` `ArrowLeft` `Delete` `F2` `ArrowRight`.
- Press `F3` to copy last zero-share participant label through the new copy control, with the same Markdown. Ignored in inputs. Distinct from `Delete` first-at-hold remaining, `Insert` last-at-hold remaining, `4` last-unbounded remaining, and `PageUp` first-unbounded remaining. It is not a forecast.
- Press `F4` to jump to Copy last zero-share participant label, or the Participants heading if that control is missing, when no field is focused. It does not copy. Distinct from `F2` first-at-hold remaining jump, `ArrowDown` last-at-hold remaining jump, `Home` last-unbounded remaining jump, and `PageDown` first-unbounded remaining jump.
- Press `Backspace` to jump to Hide the last participant with zero revenue share, or the Participants heading if that control is missing, when no field is focused. Distinct from `ArrowRight` hideFirstZeroShareParticipant and `hideZeroShareParticipants`. Named keys `F3`, `F4`, and `Backspace` are not length-1; existing letter handling stays. Shortcuts return if default is already prevented and stay ignored while typing.
- Added the Rowing carnival split starting point, a three-party carnival committee plus boat hire plus first-aid with different cost bases, distinct from Balanced, Thin Margin, Growth at a Cost, Creator take-rate, Three-party JV, Two-party 50/50 studio, Four-party marketplace, Licensor and distributor, Talent, agent, and platform, Three-party joint venture, Podcast host and network, Community hall split, Festival stall split, Pop-up cinema split, Community radio split, School concert split, Sports carnival split, Netball carnival, Swimming carnival split, Athletics carnival split, Cricket carnival split, Tennis carnival split, Basketball carnival split, Volleyball carnival split, Rugby carnival split, Hockey carnival split, Baseball carnival split, Softball carnival split, Lacrosse carnival split, and Water polo carnival split. Monthly volume 4300, fee 8, addressable 5500, and listed capacities 5200, 6300, and 4300. It is a synthetic starting point, not a live roster. Counts remain counts. Ranking is not a forecast of who will exit.
- Hide the last participant with zero revenue share now travels on saved case JSON as optional `hideLastZeroShareParticipant`. Older files omit it and default to showing that roster row. Unknown sibling fields are still rejected. Display only; the solver is unchanged. Hidden rows still count. Distinct from `hideZeroShareParticipants`, which hides every zero-share row, from `hideFirstZeroShareParticipant`, which hides the first zero-share row, and from ArrowRight first-zero-share hide. If only one roster row currently has zero revenue share, hiding last hides that one row. `hideHoldingParticipants`, `hideAllHoldLedger`, `hideZeroShareParticipants`, `hideParticipantsOverCapacity`, `hideParticipantsAtHold`, `hideParticipantsWithoutCapacity`, `hideParticipantsWithSpareCapacity`, `hideParticipantsAtLeastHeadroom`, `hideParticipantsWithinCapacity`, `hideFirstBreakpointParticipant`, `hideFirstOverCapacityParticipant`, `hideLastOverCapacityParticipant`, `hideLastBreakpointParticipant`, `hideLastWithinCapacityParticipant`, `hideFirstWithinCapacityParticipant`, `hideLastSpareCapacityParticipant`, `hideFirstSpareCapacityParticipant`, `hideLastParticipantWithoutCapacity`, `hideFirstParticipantWithoutCapacity`, `hideLastParticipantAtHold`, `hideFirstParticipantAtHold`, and `hideFirstZeroShareParticipant` stay available.
- Print one-pager includes last zero-share participant label as one line when the case is valid. The line is an honest empty when none have zero revenue share. The saved case is unchanged.
- Copy last zero-share participant label copies a one-line Markdown name of the last roster row with zero revenue share, or an honest empty line when none. Clipboard falls back to a textarea. It is distinct from first at-hold remaining-to-hold copy, last at-hold remaining-to-hold copy, last unbounded remaining-to-hold copy, and first unbounded remaining-to-hold copy. It is not a forecast. Copy first at-hold remaining-to-hold stays available. Counts remain counts. Ranking is not a forecast of who will exit.

## 1.5.23

- Keeps the 1.5.0 constraint review and 1.5.1/1.5.2/1.5.3/1.5.4/1.5.5/1.5.6/1.5.7/1.5.8/1.5.9/1.5.10/1.5.11/1.5.12/1.5.13/1.5.14/1.5.15/1.5.16/1.5.17/1.5.18/1.5.19/1.5.20/1.5.21/1.5.22 review UI: `createPartnershipReviewPacket`, `replayPartnershipReviewPacket`, `PARTNERSHIP_REVIEW_TOOLS`, `analyzePartnershipReview`, waterfall SVG, copy waterfall, persisted hide holders, Talent/agent/platform, ledger all-hold filter, Three-party joint venture, Podcast host and network, first-breakpoint snapshot, Community hall split, Festival stall split, Pop-up cinema split, Community radio split, School concert split, Sports carnival split, Netball carnival, Swimming carnival split, Athletics carnival split, Cricket carnival split, Tennis carnival split, Basketball carnival split, Volleyball carnival split, Rugby carnival split, Hockey carnival split, Baseball carnival split, Softball carnival split, Lacrosse carnival split, and keys `f` `w` `l` `b` `c` `p` `t` `d` `k` `h` `a` `m` `v` `i` `o` `j` `q` `x` `y` `z` `,` `.` `/` `;` `[` `]` `'` `<` `>` `:` `-` `=` `"` `_` `{` `}` `+` `|` `~` `!` `@` `(` `)` `#` `*` `&` `%` `$` `^` backtick `5` `6` `7` `8` `9` `0` `1` `2` `3` `4` `PageUp` `PageDown` `Home` `End` `ArrowUp` `Insert` `ArrowDown` `ArrowLeft`.
- Press `Delete` to copy first at-hold remaining-to-hold through the new copy control, with the same Markdown. Ignored in inputs. Distinct from `Insert` last-at-hold remaining, `4` last-unbounded remaining, and `PageUp` first-unbounded remaining. It is not a forecast.
- Press `F2` to jump to Copy first at-hold remaining-to-hold, or the Participants heading if that control is missing, when no field is focused. It does not copy. Distinct from `ArrowDown` last-at-hold remaining jump, `Home` last-unbounded remaining jump, and `PageDown` first-unbounded remaining jump.
- Press `ArrowRight` to jump to Hide the first participant with zero revenue share, or the Participants heading if that control is missing, when no field is focused. Distinct from `ArrowLeft` hideFirstParticipantAtHold, `ArrowUp` hideLastParticipantAtHold, and `hideZeroShareParticipants`. Named keys `Delete`, `F2`, and `ArrowRight` are not length-1; existing letter handling stays. Shortcuts return if default is already prevented and stay ignored while typing.
- Added the Water polo carnival split starting point, a three-party carnival committee plus pool hire plus first-aid with different cost bases, distinct from Balanced, Thin Margin, Growth at a Cost, Creator take-rate, Three-party JV, Two-party 50/50 studio, Four-party marketplace, Licensor and distributor, Talent, agent, and platform, Three-party joint venture, Podcast host and network, Community hall split, Festival stall split, Pop-up cinema split, Community radio split, School concert split, Sports carnival split, Netball carnival, Swimming carnival split, Athletics carnival split, Cricket carnival split, Tennis carnival split, Basketball carnival split, Volleyball carnival split, Rugby carnival split, Hockey carnival split, Baseball carnival split, Softball carnival split, and Lacrosse carnival split. Monthly volume 4200, fee 8, addressable 5400, and listed capacities 5100, 6200, and 4200. It is a synthetic starting point, not a live roster. Counts remain counts. Ranking is not a forecast of who will exit.
- Hide the first participant with zero revenue share now travels on saved case JSON as optional `hideFirstZeroShareParticipant`. Older files omit it and default to showing that roster row. Unknown sibling fields are still rejected. Display only; the solver is unchanged. Hidden rows still count. Distinct from `hideZeroShareParticipants`, which hides every zero-share row, from `hideFirstParticipantAtHold`, and from `hideLastParticipantAtHold`. If only one roster row currently has zero revenue share, hiding first hides that one row. `hideHoldingParticipants`, `hideAllHoldLedger`, `hideZeroShareParticipants`, `hideParticipantsOverCapacity`, `hideParticipantsAtHold`, `hideParticipantsWithoutCapacity`, `hideParticipantsWithSpareCapacity`, `hideParticipantsAtLeastHeadroom`, `hideParticipantsWithinCapacity`, `hideFirstBreakpointParticipant`, `hideFirstOverCapacityParticipant`, `hideLastOverCapacityParticipant`, `hideLastBreakpointParticipant`, `hideLastWithinCapacityParticipant`, `hideFirstWithinCapacityParticipant`, `hideLastSpareCapacityParticipant`, `hideFirstSpareCapacityParticipant`, `hideLastParticipantWithoutCapacity`, `hideFirstParticipantWithoutCapacity`, `hideLastParticipantAtHold`, and `hideFirstParticipantAtHold` stay available.
- Print one-pager includes first at-hold remaining-to-hold as one line when the case is valid. The line is an honest empty when none are currently at hold. The saved case is unchanged.
- Copy first at-hold remaining-to-hold copies a one-line Markdown amount of remaining to hold for the first roster row currently at hold, or an honest empty line when none. Clipboard falls back to a textarea. It is distinct from last at-hold remaining-to-hold copy, last unbounded remaining-to-hold copy, and first unbounded remaining-to-hold copy. It is not a forecast. Copy last at-hold remaining-to-hold stays available. Counts remain counts. Ranking is not a forecast of who will exit.

## 1.5.22

- Keeps the 1.5.0 constraint review and 1.5.1/1.5.2/1.5.3/1.5.4/1.5.5/1.5.6/1.5.7/1.5.8/1.5.9/1.5.10/1.5.11/1.5.12/1.5.13/1.5.14/1.5.15/1.5.16/1.5.17/1.5.18/1.5.19/1.5.20/1.5.21 review UI: `createPartnershipReviewPacket`, `replayPartnershipReviewPacket`, `PARTNERSHIP_REVIEW_TOOLS`, `analyzePartnershipReview`, waterfall SVG, copy waterfall, persisted hide holders, Talent/agent/platform, ledger all-hold filter, Three-party joint venture, Podcast host and network, first-breakpoint snapshot, Community hall split, Festival stall split, Pop-up cinema split, Community radio split, School concert split, Sports carnival split, Netball carnival, Swimming carnival split, Athletics carnival split, Cricket carnival split, Tennis carnival split, Basketball carnival split, Volleyball carnival split, Rugby carnival split, Hockey carnival split, Baseball carnival split, Softball carnival split, and keys `f` `w` `l` `b` `c` `p` `t` `d` `k` `h` `a` `m` `v` `i` `o` `j` `q` `x` `y` `z` `,` `.` `/` `;` `[` `]` `'` `<` `>` `:` `-` `=` `"` `_` `{` `}` `+` `|` `~` `!` `@` `(` `)` `#` `*` `&` `%` `$` `^` backtick `5` `6` `7` `8` `9` `0` `1` `2` `3` `4` `PageUp` `PageDown` `Home` `End` `ArrowUp`.
- Press `Insert` to copy last at-hold remaining-to-hold through the new copy control, with the same Markdown. Ignored in inputs. Distinct from `4` last-unbounded remaining and `PageUp` first-unbounded remaining. It is not a forecast.
- Press `ArrowDown` to jump to Copy last at-hold remaining-to-hold, or the Participants heading if that control is missing, when no field is focused. It does not copy. Distinct from `Home` last-unbounded remaining jump and `PageDown` first-unbounded remaining jump.
- Press `ArrowLeft` to jump to Hide the first participant at hold, or the Participants heading if that control is missing, when no field is focused. Distinct from `End` hideFirstParticipantWithoutCapacity and `ArrowUp` hideLastParticipantAtHold. Named keys `Insert`, `ArrowDown`, and `ArrowLeft` are not length-1; existing letter handling stays. Shortcuts return if default is already prevented and stay ignored while typing.
- Added the Lacrosse carnival split starting point, a three-party carnival committee plus ground hire plus first-aid with different cost bases, distinct from Balanced, Thin Margin, Growth at a Cost, Creator take-rate, Three-party JV, Two-party 50/50 studio, Four-party marketplace, Licensor and distributor, Talent, agent, and platform, Three-party joint venture, Podcast host and network, Community hall split, Festival stall split, Pop-up cinema split, Community radio split, School concert split, Sports carnival split, Netball carnival, Swimming carnival split, Athletics carnival split, Cricket carnival split, Tennis carnival split, Basketball carnival split, Volleyball carnival split, Rugby carnival split, Hockey carnival split, Baseball carnival split, and Softball carnival split. Monthly volume 4100, fee 8, addressable 5300, and listed capacities 5000, 6100, and 4100. It is a synthetic starting point, not a live roster. Counts remain counts. Ranking is not a forecast of who will exit.
- Hide the first participant at hold now travels on saved case JSON as optional `hideFirstParticipantAtHold`. Older files omit it and default to showing that roster row. Unknown sibling fields are still rejected. Display only; the solver is unchanged. Hidden rows still count. Distinct from `hideParticipantsAtHold`, which hides every at-hold row, from `hideLastParticipantAtHold`, from `hideFirstParticipantWithoutCapacity`, and from `hideLastParticipantWithoutCapacity`. If only one roster row is currently at hold (remaining-to-hold is 0), hiding first hides that one row. `hideHoldingParticipants`, `hideAllHoldLedger`, `hideZeroShareParticipants`, `hideParticipantsOverCapacity`, `hideParticipantsAtHold`, `hideParticipantsWithoutCapacity`, `hideParticipantsWithSpareCapacity`, `hideParticipantsAtLeastHeadroom`, `hideParticipantsWithinCapacity`, `hideFirstBreakpointParticipant`, `hideFirstOverCapacityParticipant`, `hideLastOverCapacityParticipant`, `hideLastBreakpointParticipant`, `hideLastWithinCapacityParticipant`, `hideFirstWithinCapacityParticipant`, `hideLastSpareCapacityParticipant`, `hideFirstSpareCapacityParticipant`, `hideLastParticipantWithoutCapacity`, `hideFirstParticipantWithoutCapacity`, and `hideLastParticipantAtHold` stay available.
- Print one-pager includes last at-hold remaining-to-hold as one line when the case is valid. The line is an honest empty when none are currently at hold. The saved case is unchanged.
- Copy last at-hold remaining-to-hold copies a one-line Markdown amount of remaining to hold for the last roster row currently at hold, or an honest empty line when none. Clipboard falls back to a textarea. It is distinct from last unbounded remaining-to-hold copy and first unbounded remaining-to-hold copy. It is not a forecast. Copy first unbounded remaining-to-hold stays available. Counts remain counts. Ranking is not a forecast of who will exit.

## 1.5.21

- Keeps the 1.5.0 constraint review and 1.5.1/1.5.2/1.5.3/1.5.4/1.5.5/1.5.6/1.5.7/1.5.8/1.5.9/1.5.10/1.5.11/1.5.12/1.5.13/1.5.14/1.5.15/1.5.16/1.5.17/1.5.18/1.5.19/1.5.20 review UI: `createPartnershipReviewPacket`, `replayPartnershipReviewPacket`, `PARTNERSHIP_REVIEW_TOOLS`, `analyzePartnershipReview`, waterfall SVG, copy waterfall, persisted hide holders, Talent/agent/platform, ledger all-hold filter, Three-party joint venture, Podcast host and network, first-breakpoint snapshot, Community hall split, Festival stall split, Pop-up cinema split, Community radio split, School concert split, Sports carnival split, Netball carnival, Swimming carnival split, Athletics carnival split, Cricket carnival split, Tennis carnival split, Basketball carnival split, Volleyball carnival split, Rugby carnival split, Hockey carnival split, Baseball carnival split, and keys `f` `w` `l` `b` `c` `p` `t` `d` `k` `h` `a` `m` `v` `i` `o` `j` `q` `x` `y` `z` `,` `.` `/` `;` `[` `]` `'` `<` `>` `:` `-` `=` `"` `_` `{` `}` `+` `|` `~` `!` `@` `(` `)` `#` `*` `&` `%` `$` `^` backtick `5` `6` `7` `8` `9` `0` `1` `2` `3` `4`.
- Press `PageUp` to copy first unbounded remaining-to-hold through the new copy control, with the same Markdown. Ignored in inputs. Distinct from `4` last-unbounded remaining, `1` first-spare remaining, and `8` last-spare remaining. It is not a forecast.
- Press `PageDown` to jump to Copy first unbounded remaining-to-hold, or the Participants heading if that control is missing, when no field is focused. It does not copy. Distinct from `Home` last-unbounded remaining jump.
- Press `ArrowUp` to jump to Hide the last participant at hold, or the Participants heading if that control is missing, when no field is focused. Distinct from `End` hideFirstParticipantWithoutCapacity. Named keys `PageUp`, `PageDown`, and `ArrowUp` are not length-1; existing letter handling stays. Shortcuts return if default is already prevented and stay ignored while typing.
- Added the Softball carnival split starting point, a three-party carnival committee plus diamond hire plus first-aid with different cost bases, distinct from Balanced, Thin Margin, Growth at a Cost, Creator take-rate, Three-party JV, Two-party 50/50 studio, Four-party marketplace, Licensor and distributor, Talent, agent, and platform, Three-party joint venture, Podcast host and network, Community hall split, Festival stall split, Pop-up cinema split, Community radio split, School concert split, Sports carnival split, Netball carnival, Swimming carnival split, Athletics carnival split, Cricket carnival split, Tennis carnival split, Basketball carnival split, Volleyball carnival split, Rugby carnival split, Hockey carnival split, and Baseball carnival split. It is a synthetic starting point, not a live roster. Counts remain counts. Ranking is not a forecast of who will exit.
- Hide the last participant at hold now travels on saved case JSON as optional `hideLastParticipantAtHold`. Older files omit it and default to showing that roster row. Unknown sibling fields are still rejected. Display only; the solver is unchanged. Hidden rows still count. Distinct from `hideParticipantsAtHold`, which hides every at-hold row, from `hideLastBreakpointParticipant`, from `hideFirstParticipantWithoutCapacity`, and from `hideLastParticipantWithoutCapacity`. If only one roster row is currently at hold (remaining-to-hold is 0), hiding last hides that one row. `hideHoldingParticipants`, `hideAllHoldLedger`, `hideZeroShareParticipants`, `hideParticipantsOverCapacity`, `hideParticipantsAtHold`, `hideParticipantsWithoutCapacity`, `hideParticipantsWithSpareCapacity`, `hideParticipantsAtLeastHeadroom`, `hideParticipantsWithinCapacity`, `hideFirstBreakpointParticipant`, `hideFirstOverCapacityParticipant`, `hideLastOverCapacityParticipant`, `hideLastBreakpointParticipant`, `hideLastWithinCapacityParticipant`, `hideFirstWithinCapacityParticipant`, `hideLastSpareCapacityParticipant`, `hideFirstSpareCapacityParticipant`, `hideLastParticipantWithoutCapacity`, and `hideFirstParticipantWithoutCapacity` stay available.
- Print one-pager includes first unbounded remaining-to-hold as one line when the case is valid. The line is an honest empty when none have unbounded or omitted capacity. The saved case is unchanged.
- Copy first unbounded remaining-to-hold copies a one-line Markdown amount of remaining to hold for the first roster row whose capacity is unbounded or omitted, or an honest empty line when none. Clipboard falls back to a textarea. It is distinct from last unbounded remaining-to-hold copy, first spare-capacity remaining copy, and last spare-capacity remaining copy. It is not a forecast. Copy last unbounded remaining-to-hold stays available. Counts remain counts. Ranking is not a forecast of who will exit.

## 1.5.20

- Keeps the 1.5.0 constraint review and 1.5.1/1.5.2/1.5.3/1.5.4/1.5.5/1.5.6/1.5.7/1.5.8/1.5.9/1.5.10/1.5.11/1.5.12/1.5.13/1.5.14/1.5.15/1.5.16/1.5.17/1.5.18/1.5.19 review UI: `createPartnershipReviewPacket`, `replayPartnershipReviewPacket`, `PARTNERSHIP_REVIEW_TOOLS`, `analyzePartnershipReview`, waterfall SVG, copy waterfall, persisted hide holders, Talent/agent/platform, ledger all-hold filter, Three-party joint venture, Podcast host and network, first-breakpoint snapshot, Community hall split, Festival stall split, Pop-up cinema split, Community radio split, School concert split, Sports carnival split, Netball carnival, Swimming carnival split, Athletics carnival split, Cricket carnival split, Tennis carnival split, Basketball carnival split, Volleyball carnival split, Rugby carnival split, Hockey carnival split, and keys `f` `w` `l` `b` `c` `p` `t` `d` `k` `h` `a` `m` `v` `i` `o` `j` `q` `x` `y` `z` `,` `.` `/` `;` `[` `]` `'` `<` `>` `:` `-` `=` `"` `_` `{` `}` `+` `|` `~` `!` `@` `(` `)` `#` `*` `&` `%` `$` `^` backtick `5` `6` `7` `8` `9` `0` `1` `2` `3`.
- Press `4` to copy last unbounded remaining-to-hold through the new copy control, with the same Markdown. Ignored in inputs. Distinct from `1` first-spare-capacity remaining and `8` last-spare-capacity remaining. It is not a forecast.
- Press `Home` to jump to Copy last unbounded remaining-to-hold, or the Participants heading if that control is missing, when no field is focused. It does not copy. Distinct from `2` first-spare remaining jump.
- Press `End` to jump to Hide the first participant without listed capacity, or the Participants heading if that control is missing, when no field is focused. Distinct from `3` hideLastParticipantWithoutCapacity.
- Added the Baseball carnival split starting point, a three-party carnival committee plus diamond hire plus first-aid with different cost bases, distinct from Balanced, Thin Margin, Growth at a Cost, Creator take-rate, Three-party JV, Two-party 50/50 studio, Four-party marketplace, Licensor and distributor, Talent, agent, and platform, Three-party joint venture, Podcast host and network, Community hall split, Festival stall split, Pop-up cinema split, Community radio split, School concert split, Sports carnival split, Netball carnival, Swimming carnival split, Athletics carnival split, Cricket carnival split, Tennis carnival split, Basketball carnival split, Volleyball carnival split, Rugby carnival split, and Hockey carnival split. It is a synthetic starting point, not a live roster. Counts remain counts. Ranking is not a forecast of who will exit.
- Hide the first participant without listed capacity now travels on saved case JSON as optional `hideFirstParticipantWithoutCapacity`. Older files omit it and default to showing that roster row. Unknown sibling fields are still rejected. Display only; the solver is unchanged. Hidden rows still count. Distinct from `hideParticipantsWithoutCapacity`, which hides every unbounded or omitted-capacity row, and from `hideLastParticipantWithoutCapacity`, which hides the last unbounded or omitted-capacity row. If only one roster row is unbounded or omitted, hiding first hides that one row. `hideHoldingParticipants`, `hideAllHoldLedger`, `hideZeroShareParticipants`, `hideParticipantsOverCapacity`, `hideParticipantsAtHold`, `hideParticipantsWithoutCapacity`, `hideParticipantsWithSpareCapacity`, `hideParticipantsAtLeastHeadroom`, `hideParticipantsWithinCapacity`, `hideFirstBreakpointParticipant`, `hideFirstOverCapacityParticipant`, `hideLastOverCapacityParticipant`, `hideLastBreakpointParticipant`, `hideLastWithinCapacityParticipant`, `hideFirstWithinCapacityParticipant`, `hideLastSpareCapacityParticipant`, `hideFirstSpareCapacityParticipant`, and `hideLastParticipantWithoutCapacity` stay available.
- Print one-pager includes last unbounded remaining-to-hold as one line when the case is valid. The line is an honest empty when none have unbounded or omitted capacity. The saved case is unchanged.
- Copy last unbounded remaining-to-hold copies a one-line Markdown amount of remaining to hold for the last roster row whose capacity is unbounded or omitted, or an honest empty line when none. Clipboard falls back to a textarea. It is distinct from first spare-capacity remaining copy and last spare-capacity remaining copy. It is not a forecast. Copy first spare-capacity remaining listed capacity stays available. Counts remain counts. Ranking is not a forecast of who will exit.

## 1.5.19

- Keeps the 1.5.0 constraint review and 1.5.1/1.5.2/1.5.3/1.5.4/1.5.5/1.5.6/1.5.7/1.5.8/1.5.9/1.5.10/1.5.11/1.5.12/1.5.13/1.5.14/1.5.15/1.5.16/1.5.17/1.5.18 review UI: `createPartnershipReviewPacket`, `replayPartnershipReviewPacket`, `PARTNERSHIP_REVIEW_TOOLS`, `analyzePartnershipReview`, waterfall SVG, copy waterfall, persisted hide holders, Talent/agent/platform, ledger all-hold filter, Three-party joint venture, Podcast host and network, first-breakpoint snapshot, Community hall split, Festival stall split, Pop-up cinema split, Community radio split, School concert split, Sports carnival split, Netball carnival, Swimming carnival split, Athletics carnival split, Cricket carnival split, Tennis carnival split, Basketball carnival split, Volleyball carnival split, Rugby carnival split, and keys `f` `w` `l` `b` `c` `p` `t` `d` `k` `h` `a` `m` `v` `i` `o` `j` `q` `x` `y` `z` `,` `.` `/` `;` `[` `]` `'` `<` `>` `:` `-` `=` `"` `_` `{` `}` `+` `|` `~` `!` `@` `(` `)` `#` `*` `&` `%` `$` `^` backtick `5` `6` `7` `8` `9` `0`.
- Press `1` to copy first spare-capacity remaining listed capacity through the new copy control, with the same Markdown. Ignored in inputs. Distinct from `$` first-within-capacity remaining and `8` last-spare-capacity remaining. It is not a forecast.
- Press `2` to jump to Copy first spare-capacity remaining listed capacity, or the results heading if that control is missing, when no field is focused. It does not copy. Distinct from `9` last-spare remaining jump and `6` last-within remaining jump.
- Press `3` to jump to Hide the last participant without listed capacity, or the Participants heading if that control is missing, when no field is focused. Distinct from hide-all unbounded (`hideParticipantsWithoutCapacity`), from `0` hide first-spare, and from `7` hide last-spare.
- Added the Hockey carnival split starting point, a three-party carnival committee plus ice hire plus first-aid with different cost bases, distinct from Balanced, Thin Margin, Growth at a Cost, Creator take-rate, Three-party JV, Two-party 50/50 studio, Four-party marketplace, Licensor and distributor, Talent, agent, and platform, Three-party joint venture, Podcast host and network, Community hall split, Festival stall split, Pop-up cinema split, Community radio split, School concert split, Sports carnival split, Netball carnival, Swimming carnival split, Athletics carnival split, Cricket carnival split, Tennis carnival split, Basketball carnival split, Volleyball carnival split, and Rugby carnival split. It is a synthetic starting point, not a live roster. Counts remain counts. Ranking is not a forecast of who will exit.
- Hide the last participant without listed capacity now travels on saved case JSON as optional `hideLastParticipantWithoutCapacity`. Older files omit it and default to showing that roster row. Unknown sibling fields are still rejected. Display only; the solver is unchanged. Hidden rows still count. Distinct from `hideParticipantsWithoutCapacity`, which hides every unbounded or omitted-capacity row. If only one roster row is unbounded or omitted, hiding last hides that one row. `hideHoldingParticipants`, `hideAllHoldLedger`, `hideZeroShareParticipants`, `hideParticipantsOverCapacity`, `hideParticipantsAtHold`, `hideParticipantsWithoutCapacity`, `hideParticipantsWithSpareCapacity`, `hideParticipantsAtLeastHeadroom`, `hideParticipantsWithinCapacity`, `hideFirstBreakpointParticipant`, `hideFirstOverCapacityParticipant`, `hideLastOverCapacityParticipant`, `hideLastBreakpointParticipant`, `hideLastWithinCapacityParticipant`, `hideFirstWithinCapacityParticipant`, `hideLastSpareCapacityParticipant`, and `hideFirstSpareCapacityParticipant` stay available.
- Print one-pager includes first-spare-capacity remaining listed capacity as one line when the case is valid. The line is an honest empty when none have unused listed capacity. The saved case is unchanged.
- Copy first spare-capacity remaining listed capacity copies a one-line Markdown amount of remaining listed capacity for the first roster row currently with unused listed capacity, or an honest empty line when none. Clipboard falls back to a textarea. It is distinct from last-spare-capacity remaining copy and first-within-capacity remaining copy. It is not a forecast. Copy last spare-capacity remaining listed capacity stays available. Counts remain counts. Ranking is not a forecast of who will exit.

## 1.5.17

- Keeps the 1.5.0 constraint review and 1.5.1/1.5.2/1.5.3/1.5.4/1.5.5/1.5.6/1.5.7/1.5.8/1.5.9/1.5.10/1.5.11/1.5.12/1.5.13/1.5.14/1.5.15/1.5.16 review UI: `createPartnershipReviewPacket`, `replayPartnershipReviewPacket`, `PARTNERSHIP_REVIEW_TOOLS`, `analyzePartnershipReview`, waterfall SVG, copy waterfall, persisted hide holders, Talent/agent/platform, ledger all-hold filter, Three-party joint venture, Podcast host and network, first-breakpoint snapshot, Community hall split, Festival stall split, Pop-up cinema split, Community radio split, School concert split, Sports carnival split, Netball carnival, Swimming carnival split, Athletics carnival split, Cricket carnival split, Tennis carnival split, Basketball carnival split, and keys `f` `w` `l` `b` `c` `p` `t` `d` `k` `h` `a` `m` `v` `i` `o` `j` `q` `x` `y` `z` `,` `.` `/` `;` `[` `]` `'` `<` `>` `:` `-` `=` `"` `_` `{` `}` `+` `|` `~` `!` `@` `(` `)` `#` `*` `&` `%` `$` `^` backtick.
- Press `5` to copy last within-capacity remaining listed capacity through the new copy control, with the same Markdown. Ignored in inputs. Distinct from `$` first-within-capacity remaining and `*` last-over-capacity remaining. It is not a forecast.
- Press `6` to jump to Copy last within-capacity remaining listed capacity, or the results heading if that control is missing, when no field is focused. It does not copy. Distinct from `^` first-within remaining jump.
- Press `7` to jump to Hide the last spare-capacity participant, or the Participants heading if that control is missing, when no field is focused. Distinct from backtick first-within hide and `%` last-breakpoint hide.
- Added the Volleyball carnival split starting point, a three-party carnival committee plus court hire plus first-aid with different cost bases, distinct from Balanced, Thin Margin, Growth at a Cost, Creator take-rate, Three-party JV, Two-party 50/50 studio, Four-party marketplace, Licensor and distributor, Talent, agent, and platform, Three-party joint venture, Podcast host and network, Community hall split, Festival stall split, Pop-up cinema split, Community radio split, School concert split, Sports carnival split, Netball carnival, Swimming carnival split, Athletics carnival split, Cricket carnival split, Tennis carnival split, and Basketball carnival split. It is a synthetic starting point, not a live roster. Counts remain counts. Ranking is not a forecast of who will exit.
- Hide the last spare-capacity participant now travels on saved case JSON as optional `hideLastSpareCapacityParticipant`. Older files omit it and default to showing that roster row. Unknown sibling fields are still rejected. Display only; the solver is unchanged. Hidden rows still count. Distinct from `hideParticipantsWithSpareCapacity`, which hides every unused-capacity row, and from `hideLastWithinCapacityParticipant`, which hides the last within-capacity row. `hideHoldingParticipants`, `hideAllHoldLedger`, `hideZeroShareParticipants`, `hideParticipantsOverCapacity`, `hideParticipantsAtHold`, `hideParticipantsWithoutCapacity`, `hideParticipantsWithSpareCapacity`, `hideParticipantsAtLeastHeadroom`, `hideParticipantsWithinCapacity`, `hideFirstBreakpointParticipant`, `hideFirstOverCapacityParticipant`, `hideLastOverCapacityParticipant`, `hideLastBreakpointParticipant`, `hideLastWithinCapacityParticipant`, and `hideFirstWithinCapacityParticipant` stay available.
- Print one-pager includes last-within-capacity remaining listed capacity as one line when the case is valid. The line is an honest empty when none are within listed capacity. The saved case is unchanged.
- Copy last within-capacity remaining listed capacity copies a one-line Markdown amount of remaining listed capacity for the last roster row currently within listed capacity, or an honest empty line when none. Clipboard falls back to a textarea. It is distinct from first-within-capacity remaining copy and last-over-capacity remaining copy. It is not a forecast. Copy last first-breakpoint participant label stays available. Counts remain counts. Ranking is not a forecast of who will exit.

## 1.5.16

- Keeps the 1.5.0 constraint review and 1.5.1/1.5.2/1.5.3/1.5.4/1.5.5/1.5.6/1.5.7/1.5.8/1.5.9/1.5.10/1.5.11/1.5.12/1.5.13/1.5.14/1.5.15 review UI: `createPartnershipReviewPacket`, `replayPartnershipReviewPacket`, `PARTNERSHIP_REVIEW_TOOLS`, `analyzePartnershipReview`, waterfall SVG, copy waterfall, persisted hide holders, Talent/agent/platform, ledger all-hold filter, Three-party joint venture, Podcast host and network, first-breakpoint snapshot, Community hall split, Festival stall split, Pop-up cinema split, Community radio split, School concert split, Sports carnival split, Netball carnival, Swimming carnival split, Athletics carnival split, Cricket carnival split, Tennis carnival split, and keys `f` `w` `l` `b` `c` `p` `t` `d` `k` `h` `a` `m` `v` `i` `o` `j` `q` `x` `y` `z` `,` `.` `/` `;` `[` `]` `'` `<` `>` `:` `-` `=` `"` `_` `{` `}` `+` `|` `~` `!` `@` `(` `)` `#` `*` `&` `%`.
- Press `$` to copy first within-capacity remaining listed capacity through the existing copy control, with the same Markdown. Ignored in inputs. Distinct from `*` last-over-capacity remaining and `~` first-over-capacity remaining.
- Press `^` to jump to Copy first within-capacity remaining listed capacity, or the First breakpoint heading then the Participants heading if that control is missing, when no field is focused. It does not copy. Distinct from `&` last-over-capacity remaining jump.
- Press backtick to jump to Hide the first within-capacity participant, or the Participants heading if that control is missing, when no field is focused. Distinct from `%` last-breakpoint hide and `{` hide all within-capacity.
- Added the Basketball carnival split starting point, a three-party carnival committee plus stadium hire plus first-aid with different cost bases, distinct from Balanced, Thin Margin, Growth at a Cost, Creator take-rate, Three-party JV, Two-party 50/50 studio, Four-party marketplace, Licensor and distributor, Talent, agent, and platform, Three-party joint venture, Podcast host and network, Community hall split, Festival stall split, Pop-up cinema split, Community radio split, School concert split, Sports carnival split, Netball carnival, Swimming carnival split, Athletics carnival split, Cricket carnival split, and Tennis carnival split. It is a synthetic starting point, not a live roster.
- Hide the first within-capacity participant now travels on saved case JSON as optional `hideFirstWithinCapacityParticipant`. Older files omit it and default to showing that roster row. Unknown sibling fields are still rejected. Display only; the solver is unchanged. Distinct from `hideLastWithinCapacityParticipant`, which hides the last within-capacity row, and from `hideParticipantsWithinCapacity`, which hides every within-capacity row. `hideHoldingParticipants`, `hideAllHoldLedger`, `hideZeroShareParticipants`, `hideParticipantsOverCapacity`, `hideParticipantsAtHold`, `hideParticipantsWithoutCapacity`, `hideParticipantsWithSpareCapacity`, `hideParticipantsAtLeastHeadroom`, `hideParticipantsWithinCapacity`, `hideFirstBreakpointParticipant`, `hideFirstOverCapacityParticipant`, `hideLastOverCapacityParticipant`, `hideLastBreakpointParticipant`, and `hideLastWithinCapacityParticipant` stay available.
- Print one-pager includes first-within-capacity remaining listed capacity as one line when the case is valid. The line is an honest empty when none are within listed capacity. The saved case is unchanged.
- Copy first within-capacity remaining listed capacity copies a one-line Markdown amount of remaining listed capacity for the first roster row currently within listed capacity, or an honest empty line when none. Clipboard falls back to a textarea. It is distinct from last-over-capacity remaining copy and first-over-capacity remaining copy. It is not a forecast. Copy last first-breakpoint participant label stays available. Counts remain counts. Ranking is not a forecast of who will exit.

## 1.5.15

- Keeps the 1.5.0 constraint review and 1.5.1/1.5.2/1.5.3/1.5.4/1.5.5/1.5.6/1.5.7/1.5.8/1.5.9/1.5.10/1.5.11/1.5.12/1.5.13/1.5.14 review UI: `createPartnershipReviewPacket`, `replayPartnershipReviewPacket`, `PARTNERSHIP_REVIEW_TOOLS`, `analyzePartnershipReview`, waterfall SVG, copy waterfall, persisted hide holders, Talent/agent/platform, ledger all-hold filter, Three-party joint venture, Podcast host and network, first-breakpoint snapshot, Community hall split, Festival stall split, Pop-up cinema split, Community radio split, School concert split, Sports carnival split, Netball carnival, Swimming carnival split, Athletics carnival split, Cricket carnival split, and keys `f` `w` `l` `b` `c` `p` `t` `d` `k` `h` `a` `m` `v` `i` `o` `j` `q` `x` `y` `z` `,` `.` `/` `;` `[` `]` `'` `<` `>` `:` `-` `=` `"` `_` `{` `}` `+` `|` `~` `!` `@` `(` `)` `#`.
- Press `*` to copy last over-capacity remaining listed capacity through the existing copy control, with the same Markdown. Ignored in inputs. Distinct from `(` last-over-capacity label and `~` first-over-capacity remaining.
- Press `&` to jump to Copy last over-capacity remaining listed capacity, or the First breakpoint heading then the Participants heading if that control is missing, when no field is focused. It does not copy. Distinct from `)` last-over-capacity label jump.
- Press `%` to jump to Hide the last first-breakpoint participant, or the Participants heading if that control is missing, when no field is focused. Distinct from `#` last-over-capacity hide.
- Added the Tennis carnival split starting point, a three-party carnival committee plus court hire plus first-aid with different cost bases, distinct from Balanced, Thin Margin, Growth at a Cost, Creator take-rate, Three-party JV, Two-party 50/50 studio, Four-party marketplace, Licensor and distributor, Talent, agent, and platform, Three-party joint venture, Podcast host and network, Community hall split, Festival stall split, Pop-up cinema split, Community radio split, School concert split, Sports carnival split, Netball carnival, Swimming carnival split, Athletics carnival split, and Cricket carnival split. It is a synthetic starting point, not a live roster.
- Hide the last within-capacity participant now travels on saved case JSON as optional `hideLastWithinCapacityParticipant`. Older files omit it and default to showing that roster row. Unknown sibling fields are still rejected. Display only; the solver is unchanged. Distinct from `hideParticipantsWithinCapacity`, which hides every within-capacity row, and from `hideLastOverCapacityParticipant`. `hideHoldingParticipants`, `hideAllHoldLedger`, `hideZeroShareParticipants`, `hideParticipantsOverCapacity`, `hideParticipantsAtHold`, `hideParticipantsWithoutCapacity`, `hideParticipantsWithSpareCapacity`, `hideParticipantsAtLeastHeadroom`, `hideParticipantsWithinCapacity`, `hideFirstBreakpointParticipant`, `hideFirstOverCapacityParticipant`, `hideLastOverCapacityParticipant`, and `hideLastBreakpointParticipant` stay available.
- Print one-pager includes last-over-capacity remaining listed capacity as one line when the case is valid. The line is an honest empty when none are over. The saved case is unchanged.
- Copy last first-breakpoint participant label copies a one-line Markdown name of the last roster row that is the first-breakpoint, or an honest empty line when none. Clipboard falls back to a textarea. It is distinct from first-breakpoint label copy. It is not a forecast. Counts remain counts. Ranking is not a forecast of who will exit.

## 1.5.14

- Keeps the 1.5.0 constraint review and 1.5.1/1.5.2/1.5.3/1.5.4/1.5.5/1.5.6/1.5.7/1.5.8/1.5.9/1.5.10/1.5.11/1.5.12/1.5.13 review UI: `createPartnershipReviewPacket`, `replayPartnershipReviewPacket`, `PARTNERSHIP_REVIEW_TOOLS`, `analyzePartnershipReview`, waterfall SVG, copy waterfall, persisted hide holders, Talent/agent/platform, ledger all-hold filter, Three-party joint venture, Podcast host and network, first-breakpoint snapshot, Community hall split, Festival stall split, Pop-up cinema split, Community radio split, School concert split, Sports carnival split, Netball carnival, Swimming carnival split, Athletics carnival split, and keys `f` `w` `l` `b` `c` `p` `t` `d` `k` `h` `a` `m` `v` `i` `o` `j` `q` `x` `y` `z` `,` `.` `/` `;` `[` `]` `'` `<` `>` `:` `-` `=` `"` `_` `{` `}` `+` `|` `~` `!` `@`.
- Press `(` to copy the last over-capacity participant label through the existing copy control, with the same Markdown. Ignored in inputs. Distinct from `}` first over-capacity label and `~` first-over-capacity remaining.
- Press `)` to jump to Copy last over-capacity participant label, or the First breakpoint heading then the Participants heading if that control is missing, when no field is focused. It does not copy. Distinct from `!` first-over-capacity remaining jump.
- Press `#` to jump to Hide the last over-capacity participant, or the Participants heading if that control is missing, when no field is focused. Distinct from `@` first over-capacity hide.
- Added the Cricket carnival split starting point, a three-party carnival committee plus pitch hire plus first-aid with different cost bases, distinct from Balanced, Thin Margin, Growth at a Cost, Creator take-rate, Three-party JV, Two-party 50/50 studio, Four-party marketplace, Licensor and distributor, Talent, agent, and platform, Three-party joint venture, Podcast host and network, Community hall split, Festival stall split, Pop-up cinema split, Community radio split, School concert split, Sports carnival split, Netball carnival, Swimming carnival split, and Athletics carnival split. It is a synthetic starting point, not a live roster.
- Hide the last first-breakpoint participant now travels on saved case JSON as optional `hideLastBreakpointParticipant`. Older files omit it and default to showing that roster row. Unknown sibling fields are still rejected. Display only; the solver is unchanged. Distinct from `hideFirstBreakpointParticipant`, which hides the first-breakpoint row. If the model exposes a single first-breakpoint participant, hiding last first-breakpoint hides that one row. `hideHoldingParticipants`, `hideAllHoldLedger`, `hideZeroShareParticipants`, `hideParticipantsOverCapacity`, `hideParticipantsAtHold`, `hideParticipantsWithoutCapacity`, `hideParticipantsWithSpareCapacity`, `hideParticipantsAtLeastHeadroom`, `hideParticipantsWithinCapacity`, `hideFirstBreakpointParticipant`, `hideFirstOverCapacityParticipant`, and `hideLastOverCapacityParticipant` stay available.
- Print one-pager includes the last over-capacity participant label as one line when the case is valid. The line is an honest empty when none are over. The saved case is unchanged.
- Copy last over-capacity remaining listed capacity copies a one-line Markdown amount of how far over listed capacity that last over-capacity participant currently is, or an honest empty line when none. Clipboard falls back to a textarea. It is distinct from first-over-capacity remaining copy and last-over-capacity label copy. It is not a forecast. Counts remain counts. Ranking is not a forecast of who will exit.

## 1.5.13

- Keeps the 1.5.0 constraint review and 1.5.1/1.5.2/1.5.3/1.5.4/1.5.5/1.5.6/1.5.7/1.5.8/1.5.9/1.5.10/1.5.11/1.5.12 review UI: `createPartnershipReviewPacket`, `replayPartnershipReviewPacket`, `PARTNERSHIP_REVIEW_TOOLS`, `analyzePartnershipReview`, waterfall SVG, copy waterfall, persisted hide holders, Talent/agent/platform, ledger all-hold filter, Three-party joint venture, Podcast host and network, first-breakpoint snapshot, Community hall split, Festival stall split, Pop-up cinema split, Community radio split, School concert split, Sports carnival split, Netball carnival, Swimming carnival split, and keys `f` `w` `l` `b` `c` `p` `t` `d` `k` `h` `a` `m` `v` `i` `o` `j` `q` `x` `y` `z` `,` `.` `/` `;` `[` `]` `'` `<` `>` `:` `-` `=` `"` `_` `{` `}` `+` `|`.
- Press `~` to copy first-over-capacity remaining listed capacity through the existing copy control, with the same Markdown. Ignored in inputs. Distinct from `}` first over-capacity label and `"` over-capacity count.
- Press `!` to jump to Copy first over-capacity remaining listed capacity, or the First breakpoint heading then the Participants heading if that control is missing, when no field is focused. It does not copy. Distinct from `+` first-over-capacity label jump.
- Press `@` to jump to Hide the first over-capacity participant, or the Participants heading if that control is missing, when no field is focused. Distinct from `|` first-breakpoint hide and `{` within-capacity hide.
- Added the Athletics carnival split starting point, a three-party carnival committee plus track hire plus first-aid with different cost bases, distinct from Balanced, Thin Margin, Growth at a Cost, Creator take-rate, Three-party JV, Two-party 50/50 studio, Four-party marketplace, Licensor and distributor, Talent, agent, and platform, Three-party joint venture, Podcast host and network, Community hall split, Festival stall split, Pop-up cinema split, Community radio split, School concert split, Sports carnival split, Netball carnival, and Swimming carnival split. It is a synthetic starting point, not a live roster.
- Hide the last over-capacity participant now travels on saved case JSON as optional `hideLastOverCapacityParticipant`. Older files omit it and default to showing that roster row. Unknown sibling fields are still rejected. Display only; the solver is unchanged. Distinct from `hideFirstOverCapacityParticipant`, which hides the first over-capacity row, and from `hideParticipantsOverCapacity`, which hides every over-capacity row. `hideHoldingParticipants`, `hideAllHoldLedger`, `hideZeroShareParticipants`, `hideParticipantsOverCapacity`, `hideParticipantsAtHold`, `hideParticipantsWithoutCapacity`, `hideParticipantsWithSpareCapacity`, `hideParticipantsAtLeastHeadroom`, `hideParticipantsWithinCapacity`, `hideFirstBreakpointParticipant`, and `hideFirstOverCapacityParticipant` stay available.
- Print one-pager includes first-over-capacity remaining listed capacity as one line when the case is valid. The line is an honest empty when none are over. The saved case is unchanged.
- Copy last over-capacity participant label copies a one-line Markdown name of the last roster participant currently over listed capacity, or an honest empty line when none. Clipboard falls back to a textarea. It is distinct from first-over-capacity label copy and remaining copy. It is not a forecast. Counts remain counts. Ranking is not a forecast of who will exit.

## 1.5.12

- Keeps the 1.5.0 constraint review and 1.5.1/1.5.2/1.5.3/1.5.4/1.5.5/1.5.6/1.5.7/1.5.8/1.5.9/1.5.10/1.5.11 review UI: `createPartnershipReviewPacket`, `replayPartnershipReviewPacket`, `PARTNERSHIP_REVIEW_TOOLS`, `analyzePartnershipReview`, waterfall SVG, copy waterfall, persisted hide holders, Talent/agent/platform, ledger all-hold filter, Three-party joint venture, Podcast host and network, first-breakpoint snapshot, Community hall split, Festival stall split, Pop-up cinema split, Community radio split, School concert split, Sports carnival split, Netball carnival, and keys `f` `w` `l` `b` `c` `p` `t` `d` `k` `h` `a` `m` `v` `i` `o` `j` `q` `x` `y` `z` `,` `.` `/` `;` `[` `]` `'` `<` `>` `:` `-` `=` `"` `_` `{`.
- Press `}` to copy the first over-capacity participant label through the existing copy control, with the same Markdown. Ignored in inputs. Distinct from `"` over-capacity count.
- Press `+` (Shift+=, not `=`) to jump to Copy first over-capacity participant label, or the First breakpoint heading then the Participants heading if that control is missing, when no field is focused. It does not copy. Distinct from `=` least-headroom hide.
- Press `|` to jump to Hide the first-breakpoint participant, or the Participants heading if that control is missing, when no field is focused. Distinct from `{` within-capacity hide.
- Added the Swimming carnival split starting point, a three-party carnival committee plus pool operations plus canteen with different cost bases, distinct from Balanced, Thin Margin, Growth at a Cost, Creator take-rate, Three-party JV, Two-party 50/50 studio, Four-party marketplace, Licensor and distributor, Talent, agent, and platform, Three-party joint venture, Podcast host and network, Community hall split, Festival stall split, Pop-up cinema split, Community radio split, School concert split, Sports carnival split, and Netball carnival. It is a synthetic starting point, not a live roster.
- Hide the first over-capacity participant now travels on saved case JSON as optional `hideFirstOverCapacityParticipant`. Older files omit it and default to showing that roster row. Unknown sibling fields are still rejected. Display only; the solver is unchanged. Distinct from `hideParticipantsOverCapacity`, which hides every over-capacity row. `hideHoldingParticipants`, `hideAllHoldLedger`, `hideZeroShareParticipants`, `hideParticipantsOverCapacity`, `hideParticipantsAtHold`, `hideParticipantsWithoutCapacity`, `hideParticipantsWithSpareCapacity`, `hideParticipantsAtLeastHeadroom`, `hideParticipantsWithinCapacity`, and `hideFirstBreakpointParticipant` stay available.
- Print one-pager includes the first over-capacity participant label as one line when the case is valid. The line is an honest empty when none are over. The saved case is unchanged.
- Copy first over-capacity remaining listed capacity copies a one-line Markdown amount of how far over listed capacity that first over-capacity participant currently is, or an honest empty line when none. Clipboard falls back to a textarea. It is distinct from over-capacity count copy, first-over-capacity label copy, volume-to-hold copy, and remaining-to-hold copy. It is not a forecast.

## 1.5.11

- Keeps the 1.5.0 constraint review and 1.5.1/1.5.2/1.5.3/1.5.4/1.5.5/1.5.6/1.5.7/1.5.8/1.5.9/1.5.10 review UI: `createPartnershipReviewPacket`, `replayPartnershipReviewPacket`, `PARTNERSHIP_REVIEW_TOOLS`, `analyzePartnershipReview`, waterfall SVG, copy waterfall, persisted hide holders, Talent/agent/platform, ledger all-hold filter, Three-party joint venture, Podcast host and network, first-breakpoint snapshot, Community hall split, Festival stall split, Pop-up cinema split, Community radio split, School concert split, Sports carnival split, and keys `f` `w` `l` `b` `c` `p` `t` `d` `k` `h` `a` `m` `v` `i` `o` `j` `q` `x` `y` `z` `,` `.` `/` `;` `[` `]` `'` `<` `>` `:` `-` `=`.
- Press `"` (quote) to copy over-capacity participant count through the existing copy control, with the same Markdown. Ignored in inputs. Distinct from `:` volume-to-hold and `'` remaining-to-hold.
- Press `_` to jump to Copy over-capacity participant count, or the Participants heading if that control is missing, when no field is focused. Shortcuts stay ignored while a field is focused.
- Press `{` to jump to Hide participants who are within listed capacity, or the Participants heading if that control is missing, when no field is focused. Distinct from `=` least-headroom hide.
- Added the Netball carnival starting point, a three-party carnival committee plus canteen plus first-aid with different cost bases, distinct from Balanced, Thin Margin, Growth at a Cost, Creator take-rate, Three-party JV, Two-party 50/50 studio, Four-party marketplace, Licensor and distributor, Talent, agent, and platform, Three-party joint venture, Podcast host and network, Community hall split, Festival stall split, Pop-up cinema split, Community radio split, School concert split, and Sports carnival split. It is a synthetic starting point, not a live roster.
- Hide the first-breakpoint participant now travels on saved case JSON as optional `hideFirstBreakpointParticipant`. Older files omit it and default to showing that roster row. Unknown sibling fields are still rejected. Display only; the solver is unchanged. `hideHoldingParticipants`, `hideAllHoldLedger`, `hideZeroShareParticipants`, `hideParticipantsOverCapacity`, `hideParticipantsAtHold`, `hideParticipantsWithoutCapacity`, `hideParticipantsWithSpareCapacity`, `hideParticipantsAtLeastHeadroom`, and `hideParticipantsWithinCapacity` stay available.
- Print one-pager includes the over-capacity participant count as one line when the case is valid. The line is an honest zero when none are over. The saved case is unchanged.
- Copy first over-capacity participant label copies a one-line Markdown name of the first roster participant currently over listed capacity, or an honest empty line when none. Clipboard falls back to a textarea. It is distinct from first-breakpoint label copy, least-headroom copy, and over-capacity count copy. It is not a forecast.

## 1.5.10

- Keeps the 1.5.0 constraint review and 1.5.1/1.5.2/1.5.3/1.5.4/1.5.5/1.5.6/1.5.7/1.5.8/1.5.9 review UI: `createPartnershipReviewPacket`, `replayPartnershipReviewPacket`, `PARTNERSHIP_REVIEW_TOOLS`, `analyzePartnershipReview`, waterfall SVG, copy waterfall, persisted hide holders, Talent/agent/platform, ledger all-hold filter, Three-party joint venture, Podcast host and network, first-breakpoint snapshot, Community hall split, Festival stall split, Pop-up cinema split, Community radio split, School concert split, and keys `f` `w` `l` `b` `c` `p` `t` `d` `k` `h` `a` `m` `v` `i` `o` `j` `q` `x` `y` `z` `,` `.` `/` `;` `[` `]` `'` `<` `>`.
- Press `:` (colon) to copy first-breakpoint volume-to-hold through the existing copy control, with the same Markdown. Ignored in inputs. Distinct from `'` remaining-to-hold, `;` least-headroom label, and `,` first-breakpoint label.
- Press `-` to jump to Copy first-breakpoint volume-to-hold, or the First breakpoint heading if that control is missing, when no field is focused. Shortcuts stay ignored while a field is focused.
- Press `=` to jump to Hide the least-headroom participant, or the Participants heading if that control is missing, when no field is focused. Distinct from `>` spare-capacity hide.
- Added the Sports carnival split starting point, a three-party carnival committee plus ride operator plus ticket booth with different cost bases, distinct from Balanced, Thin Margin, Growth at a Cost, Creator take-rate, Three-party JV, Two-party 50/50 studio, Four-party marketplace, Licensor and distributor, Talent, agent, and platform, Three-party joint venture, Podcast host and network, Community hall split, Festival stall split, Pop-up cinema split, Community radio split, and School concert split. It is a synthetic starting point, not a live roster.
- Hide participants who are within listed capacity (not over capacity) now travels on saved case JSON as optional `hideParticipantsWithinCapacity`. Older files omit it and default to showing those roster rows. Unknown sibling fields are still rejected. Display only; the solver is unchanged. `hideHoldingParticipants`, `hideAllHoldLedger`, `hideZeroShareParticipants`, `hideParticipantsOverCapacity`, `hideParticipantsAtHold`, `hideParticipantsWithoutCapacity`, `hideParticipantsWithSpareCapacity`, and `hideParticipantsAtLeastHeadroom` stay available.
- Print one-pager includes first-breakpoint volume-to-hold as one line when the case is valid. The line is honest when that volume is unavailable. The saved case is unchanged.
- Copy over-capacity participant count copies a one-line Markdown count of roster rows currently over listed capacity, or an honest zero when none are over. Clipboard falls back to a textarea. It is distinct from volume-to-hold copy, remaining-to-hold copy, and least-headroom copy. It is not a forecast.

## 1.5.9

- Keeps the 1.5.0 constraint review and 1.5.1/1.5.2/1.5.3/1.5.4/1.5.5/1.5.6/1.5.7/1.5.8 review UI: `createPartnershipReviewPacket`, `replayPartnershipReviewPacket`, `PARTNERSHIP_REVIEW_TOOLS`, `analyzePartnershipReview`, waterfall SVG, copy waterfall, persisted hide holders, Talent/agent/platform, ledger all-hold filter, Three-party joint venture, Podcast host and network, first-breakpoint snapshot, Community hall split, Festival stall split, Pop-up cinema split, Community radio split, and keys `f` `w` `l` `b` `c` `p` `t` `d` `k` `h` `a` `m` `v` `i` `o` `j` `q` `x` `y` `z` `,` `.` `/` `;` `[` `]`.
- Press `'` (apostrophe, no shift) to copy first-breakpoint remaining-to-hold through the existing copy control, with the same Markdown. Ignored in inputs.
- Press `<` to jump to Copy first-breakpoint remaining-to-hold, or the First breakpoint heading if that control is missing, when no field is focused. Shortcuts stay ignored while a field is focused.
- Press `>` to jump to Hide participants with unused listed capacity, or the Participants heading if that control is missing, when no field is focused.
- Added the School concert split starting point, a three-party concert venue plus PTA plus ticketing with different cost bases, distinct from Balanced, Thin Margin, Growth at a Cost, Creator take-rate, Three-party JV, Two-party 50/50 studio, Four-party marketplace, Licensor and distributor, Talent, agent, and platform, Three-party joint venture, Podcast host and network, Community hall split, Festival stall split, Pop-up cinema split, and Community radio split. It is a synthetic starting point, not a live roster.
- Hide the least-headroom roster row now travels on saved case JSON as optional `hideParticipantsAtLeastHeadroom`. Older files omit it and default to showing that roster row. Unknown sibling fields are still rejected. Display only; the solver is unchanged. `hideHoldingParticipants`, `hideAllHoldLedger`, `hideZeroShareParticipants`, `hideParticipantsOverCapacity`, `hideParticipantsAtHold`, `hideParticipantsWithoutCapacity`, and `hideParticipantsWithSpareCapacity` stay available.
- Print one-pager includes first-breakpoint remaining-to-hold as one line when the case is valid. The line is honest when that amount is unavailable. The saved case is unchanged.
- Copy first-breakpoint volume-to-hold copies a one-line Markdown exit volume for that first-breakpoint participant, matching the existing model field, or an honest empty line when that volume is unavailable. Clipboard falls back to a textarea. It is distinct from remaining-to-hold copy, least-headroom copy, and first-breakpoint label copy.

## 1.5.8

- Keeps the 1.5.0 constraint review and 1.5.1/1.5.2/1.5.3/1.5.4/1.5.5/1.5.6/1.5.7 review UI: `createPartnershipReviewPacket`, `replayPartnershipReviewPacket`, `PARTNERSHIP_REVIEW_TOOLS`, `analyzePartnershipReview`, waterfall SVG, copy waterfall, persisted hide holders, Talent/agent/platform, ledger all-hold filter, Three-party joint venture, Podcast host and network, first-breakpoint snapshot, Community hall split, Festival stall split, Pop-up cinema split, and keys `f` `w` `l` `b` `c` `p` `t` `d` `k` `h` `a` `m` `v` `i` `o` `j` `q` `x` `y` `z` `,` `.` `/`.
- Press `;` (semicolon, no shift) to copy the least-headroom participant label through the existing copy control, with the same Markdown. Ignored in inputs.
- Press `[` to jump to Copy least-headroom participant label, or the First breakpoint heading or results heading if that control is missing, when no field is focused. Shortcuts stay ignored while a field is focused.
- Press `]` to jump to Print one-pager, or the print / one-pager heading if that control is missing, when no field is focused.
- Added the Community radio split starting point, a three-party presenter plus station plus underwriter with different cost bases, distinct from Balanced, Thin Margin, Growth at a Cost, Creator take-rate, Three-party JV, Two-party 50/50 studio, Four-party marketplace, Licensor and distributor, Talent, agent, and platform, Three-party joint venture, Podcast host and network, Community hall split, Festival stall split, and Pop-up cinema split. It is a synthetic starting point, not a live roster.
- Hide participants with unused listed capacity now travels on saved case JSON as optional `hideParticipantsWithSpareCapacity`. Older files omit it and default to showing those roster rows. Unknown sibling fields are still rejected. Display only; the solver is unchanged. `hideHoldingParticipants`, `hideAllHoldLedger`, `hideZeroShareParticipants`, `hideParticipantsOverCapacity`, `hideParticipantsAtHold`, and `hideParticipantsWithoutCapacity` stay available.
- Print one-pager includes the least-headroom participant label when the case is valid. It is a synthetic ranking, not a forecast. The saved case is unchanged.
- Copy first-breakpoint remaining-to-hold copies a one-line Markdown volume or share still needed for that first-breakpoint participant, matching existing model fields, or an honest empty line when that amount is unavailable. Clipboard falls back to a textarea. It is distinct from first-breakpoint participant copy and least-headroom copy.

## 1.5.7

- Keeps the 1.5.0 constraint review and 1.5.1/1.5.2/1.5.3/1.5.4/1.5.5/1.5.6 review UI: `createPartnershipReviewPacket`, `replayPartnershipReviewPacket`, `PARTNERSHIP_REVIEW_TOOLS`, `analyzePartnershipReview`, waterfall SVG, copy waterfall, persisted hide holders, Talent/agent/platform, ledger all-hold filter, Three-party joint venture, Podcast host and network, first-breakpoint snapshot, Community hall split, Festival stall split, and keys `f` `w` `l` `b` `c` `p` `t` `d` `k` `h` `a` `m` `v` `i` `o` `j` `q` `x` `y` `z`.
- Press `,` (comma, no shift) to copy the first-breakpoint participant label through the existing copy control, with the same Markdown. Ignored in inputs.
- Press `.` (period, no shift) to jump to Copy first-breakpoint participant label, or the First breakpoint heading if that control is missing, when no field is focused. Shortcuts stay ignored while a field is focused.
- Press `/` without Shift to jump to Copy deal title and currency, or the Shared deal heading if that control is missing, when no field is focused. Shift+/ stays help (`?`).
- Added the Pop-up cinema split starting point, a three-party venue plus projectionist plus ticket desk with different cost bases, distinct from Balanced, Thin Margin, Growth at a Cost, Creator take-rate, Three-party JV, Two-party 50/50 studio, Four-party marketplace, Licensor and distributor, Talent, agent, and platform, Three-party joint venture, Podcast host and network, Community hall split, and Festival stall split. It is a synthetic starting point, not a live roster.
- Hide participants whose capacity is unbounded or omitted now travels on saved case JSON as optional `hideParticipantsWithoutCapacity`. Older files omit it and default to showing those roster rows. Unknown sibling fields are still rejected. Display only; the solver is unchanged. `hideHoldingParticipants`, `hideAllHoldLedger`, `hideZeroShareParticipants`, `hideParticipantsOverCapacity`, and `hideParticipantsAtHold` stay available.
- Print one-pager includes the first-breakpoint participant label line when the case is valid. It is a synthetic ranking, not a forecast. The saved case is unchanged.
- Copy least-headroom participant label copies a one-line Markdown name, or an honest empty line when that name is unavailable. Clipboard falls back to a textarea. It is distinct from first-breakpoint participant copy.

## 1.5.6

- Keeps the 1.5.0 constraint review and 1.5.1/1.5.2/1.5.3/1.5.4/1.5.5 review UI: `createPartnershipReviewPacket`, `replayPartnershipReviewPacket`, `PARTNERSHIP_REVIEW_TOOLS`, `analyzePartnershipReview`, waterfall SVG, copy waterfall, persisted hide holders, Talent/agent/platform, ledger all-hold filter, Three-party joint venture, Podcast host and network, first-breakpoint snapshot, Community hall split, and keys `f` `w` `l` `b` `c` `p` `t` `d` `k` `h` `a` `m` `v` `i` `o` `j` `q`.
- Press `x` to jump to the first roster row currently over listed capacity, or the Participants heading if none, when no field is focused. Shortcuts stay ignored while a field is focused.
- Press `y` to copy deal notes as one-line Markdown, or an honest empty line when notes are omitted. Clipboard falls back to a textarea. It is not a forecast.
- Press `z` to jump to Copy deal title and currency, or the Shared deal heading if that control is missing, when no field is focused.
- Added the Festival stall split starting point, a three-party stallholder plus site manager plus ticket office with different cost bases, distinct from Balanced, Thin Margin, Growth at a Cost, Creator take-rate, Three-party JV, Two-party 50/50 studio, Four-party marketplace, Licensor and distributor, Talent, agent, and platform, Three-party joint venture, Podcast host and network, and Community hall split. It is a synthetic starting point, not a live roster.
- Hide participants whose current volume headroom is at or above a hold with no listed capacity breach now travels on saved case JSON as optional `hideParticipantsAtHold`. Older files omit it and default to showing those roster rows. Unknown sibling fields are still rejected. Display only; the solver is unchanged. `hideHoldingParticipants`, `hideAllHoldLedger`, `hideZeroShareParticipants`, and `hideParticipantsOverCapacity` stay available.
- Print one-pager includes a currency code line when the case is valid. The saved case is unchanged.
- Copy first-breakpoint participant label copies a one-line Markdown name with a synthetic-not-forecast notice. Clipboard falls back to a textarea. It is distinct from capacity utilization and allocation-balance copy.

## 1.5.5

- Keeps the 1.5.0 constraint review and 1.5.1/1.5.2/1.5.3/1.5.4 review UI: `createPartnershipReviewPacket`, `replayPartnershipReviewPacket`, `PARTNERSHIP_REVIEW_TOOLS`, `analyzePartnershipReview`, waterfall SVG, copy waterfall, persisted hide holders, Talent/agent/platform, ledger all-hold filter, Three-party joint venture, Podcast host and network, first-breakpoint snapshot, and keys `f` `w` `l` `b` `c` `p` `t` `d` `k` `h` `a` `m` `v`.
- Press `i` to jump to the inspect or compare cases heading when no field is focused. Shortcuts stay ignored while a field is focused.
- Press `o` to jump to the Operating region heading when no field is focused.
- Press `j` to copy capacity utilization as Markdown. Clipboard falls back to a textarea. It is a display, not a forecast.
- Press `q` to jump to Equal split, or Normalize current shares if that control is missing, when no field is focused.
- Added the Community hall split starting point, a three-party venue plus promoter plus sound with different cost bases, distinct from Balanced, Thin Margin, Growth at a Cost, Creator take-rate, Three-party JV, Two-party 50/50 studio, Four-party marketplace, Licensor and distributor, Talent, agent, and platform, Three-party joint venture, and Podcast host and network. It is a synthetic starting point, not a live roster.
- Hide participants whose volume is above listed capacity now travels on saved case JSON as optional `hideParticipantsOverCapacity`. Older files omit it and default to showing those roster rows. Unknown sibling fields are still rejected. Display only; the solver is unchanged. `hideHoldingParticipants`, `hideAllHoldLedger`, and `hideZeroShareParticipants` stay available.
- Copy deal title and currency copies a one-line Markdown summary, or an honest empty line when both are omitted. Clipboard falls back to a textarea. It is display text, not a forecast.
- Print one-pager includes a deal title line when the case is valid. The saved case is unchanged.

## 1.5.4

- Keeps the 1.5.0 constraint review and 1.5.1/1.5.2/1.5.3 review UI: `createPartnershipReviewPacket`, `replayPartnershipReviewPacket`, `PARTNERSHIP_REVIEW_TOOLS`, `analyzePartnershipReview`, waterfall SVG, copy waterfall, persisted hide holders, Talent/agent/platform, ledger all-hold filter, Three-party joint venture, and keys `f` `w` `l` `b` `c` `p` `t` `d` `k`.
- Press `h` to jump to the least-headroom participant card, or the Participants heading if none, when no field is focused. Shortcuts stay ignored while a field is focused.
- Press `a` to jump to Add participant when no field is focused.
- Press `m` to jump to deal notes when no field is focused.
- Press `v` to jump to the viability card when no field is focused.
- Copy allocation balance names missing or excess revenue share, or an honest empty line when shares are missing. Clipboard falls back to a textarea. It is not a negotiated allocation.
- Added the Podcast host and network starting point, a two-party host plus network with different cost bases, distinct from Balanced, Thin Margin, Growth at a Cost, Creator take-rate, Three-party JV, Two-party 50/50 studio, Four-party marketplace, Licensor and distributor, Talent, agent, and platform, and Three-party joint venture. It is a synthetic starting point, not a live roster.
- Hide participants with zero revenue share now travels on saved case JSON as optional `hideZeroShareParticipants`. Older files omit it and default to showing those roster rows. Unknown sibling fields are still rejected. Display only; the solver is unchanged. `hideHoldingParticipants` and `hideAllHoldLedger` stay available.
- Print one-pager includes an allocation-balance line when the case is valid. The saved case is unchanged.
- Copy first-breakpoint snapshot copies a one-line Markdown ranking with a synthetic-not-forecast notice. Clipboard falls back to a textarea. It is distinct from tornado, waterfall, operating-region, and allocation-balance copy.

## 1.5.3

- Keeps the 1.5.0 constraint review and 1.5.1/1.5.2 review UI: `createPartnershipReviewPacket`, `replayPartnershipReviewPacket`, `PARTNERSHIP_REVIEW_TOOLS`, `analyzePartnershipReview`, waterfall SVG, copy waterfall, persisted hide holders, Talent/agent/platform, ledger all-hold filter, and keys `f` `w` `l` `b` `c` `p`.
- Press `t` to jump to the tornado chart heading when no field is focused. Shortcuts stay ignored while a field is focused.
- Copy tornado copies each participant, shock axis, and bounded percentage as Markdown. Clipboard falls back to a textarea. It is a comparison aid, not a forecast.
- Press `d` to jump to the Shared deal heading, which includes deal notes, when no field is focused.
- Copy operating region copies the displayed fee and volume sensitivity grid as Markdown. Display only.
- Added the Three-party joint venture starting point, a synthetic operator plus capital partner plus operator-talent with different cost bases, distinct from Balanced, Two-party 50/50 studio, Four-party marketplace, Licensor and distributor, Talent, agent, and platform, and Three-party JV. It is a synthetic starting point, not a live roster.
- Hide participants who hold in every tested compound case now travels on saved case JSON as optional `hideAllHoldLedger`. Older files omit it and default to showing those ledger rows. Unknown sibling fields are still rejected. Display only; counts stay unchanged.
- Press `k` to jump to the Compound stress heading, which includes Inspect cases, when no field is focused.
- Copy tested split copies hold counts and whether a fixed split is available as Markdown. Clipboard falls back to a textarea. Counts are counts. It is not a probability.
- Hide unbounded shocks filters unbounded or impossible shocks from the tornado chart and table. Restore shows all. Display only. Model math is unchanged.
- Print one-pager includes the least-headroom participant line when the case is valid. The saved case is unchanged.

## 1.5.2

- Keeps the 1.5.0 constraint review and 1.5.1 review UI: `createPartnershipReviewPacket`, `replayPartnershipReviewPacket`, `PARTNERSHIP_REVIEW_TOOLS`, `analyzePartnershipReview`, exact recompute, stale-read cancel, and no replacement of the working case.
- Press `f` to jump to the First breakpoint heading when no field is focused. Shortcuts stay ignored while a field is focused.
- Copy contribution waterfall copies each participant, contribution per transaction, and revenue share as Markdown. Clipboard falls back to a textarea. It is a comparison aid, not a forecast.
- Press `w` to jump to the Contribution waterfall heading when no field is focused.
- Copy the viability card copies the least-headroom participant, headroom, and binding limit as Markdown. Clipboard falls back to a textarea. Counts are counts. It is not a probability.
- Hide participants who currently hold now travels on saved case JSON as optional `hideHoldingParticipants`. Older files omit it and default to showing holders. Unknown sibling fields are still rejected. Display only; counts stay unchanged.
- Added the Talent, agent, and platform starting point, a three-party talent plus booking agent plus platform with different cost bases, distinct from Balanced, Two-party 50/50 studio, Four-party marketplace, and Licensor and distributor. It is a synthetic starting point, not a live roster.
- Copy capacity utilization copies volume over capacity, or Unbounded, as Markdown. Clipboard falls back to a textarea. Display only. It is not a probability.
- Hide participants who hold in every tested compound case filters the participant ledger display only. Expand restores them. Grid counts stay unchanged. Model math is unchanged.
- Press `l` to jump to the Participant ledger heading when no field is focused.
- Press `b` to jump to the viability and binding-limit card heading when no field is focused.
- Results jump nav includes Viability and Waterfall in-page links with visible focus.

## 1.5.1

- Keeps the 1.5.0 constraint review: `createPartnershipReviewPacket`, `replayPartnershipReviewPacket`, `PARTNERSHIP_REVIEW_TOOLS`, exact recompute, stale-read cancel, and no replacement of the working case.
- Download contribution waterfall SVG writes every participant chart as one namespaced SVG file with the same XML declaration as tornado. Invalid cases are refused.
- Press `c` to jump to the snapshot or imported JSON compare heading. Shortcuts stay ignored while a field is focused.
- Copy first breakpoint copies the displayed participant, shock axis, and magnitude as Markdown. Clipboard falls back to a textarea. It is a comparison aid, not a forecast.
- Hide participants who currently hold filters the roster display only. Expand restores them. Tested-case and model counts stay unchanged.
- Press `p` to print the one-pager when the case is valid, the same as the print control. Ignored in inputs.
- Swap with next exchanges two adjacent participants. Identifiers and shares stay with each person. The last row is disabled. Undo restores the previous order.
- Added the Licensor and distributor starting point, a two-party IP licensor plus territory distributor with different cost bases, distinct from Balanced, Creator take-rate, Three-party JV, Two-party 50/50 studio, and Four-party marketplace.
- Copy share-to-hold preview copies an open preview as Markdown. Clipboard falls back to a textarea. It is a solvability result, not a probability.
- Collapse cases every participant holds now travels on saved case JSON as optional `collapseAllHoldCases`. Older files omit it and default to expanded. Unknown sibling fields are still rejected.
- Copy deal notes copies entered notes, or an honest empty line, as Markdown. Clipboard falls back to a textarea. Notes are user-entered text, not a forecast.

## 1.5.0

- Optional constraint review covers effective-volume intervals, independent slack, fixed and variable cost allowances, funding shares, fee and volume scenarios, operational conflicts, and zero-volume obligations.
- Export and inspect bounded input-bound review packets with recomputation, exact result checks, stale-read cancellation, and no replacement of the working case.
- Retains all 1.4.3 roster, comparison, export, print, keyboard and preset features.


## 1.4.3

- Export participant CSV uses the same columns as import, with formula-safe cells. Empty optional capacity and commitment stay blank so a later import restores null. Identifiers are omitted because import regenerates them.
- Press `n` to focus Add participant, or add one if that control is missing. Shortcuts stay ignored while a field is focused.
- Download tornado SVG writes the displayed chart as a standalone SVG file with an XML declaration and SVG namespace.
- Compare current case with imported JSON aligns participants by identifier. Missing identifiers are labeled rather than filled with zeros. The compare does not replace the draft.
- A shared display name on two participants shows a warning. It does not block edits and is not a legitimacy claim.
- Paste a TSV or CSV roster into the textarea. Tabs on the first line parse as TSV, then the same CSV validation applies. Deal terms are unchanged.
- Press `s` to jump to share-to-hold (the preview if it is open, otherwise the first solver).
- Print redacted uses Participant 1 through N on the print path and in the print stylesheet. The saved case is unchanged.
- Copy visible cases CSV copies the currently displayed stress-grid rows, including after collapsing all-hold cases. It does not replace the download control. Clipboard falls back to a textarea.
- Added the Four-party marketplace starting point, distinct from Balanced, Creator take-rate, Three-party JV, and Two-party 50/50 studio.

## 1.4.2

- Added a volume-to-hold solver: binary search for the minimum monthly volume at which a named participant holds, with fee and shares fixed. Preview, then Apply. Impossible names the failing tests. This is a solvability result, not a forecast.
- Copy negotiation brief uses the Clipboard API when it exists; otherwise a visible textarea remains for copying. There is no download fallback.
- Stress-grid CSV uses the same formula-safe escaping as other CSVs. Export every tested case, or export the currently visible cases after collapsing all-hold rows. Participant rows stay complete.
- Press `g` to jump to the results nav, or the first results heading, when no field is focused.
- Print one-pager hides coach, help, and chrome, and keeps tornado, waterfall, ledger, and notes.
- Duplicate current case saves an independent snapshot with a unique ` copy` title suffix. Later draft edits do not change the snapshot.
- The roster highlights the first listed participant who fails the current baseline. The label is roster order, not a claim about who will act.
- The invalid-field summary stays sticky until the count is 0 and still announces the count.
- Added the Two-party 50/50 studio starting point, distinct from Creator take-rate and Three-party JV.
- Collapse cases every participant holds hides those evidence rows from the inspect table only. Expand restores them. Tested-case counts are unchanged.

## 1.4.1

- Kept the results jump nav sticky at the top of the results column so in-page links stay visible while scrolling. Links remain keyboard accessible and are hidden in print.
- Added participant CSV import for name, revenue share, variable cost, fixed cost, min profit, optional capacity, commitment, and risk. Formula prefixes are stored as text. Named row and column errors reject the file. A valid file replaces participants only; deal terms are unchanged.
- Showed capacity utilization in the participant ledger as volume over capacity, or Unbounded when capacity is omitted, with an SVG meter and text.
- Added a three-snapshot compare: pin two saved snapshots against the current draft. The table reports profit and hold or fail per participant. Different identifier sets are labeled rather than filled with zeros.
- Stored optional deal notes (1 to 500 characters) on the case. Notes appear in printed and Markdown reports. Unknown sibling fields are rejected. Redacted export clears notes with the title.
- Trapped Tab inside the coach and help dialogs and restored focus to the opener on close.
- Included a sanitized deal title in export filenames (`partnership-breakpoint-harbor-jv.json`). Empty or unusable titles keep the previous names.
- Kept Duplicate, Move, and Remove usable at 390px outside the participant disclosure. Cards still default to open.
- Added Copy share URL in locally served http mode. File URLs hide the control. Hide in table on the stress ledger is display-only and does not change case counts or proposals.

## 1.4.0

- Added optional deal title (1 to 80 characters) and optional 3-letter uppercase currency display prefix. Omitted currency still shows units. Illegal strings are rejected rather than coerced.
- Added Duplicate, Move up, and Move down on the participant roster. Duplicates receive a unique id, a name suffix, and a zero share so the allocation total does not change.
- Removing a participant (while more than two remain) reallocates that share across whoever remains. Remaining shares sum to exactly 1. The last two participants cannot be removed.
- Added a share-to-hold solver: binary search for the minimum revenue share at which a named participant holds, with remaining participants keeping their relative leftover. Preview, then Apply. Impossible at 100% is reported without assigning probability.
- Added a fee-to-hold solver for the minimum fee at which every participant holds with volume and shares fixed. Preview, then Apply. Capacity and commitment failures cannot be repaired by a fee change.
- Added print-friendly SVG tornado and contribution waterfall charts, each with a text-equivalent table.
- Added a dismissible first-run coach (skipped when a share link loaded the case) and keyboard shortcuts: `?` help, `u` undo, `r` redo, `e` export JSON. Shortcuts never steal keys from inputs.
- Added honest redacted JSON export (names replaced with Participant 1..N, deal title cleared) and a copyable Markdown negotiation brief.
- Added Creator take-rate and Three-party JV starting points. Snapshot comparison now highlights profit diffs. Participant cards collapse on small screens, and the invalid-field control announces how many fields need attention.

## 1.3.0

- Added named local snapshots, participant comparisons, bounded undo and redo, and snapshot removal recovery.
- Added reproducible Markdown reports, spreadsheet-safe compound CSV, and printable assumptions.
- Added fee-floor diagnostics, explicit share allocation tools, and previewed compound-case application.
- Made saving failures visible, prevented stale imports from overwriting edits, and improved field errors and keyboard access.

## 1.2.0 (27-08-2026)

- Added simultaneous volume, fee, and variable-cost stress testing with up to 27 deterministic cases.
- Added per-participant hold counts, worst profit gaps, operating failures, and case evidence in the GUI.
- Added a fixed-share negotiation envelope and an explicit apply action. Proposals must fund all participants and pass every selected case before becoming available.
- Preserved v1 case imports and baseline calculations. Optional validated stress settings now travel with exported cases.
- Added model and standalone interaction regression tests for compound failures, funding limits, operational constraints, invalid inputs, escaped names, and proposal application.
- Rebuilt the no-install standalone GUI without new dependencies or network services.
