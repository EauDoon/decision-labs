# Partnership Breakpoint

Partnership Breakpoint is a static browser workbench for locating participant-level exit thresholds in a shared commercial arrangement, then ranking the smallest adverse percentage movements in volume, fee, or variable cost that reach those boundaries. A reachable capacity limit is ranked as a volume-increase shock.

It is useful when a negotiated revenue split looks acceptable in aggregate but may fail for one participant after costs, capacity, commitments, or a modest commercial shock are included. The workbench does not assign probabilities. The viability card names the participant with the least volume headroom. First breakpoint is a separate ranking by relative shock size and can name a different participant.

Version 1.5.31 keeps the 1.5.0 constraint review packets and the 1.5.1/1.5.2/1.5.3/1.5.4/1.5.5/1.5.6/1.5.7/1.5.8/1.5.9/1.5.10/1.5.11/1.5.12/1.5.13/1.5.14/1.5.15/1.5.16/1.5.17/1.5.18/1.5.19/1.5.20/1.5.21/1.5.22/1.5.23/1.5.24/1.5.25/1.5.26/1.5.27/1.5.28/1.5.29/1.5.30 review UI, and adds keyboard copy of first over-capacity volume-to-hold (`Shift+F10`), keyboard jumps (`Shift+F11` and `Shift+F12`), a Cycling carnival split starting point, copy of first over-capacity volume-to-hold as one-line Markdown, and a print one-pager first over-capacity volume-to-hold line. Cycling carnival, first-over-capacity volume copy, and last-over-capacity hide jump in Partnership Breakpoint 1.5.31. Counts remain counts. Ranking is not a forecast of who will exit. The workbench does not assign probabilities.

## Open without installation

Double-click [standalone.html](standalone.html) to run the complete workbench directly from a local file. It includes the interface, styles, and model code in one file and makes no network requests.

Use Export JSON to transfer a case. File URLs and their fragments are local to the file location and are not portable share links. Import JSON remains available in the standalone workbench.

To regenerate the standalone file from source, with Node.js 20 or later installed, run:

```sh
npm run build:standalone
```

`npm run build:standalone -- --check` verifies that `standalone.html` matches the current sources without rewriting it.

## Launch the GUI with Node.js

Node.js 20 or later is sufficient. No packages need to be installed.

On Windows, double-click [launch-windows.cmd](launch-windows.cmd). It checks for Node.js 20 or later, starts the loopback-only server, then opens the GUI after the server is listening.

From a terminal opened in the project folder on Windows, macOS, or Linux, run:

```sh
npm run launch
```

The Windows launcher resolves the project directory automatically. The npm command uses the terminal's current project folder. Both print the local URL and, if they cannot open a browser, leave that URL available to open manually. Press Ctrl+C in the launcher terminal to stop the local server cleanly.

To run the server without the launcher:

```sh
npm start
```

Open the local address printed by the server, normally `http://127.0.0.1:4173`. The listener is loopback IPv4 only; `HOST` is ignored. Override the port with `PORT`, a base-10 integer from 1 through 65535. Invalid values, including `0`, hex, and non-integers, are rejected rather than treated as 4173.

`npm run launch` opens a browser after the server is listening. Set `PARTNERSHIP_BREAKPOINT_NO_OPEN=1` to print the URL without opening a browser.

```sh
npm test
npm run check
```

`npm test` runs every Node test file under `tests/`: the economic model, compound-stress negotiation, standalone renderer, workbench interaction, and local-server checks. `npm run check` scans project files for em dashes, private filesystem paths, and common private-key markers, syntax-checks JavaScript sources, and verifies that `standalone.html` matches the current sources. GitHub Actions runs both commands on pull requests and on pushes to `main`.

## Use the workbench

For repeatable terminal workflows, use the [offline scenario analysis CLI](CLI.md), which reads saved JSON and reuses the same model without a browser or dependencies.

1. Set Monthly volume, Fee / transaction, Addressable volume, and Volume shock % on the Shared deal form. Optionally name the deal, enter notes (up to 500 characters), and enter a 3-letter uppercase currency code used only as a display prefix. Volume shock % is the only baseline volume reduction; there is no separate churn field.
2. Add, duplicate, reorder, or edit participants. Swap with next exchanges two adjacent participants without changing ids. Hide participants who currently hold filters this roster display only. Hide participants with zero revenue share filters zero-share roster cards only. Hide the first participant with zero revenue share filters that card only. Hide the last participant with zero revenue share filters that card only. Hide participants whose volume is above listed capacity filters those roster cards only. Hide participants at hold with no listed capacity breach filters those roster cards only. Hide the last participant at hold filters that card only. Hide the first participant at hold filters that card only. Hide participants whose capacity is unbounded or omitted filters those roster cards only. Hide participants with unused listed capacity filters those roster cards only. Hide the least-headroom roster row filters that card only. Hide participants who are within listed capacity filters those roster cards only. Hide the first-breakpoint roster row filters that card only. Hide the first over-capacity roster row filters that card only. Hide the last over-capacity roster row filters that card only. Hide the last first-breakpoint roster row filters that card only. Hide the last within-capacity roster row filters that card only. Hide the first within-capacity roster row filters that card only. Hide the last spare-capacity roster row filters that card only. Hide the first spare-capacity roster row filters that card only. Hide the last participant without listed capacity filters that card only. Hide the first participant without listed capacity filters that card only. Those preferences travel on saved JSON. Import participant CSV replaces the roster only after the whole file validates; deal terms stay unchanged. Paste a TSV or CSV roster into the textarea to reuse that same validation. A shared display name warns without blocking. Revenue shares must total exactly 1. Between 2 and 24 participants are required. Removing a participant reallocates that share across whoever remains. Duplicate, Move, and Remove stay outside the card disclosure so they remain usable at 390px.
3. Enter each participant's Variable cost / txn, Fixed monthly cost, Minimum monthly profit, optional Capacity / month, optional Minimum commitment, and Risk cost / month.
4. Start with the First breakpoint card. It ranks bounded volume decrease, volume increase (capacity), fee decrease, and variable-cost increase shocks by the smallest percentage movement from the current scenario. That ranking can name a different participant than the viability card's least-headroom participant. The results jump nav stays sticky while you scroll.
5. Read the Participant ledger (including Binding limit and capacity utilization), contribution waterfall, Smallest adverse shock by participant, tornado chart, and Operating region (fee and volume sensitivity). Utilization is volume over capacity, or Unbounded when capacity is omitted. Hide participants who hold in every tested compound case filters the ledger display only; expand restores them. That preference travels on saved JSON. Copy capacity utilization copies that volume-over-capacity display. Copy tornado copies participant, shock axis, and bounded percentage. Hide unbounded shocks filters the tornado display only. Copy operating region copies the displayed fee and volume grid.
6. Edit Compound stress settings. The workbench tests up to 27 combinations of volume decline or growth, fee cuts, and variable-cost increases. Open Inspect all N compound cases to review every outcome. Hide in table removes a ledger row from display only. Collapse cases every participant holds hides those evidence rows; counts stay the same.
7. Compare each participant's Worst profit gap and Minimum share. If a fixed split can fund every participant in every tested case without capacity or commitment failures, preview the proposal and select Apply tested revenue split. The workbench rechecks it before changing the shares.
8. Use Solve minimum share to hold, Solve volume to hold on one participant, or Solve fee for all to hold, to preview a deterministic floor. Apply is required. Pin two snapshots against the current draft for a three-way profit and hold table. Compare current case with imported JSON keeps the draft and labels missing identifiers instead of filling zeros. Duplicate current case saves an independent snapshot. Export filenames include a sanitized deal title when one is present. Export redacted JSON when sharing a case without names. Export participant CSV uses the import columns. Export stress CSV for all cases, or visible cases after a collapse. Copy visible cases CSV copies that same visible subset. Copy negotiation brief, first breakpoint, first-breakpoint snapshot, first-breakpoint participant label, first-breakpoint remaining-to-hold, first-breakpoint volume-to-hold, contribution waterfall, viability card, least-headroom participant label, capacity utilization, tornado, operating region, tested split, allocation balance, deal notes, deal title and currency, over-capacity participant count, first over-capacity participant label, first over-capacity remaining listed capacity, last over-capacity participant label, last over-capacity remaining listed capacity, last over-capacity volume-to-hold, first over-capacity volume-to-hold, first within-capacity remaining listed capacity, last within-capacity remaining listed capacity, last spare-capacity remaining listed capacity, first spare-capacity remaining listed capacity, last unbounded remaining-to-hold, first unbounded remaining-to-hold, last at-hold remaining-to-hold, first at-hold remaining-to-hold, last zero-share participant label, last zero-share remaining-to-hold, last zero-share volume-to-hold, first zero-share remaining-to-hold, first zero-share volume-to-hold, first zero-share participant label, last first-breakpoint participant label, and an open share-to-hold preview use the clipboard when available. Download tornado SVG and waterfall SVG write the displayed charts. Copy share URL is available when the workbench is served over http. Print report is a one-pager of tornado, waterfall, ledger, notes, the least-headroom participant line, the least-headroom participant label line, the allocation-balance line, the deal title line, the currency code line, the first-breakpoint participant label line, the first-breakpoint remaining-to-hold line, the first-breakpoint volume-to-hold line, the over-capacity participant count line, the first over-capacity participant label line, the first over-capacity remaining listed capacity line, the first over-capacity volume-to-hold line, the last over-capacity participant label line, and the last over-capacity remaining listed capacity line, the last over-capacity volume-to-hold line, the first over-capacity volume-to-hold line, the first within-capacity remaining listed capacity line, and the last within-capacity remaining listed capacity line, and the last spare-capacity remaining listed capacity line, and the first spare-capacity remaining listed capacity line, and the last unbounded remaining-to-hold line, and the first unbounded remaining-to-hold line, and the last at-hold remaining-to-hold line, and the first at-hold remaining-to-hold line, and the last zero-share participant label line, and the last zero-share remaining-to-hold line, and the last zero-share volume-to-hold line, and the first zero-share remaining-to-hold line, and the first zero-share volume-to-hold line, and the first zero-share participant label line. Print redacted uses Participant 1 through N without changing the saved case. Cycling carnival split is a distinct synthetic starting point. Triathlon carnival split is a distinct synthetic starting point. Surf carnival split is a distinct synthetic starting point. Dragon boat carnival split is a distinct synthetic starting point. Kayaking carnival split is a distinct synthetic starting point. Canoeing carnival split is a distinct synthetic starting point. Rowing carnival split is a distinct synthetic starting point. Sailing carnival split is a distinct synthetic starting point. Water polo carnival split is a distinct synthetic starting point. Lacrosse carnival split is a distinct synthetic starting point. Softball carnival split is a distinct synthetic starting point. Baseball carnival split is a distinct synthetic starting point. Hockey carnival split is a distinct synthetic starting point. Rugby carnival split is a distinct synthetic starting point. Volleyball carnival split is a distinct synthetic starting point. Basketball carnival split is a distinct synthetic starting point. Tennis carnival split is a distinct synthetic starting point. Cricket carnival split is a distinct synthetic starting point. Athletics carnival split is a distinct synthetic starting point. Swimming carnival split, Netball carnival, Sports carnival split, School concert split, Community radio split, Pop-up cinema split, Festival stall split, and Community hall split remain available. The 1.5.0 constraint review remains under Review constraints and negotiation room.

The app autosaves valid inputs in local browser storage. When served locally, it also writes valid input state to the URL hash so the current case can be copied as a link. In standalone file mode, use Export JSON for a portable case file. Import JSON is available in both modes. Rejected imports, share links, exports, and unsaved edits name the parse or validation cause.

Existing v1 case files still import. Cases without stress settings start with illustrative stress settings, which are saved with the next export. These settings are assumptions, not forecasts. The original case economics and revenue shares stay unchanged unless you edit them or apply a proposal.

To try a feasible proposal, load Balanced, set volume decline to 5%, and set growth, fee reduction, and variable-cost increase to 0%. The two tested cases hold, and a fixed split is available to preview. Default stress settings are deliberately more demanding and expose both funding shortfalls and a capacity failure.

## What the outputs mean

- Effective volume is the post-shock monthly volume, capped by addressable volume.
- Monthly profit is participant revenue less variable cost, fixed cost, and risk cost.
- Break-even volume is the volume where accounting profit equals zero. It can be impossible when contribution per transaction is not positive.
- Exit volume is the larger of the volume needed to meet minimum acceptable profit and any stated minimum commitment.
- Headroom is effective volume less exit volume. The binding limit identifies whether the nearest boundary is minimum acceptable profit, minimum commitment, or capacity.
- Adverse-shock thresholds show the boundary at which an exit test is reached. Volume decrease tests economic exit, volume increase tests capacity within addressable demand, and fee or variable-cost movement tests profit. Any further adverse movement causes failure. They are not probability forecasts.
- First breakpoint ranks bounded shocks by percentage movement from the current scenario. It is a negotiation prioritisation aid, not a forecast of which participant will act.
- Compound case counts describe only discrete tested combinations, not likelihoods. Worst profit gap is the lowest monthly profit less the participant's minimum acceptable profit across those cases.
- Minimum fixed share is the largest share needed to meet a participant's profit floor across the tested cases. All participant minima must fit within 100%, and all capacity and commitment tests must pass, before a proposal is offered. A proposal does not prove that untested conditions or a counterparty will accept it.
- Share-to-hold is the smallest revenue share in [0, 1] at which one named participant holds while others keep their relative leftover. It is a solvability result, not a probability.
- Fee-to-hold is the smallest fee at current volume and shares at which every participant holds. It cannot repair capacity or commitment failures.
- Volume-to-hold is the smallest monthly volume at which one named participant holds with fee and shares fixed. Capacity at a larger volume cannot hide a lower holding volume. Impossible names the failing tests.

## Design boundaries

This project is a transparent deterministic monthly model. It does not prove a contract is enforceable, that counterparties will actually exit at the stated threshold, that inputs are complete, or that demand, costs, and behavior stay unchanged. It does not model taxes, cash-flow timing, credit losses, correlations, allocations outside the stated deal, or strategic value beyond the entered minimum acceptable profit.

Read [MODEL.md](MODEL.md) for formulas and conventions, [CONTRIBUTING.md](CONTRIBUTING.md) for change standards, and [SECURITY.md](SECURITY.md) for vulnerability reporting.

## Decision workflow (v1.5.31)

1. Enter shared deal terms and participant costs. Optional title, notes, and currency travel with JSON, hash links, and autosave. Copy deal notes copies the stored text, or an honest empty line. Keyboard `y` copies deal notes as one-line Markdown, or an honest empty line when notes are omitted. Copy deal title and currency copies a one-line Markdown summary, or an honest empty line when both are omitted. Keyboard `/` without Shift jumps to that copy control, or the Shared deal heading if missing. Copy allocation balance names missing or excess share, or an honest empty line. Equal split and Normalize current shares are explicit editing aids, not negotiated allocations. Duplicate, Move up, Move down, and Swap with next keep identifiers with each participant. A duplicate starts at zero share. Hide participants who currently hold is display-only and travels on saved JSON as optional `hideHoldingParticipants`. Hide participants with zero revenue share is display-only and travels as optional `hideZeroShareParticipants`. Hide participants whose volume is above listed capacity is display-only and travels as optional `hideParticipantsOverCapacity`. Hide participants at hold with no listed capacity breach is display-only and travels as optional `hideParticipantsAtHold`. Hide participants whose capacity is unbounded or omitted is display-only and travels as optional `hideParticipantsWithoutCapacity`. Hide participants with unused listed capacity is display-only and travels as optional `hideParticipantsWithSpareCapacity`. Hide the least-headroom roster row is display-only and travels as optional `hideParticipantsAtLeastHeadroom`. Hide participants who are within listed capacity is display-only and travels as optional `hideParticipantsWithinCapacity`. Hide the first-breakpoint roster row is display-only and travels as optional `hideFirstBreakpointParticipant`. Hide the first over-capacity roster row is display-only and travels as optional `hideFirstOverCapacityParticipant`. Hide the last over-capacity roster row is display-only and travels as optional `hideLastOverCapacityParticipant`. Hide the last first-breakpoint roster row is display-only and travels as optional `hideLastBreakpointParticipant`. Hide the last within-capacity roster row is display-only and travels as optional `hideLastWithinCapacityParticipant`. Hide the first within-capacity roster row is display-only and travels as optional `hideFirstWithinCapacityParticipant`. Hide the last spare-capacity roster row is display-only and travels as optional `hideLastSpareCapacityParticipant`. Hide the first spare-capacity roster row is display-only and travels as optional `hideFirstSpareCapacityParticipant`. Hide the last participant without listed capacity is display-only and travels as optional `hideLastParticipantWithoutCapacity`. Hide the first participant without listed capacity is display-only and travels as optional `hideFirstParticipantWithoutCapacity`. Hide the last participant at hold is display-only and travels as optional `hideLastParticipantAtHold`. Hide the first participant at hold is display-only and travels as optional `hideFirstParticipantAtHold`. Hide the first participant with zero revenue share is display-only and travels as optional `hideFirstZeroShareParticipant`. Hide the last participant with zero revenue share is display-only and travels as optional `hideLastZeroShareParticipant`. Import participant CSV replaces the roster only after validation. Paste a TSV or CSV roster into the textarea to reuse that validation. A shared display name warns without blocking. The roster marks the first listed participant who fails the current baseline. Two-party 50/50 studio, Four-party marketplace, Licensor and distributor, Talent, agent, and platform, Three-party joint venture, Podcast host and network, Community hall split, Festival stall split, Pop-up cinema split, Community radio split, School concert split, Sports carnival split, Netball carnival, Swimming carnival split, Athletics carnival split, Cricket carnival split, Tennis carnival split, Basketball carnival split, Volleyball carnival split, Rugby carnival split, Hockey carnival split, Baseball carnival split, Softball carnival split, Lacrosse carnival split, Water polo carnival split, Rowing carnival split, and Sailing carnival split, and Canoeing carnival split, and Kayaking carnival split, and Dragon boat carnival split, and Surf carnival split, and Triathlon carnival split, and Cycling carnival split are distinct starting points. Cycling carnival split is a synthetic carnival committee, cycling club hire, and first-aid roster, not a live carnival list. Triathlon carnival split is a synthetic carnival committee, triathlon club hire, and first-aid roster, not a live carnival list. Surf carnival split is a synthetic carnival committee, surf club hire, and first-aid roster, not a live carnival list. Dragon boat carnival split is a synthetic carnival committee, dragon boat club hire, and first-aid roster, not a live carnival list. Kayaking carnival split is a synthetic carnival committee, whitewater slalom hire, and first-aid roster, not a live carnival list. Canoeing carnival split is a synthetic carnival committee, paddle club hire, and first-aid roster, not a live carnival list. Rowing carnival split is a synthetic carnival committee, boat hire, and first-aid roster, not a live carnival list. Sailing carnival split is a synthetic carnival committee, yacht club hire, and first-aid roster, not a live carnival list. Water polo carnival split is a synthetic carnival committee, pool hire, and first-aid roster, not a live carnival list. Lacrosse carnival split is a synthetic carnival committee, ground hire, and first-aid roster, not a live carnival list. Softball carnival split is a synthetic carnival committee, diamond hire, and first-aid roster, not a live carnival list. Baseball carnival split is a synthetic carnival committee, diamond hire, and first-aid roster, not a live carnival list. Hockey carnival split is a synthetic carnival committee, ice hire, and first-aid roster, not a live carnival list. Rugby carnival split is a synthetic carnival committee, ground hire, and first-aid roster, not a live carnival list. Volleyball carnival split is a synthetic carnival committee, court hire, and first-aid roster, not a live carnival list. Basketball carnival split is a synthetic carnival committee, stadium hire, and first-aid roster, not a live carnival list. Tennis carnival split is a synthetic carnival committee, court hire, and first-aid roster, not a live carnival list. Cricket carnival split is a synthetic carnival committee, pitch hire, and first-aid roster, not a live carnival list. Athletics carnival split is a synthetic carnival committee, track hire, and first-aid roster, not a live carnival list. Swimming carnival split is a synthetic carnival committee, pool operations, and canteen roster, not a live carnival list. Netball carnival is a synthetic carnival committee, canteen, and first-aid roster, not a live carnival list. Sports carnival split is a synthetic carnival committee, ride operator, and ticket booth roster, not a live carnival list.
2. Name and save a snapshot before exploring terms, or Duplicate current case to store an independent copy with a unique title suffix. Up to 12 snapshots stay in this browser. Compare a snapshot with the current draft to see participant profit changes, with decreases and increases highlighted, and added or removed participants matched by identifier. Pin two snapshots against the current draft for a three-way profit and hold table. Compare current case with imported JSON keeps the draft. Different identifier sets are labeled honestly. Each snapshot uses its own stress settings.
3. Review the fee negotiation guide. It computes the fee required for each profit floor at current effective volume and shares, and separately flags capacity or commitment failures that fee changes cannot repair. Solve fee for all to hold previews that floor; Apply is required. Solve volume to hold previews a monthly volume floor for one named participant with fee and shares fixed. Rounded floors require rechecking.
4. Inspect a compound case, preview the changed assumptions, and apply it only when you want a new baseline. Existing volume shock resets to zero to avoid double counting. The stress grid then applies the existing settings as additional shocks from this baseline. Hide in table is display-only. Collapse cases every participant holds hides those evidence rows without changing counts. That collapse preference travels on saved JSON; older files omit it and stay expanded. Hide participants who hold in every tested compound case filters the participant ledger display only; expand restores them. That ledger filter travels on saved JSON as optional `hideAllHoldLedger`. Counts stay the same. Copy tested split copies hold counts and whether a fixed split is available.
5. Export JSON for portable case backups, redacted JSON with names removed, a Markdown decision report with reproducible inputs, a short negotiation brief, a CSV of every participant in every compound case, a CSV of currently visible cases, or a participant CSV with import columns. Copy visible cases CSV copies the displayed subset. Copy first breakpoint, first-breakpoint snapshot, first-breakpoint participant label, first-breakpoint remaining-to-hold, first-breakpoint volume-to-hold, contribution waterfall, viability card, least-headroom participant label, capacity utilization, tornado, operating region, tested split, allocation balance, deal notes, deal title and currency, over-capacity participant count, first over-capacity participant label, first over-capacity remaining listed capacity, last over-capacity participant label, last over-capacity remaining listed capacity, last over-capacity volume-to-hold, first over-capacity volume-to-hold, first within-capacity remaining listed capacity, last within-capacity remaining listed capacity, last spare-capacity remaining listed capacity, first spare-capacity remaining listed capacity, last unbounded remaining-to-hold, first unbounded remaining-to-hold, last at-hold remaining-to-hold, first at-hold remaining-to-hold, last zero-share participant label, last zero-share remaining-to-hold, last zero-share volume-to-hold, first zero-share remaining-to-hold, first zero-share volume-to-hold, first zero-share participant label, last first-breakpoint participant label, and an open share-to-hold preview use the clipboard when available. Keyboard `,` copies the first-breakpoint participant label through the existing copy control. Keyboard `.` jumps to that copy control, or the First breakpoint heading if missing. Keyboard `;` copies the least-headroom participant label through the existing copy control. Keyboard `[` jumps to that copy control, or the First breakpoint or results heading if missing. Keyboard `]` jumps to Print one-pager, or the print / one-pager heading if missing. Keyboard `'` copies first-breakpoint remaining-to-hold through the existing copy control. Keyboard `<` jumps to that copy control, or the First breakpoint heading if missing. Keyboard `>` jumps to Hide participants with unused listed capacity, or the Participants heading if missing. Keyboard `:` copies first-breakpoint volume-to-hold through the existing copy control. Keyboard `-` jumps to that copy control, or the First breakpoint heading if missing. Keyboard `=` jumps to Hide the least-headroom participant, or the Participants heading if missing. Keyboard `"` copies over-capacity participant count through the existing copy control. Keyboard `_` jumps to that copy control, or the Participants heading if missing. Keyboard `{` jumps to Hide participants who are within listed capacity, or the Participants heading if missing. Keyboard `}` copies the first over-capacity participant label through the existing copy control. Keyboard `+` jumps to that copy control, or the First breakpoint or Participants heading if missing. Keyboard `|` jumps to Hide the first-breakpoint participant, or the Participants heading if missing. Keyboard `~` copies first-over-capacity remaining listed capacity through the existing copy control. Keyboard `!` jumps to that copy control, or the First breakpoint or Participants heading if missing. Keyboard `@` jumps to Hide the first over-capacity participant, or the Participants heading if missing. Keyboard `(` copies the last over-capacity participant label through the existing copy control. Keyboard `)` jumps to that copy control, or the First breakpoint or Participants heading if missing. Keyboard `#` jumps to Hide the last over-capacity participant, or the Participants heading if missing. Keyboard `*` copies last-over-capacity remaining listed capacity through the existing copy control. Keyboard `&` jumps to that copy control, or the First breakpoint or Participants heading if missing. Keyboard `%` jumps to Hide the last first-breakpoint participant, or the Participants heading if missing. Keyboard `$` copies first-within-capacity remaining listed capacity through the existing copy control. Keyboard `^` jumps to that copy control, or the First breakpoint or Participants heading if missing. Keyboard backtick jumps to Hide the first within-capacity participant, or the Participants heading if missing. Keyboard `5` copies last-within-capacity remaining listed capacity through the new copy control. Keyboard `6` jumps to that copy control, or the results heading if missing. Keyboard `7` jumps to Hide the last spare-capacity participant, or the Participants heading if missing. Keyboard `8` copies last-spare-capacity remaining listed capacity through the new copy control. Keyboard `9` jumps to that copy control, or the results heading if missing. Keyboard `0` jumps to Hide the first spare-capacity participant, or the Participants heading if missing. Keyboard `1` copies first-spare-capacity remaining listed capacity through the new copy control. Keyboard `2` jumps to that copy control, or the results heading if missing. Keyboard `3` jumps to Hide the last participant without listed capacity, or the Participants heading if missing. Keyboard `4` copies last unbounded remaining-to-hold through the new copy control. Keyboard `Home` jumps to that copy control, or the Participants heading if missing. Keyboard `PageUp` copies first unbounded remaining-to-hold through the new copy control. Keyboard `PageDown` jumps to that copy control, or the Participants heading if missing. Keyboard `Insert` copies last at-hold remaining-to-hold through the new copy control. Keyboard `ArrowDown` jumps to that copy control, or the Participants heading if missing. Keyboard `End` jumps to Hide the first participant without listed capacity, or the Participants heading if missing. Keyboard `ArrowUp` jumps to Hide the last participant at hold, or the Participants heading if missing. Keyboard `ArrowLeft` jumps to Hide the first participant at hold, or the Participants heading if missing. Keyboard `Delete` copies first at-hold remaining-to-hold through the new copy control. Keyboard `F2` jumps to that copy control, or the Participants heading if missing. Keyboard `ArrowRight` jumps to Hide the first participant with zero revenue share, or the Participants heading if missing. Keyboard `F3` copies last zero-share participant label through the new copy control. Keyboard `F4` jumps to that copy control, or the Participants heading if missing. Keyboard `Backspace` jumps to Hide the last participant with zero revenue share, or the Participants heading if missing. Keyboard `F7` copies first zero-share participant label through the new copy control. Keyboard `F8` jumps to that copy control, or the Participants heading if missing. Keyboard `F9` jumps to Hide the first participant with zero revenue share, or the Participants heading if missing. Keyboard `ArrowRight` keeps that same first-zero-share hide jump. Keyboard `F10` copies last zero-share remaining-to-hold through the new copy control. Keyboard `F11` jumps to that copy control, or the Participants heading if missing. Keyboard `F12` jumps to Hide the last participant with zero revenue share, or the Participants heading if missing. Keyboard `Shift+F10` copies first over-capacity volume-to-hold through the new copy control. Keyboard `Shift+F11` jumps to that copy control, or the Participants heading if missing. Keyboard `Shift+F12` jumps to Hide the last over-capacity participant, or the Participants heading if missing. Last over-capacity volume-to-hold copy stays available without `Shift+F10`. First zero-share volume-to-hold copy stays available without `Shift+F10`. Last zero-share remaining stays on unshifted `F10`. Hide the last participant with zero revenue share stays. Hide participants with zero revenue share stays available without `Shift+F12`. Keyboard `ArrowRight` stays first-zero-share hide. Download tornado SVG and waterfall SVG write the displayed charts. Download names include a sanitized deal title when one is present. Formula-like text is prefixed with an apostrophe in CSV. Print report is a one-pager of tornado, waterfall, ledger, notes, the least-headroom participant line, the least-headroom participant label line, the allocation-balance line, the deal title line, the currency code line, the first-breakpoint participant label line, the first-breakpoint remaining-to-hold line, the first-breakpoint volume-to-hold line, the over-capacity participant count line, the first over-capacity participant label line, the first over-capacity remaining listed capacity line, the last over-capacity participant label line, and the last over-capacity remaining listed capacity line, and the last over-capacity volume-to-hold line, and the first over-capacity volume-to-hold line, and the first within-capacity remaining listed capacity line, and the last within-capacity remaining listed capacity line, and the last spare-capacity remaining listed capacity line, and the first spare-capacity remaining listed capacity line, and the last unbounded remaining-to-hold line, and the first unbounded remaining-to-hold line, and the last at-hold remaining-to-hold line, and the first at-hold remaining-to-hold line, and the last zero-share participant label line, and the last zero-share remaining-to-hold line, and the last zero-share volume-to-hold line, and the first zero-share remaining-to-hold line, and the first zero-share volume-to-hold line, and the first zero-share participant label line. Print redacted uses Participant 1 through N. Copy share URL copies the current hash link in locally served http mode.

The first visit shows a three-step coach. Dismiss it with Got it or Escape; dismissal is stored in this browser. Share links skip the coach. Press `?` for keyboard shortcuts (`u` undo, `r` redo, `e` export JSON, `g` jump to results, `n` add participant, `s` share-to-hold, `c` compare, `p` print, `f` first breakpoint, `w` waterfall, `l` ledger, `b` viability heading, `t` tornado, `d` Shared deal, `k` compound stress, `h` least-headroom card, `a` Add participant, `m` deal notes, `v` viability card, `i` inspect or compare cases, `o` Operating region, `j` copy capacity utilization, `q` Equal split or Normalize, `x` first over-capacity roster row, `y` copy deal notes, `z` Copy deal title and currency, `,` copy first-breakpoint participant label, `.` jump to that copy control, `/` Copy deal title and currency, `;` copy least-headroom participant label, `[` jump to that copy control, `]` Print one-pager, `'` copy first-breakpoint remaining-to-hold, `<` jump to that copy control, `>` Hide unused listed capacity, `:` copy first-breakpoint volume-to-hold, `-` jump to that copy control, `=` Hide the least-headroom participant, `"` copy over-capacity participant count, `_` jump to that copy control, `{` Hide participants within listed capacity, `}` copy first over-capacity participant label, `+` jump to that copy control, `|` Hide the first-breakpoint participant, `~` copy first-over-capacity remaining listed capacity, `!` jump to that copy control, `@` Hide the first over-capacity participant, `(` copy last over-capacity participant label, `)` jump to that copy control, `#` Hide the last over-capacity participant, `*` copy last-over-capacity remaining listed capacity, `&` jump to that copy control, `%` Hide the last first-breakpoint participant, `$` copy first-within-capacity remaining listed capacity, `^` jump to that copy control, backtick Hide the first within-capacity participant, `5` copy last-within-capacity remaining listed capacity, `6` jump to that copy control, `7` Hide the last spare-capacity participant, `8` copy last-spare-capacity remaining listed capacity, `9` jump to that copy control, `0` Hide the first spare-capacity participant, `1` copy first-spare-capacity remaining listed capacity, `2` jump to that copy control, `3` Hide the last participant without listed capacity, `4` copy last unbounded remaining-to-hold, `Home` jump to that copy control, `PageUp` copy first unbounded remaining-to-hold, `PageDown` jump to that copy control, `Insert` copy last at-hold remaining-to-hold, `ArrowDown` jump to that copy control, `End` Hide the first participant without listed capacity, `ArrowUp` Hide the last participant at hold, `ArrowLeft` Hide the first participant at hold, `Delete` copy first at-hold remaining-to-hold, `F2` jump to that copy control, `ArrowRight` Hide the first participant with zero revenue share, `F3` copy last zero-share participant label, `F4` jump to that copy control, `Backspace` Hide the last participant with zero revenue share, `F7` copy first zero-share participant label, `F8` jump to that copy control, `F9` Hide the first participant with zero revenue share, `F10` copy last zero-share remaining-to-hold, `F11` jump to that copy control, `F12` Hide the last participant with zero revenue share, `Shift+F10` copy first over-capacity volume-to-hold, `Shift+F11` jump to that copy control, `Shift+F12` Hide the last over-capacity participant). Shift+/ stays help. Shortcuts are ignored while a field is focused. The invalid-field summary stays sticky until every required field is valid.

Undo and Redo retain up to 50 edits during this tab session. Reset, imports, snapshot loads and applied proposals can be undone. The last removed snapshot can be restored during the session. Browser storage is optional; failures are displayed and JSON export remains available. New edits cancel pending imports. Local snapshots and undo history are not synced or backed up elsewhere. No requests are sent to a server.

## Constraint review (v1.5.0)

Open **Review constraints and negotiation room**, choose a question, and run it on the current valid case. The nine reviews show the feasible effective-volume interval, separate constraint slack, fixed and variable cost allowances, shared revenue funding needs, four common-fee scenarios, commitment/capacity conflicts, five effective-volume scenarios, and zero-volume obligations. Each table explains the unchanged assumptions, units and unavailable values. These are deterministic declared-input calculations, not negotiated terms or forecasts.

Export review packet saves the complete case plus its exact input JSON and recomputed result. Inspect review packet accepts at most 1 MiB, verifies that the inputs and every result cell match, and shows the saved case without replacing current inputs or autosave. Editing the case clears the review and cancels pending inspections. Packets contain participant names, costs and notes. They are unsigned consistency records: a person can rewrite a whole packet, so replay is not proof of authorship or factual inputs. Use existing redacted case export when names should be removed.
