# Common Cart

Common Cart is a local-first pooled purchase simulator. Buyers enter compatible product constraints, merchants enter conditional offers, and the app shows which offers the group can unlock without exposing individual buyer records in the merchant view.

It is a working research prototype for a simple question: can shared demand create bargaining power before a marketplace has accounts, payments, or live merchant integrations?

![Common Cart showing the built-in coffee example, requested units, fulfilled demand, and group headroom.](.github/assets/project-preview.jpg)

*Built-in synthetic coffee scenario.*

## Changelog (1.4.16)

Leftover-fill minimum copy through 5, leftover-fill minimum copy jump, hide first tertiary-fill buyer jump, Basketball carnival lunch, hide first tertiary-fill buyer, leftover print leftover-fill minimum, kept on top of 1.4.0 review packets and 1.4.1/1.4.2/1.4.3/1.4.4/1.4.5/1.4.6/1.4.7/1.4.8/1.4.9/1.4.10/1.4.11/1.4.12/1.4.13/1.4.14/1.4.15 tools. This remains an offline simulator. Residual coverage is leftover planning, not checkout. Merchant views, JSON, CSV, overlap Markdown, leftover print, winning merchant copy, winning fulfillment copy, and remaining-capacity copy still receive aggregates, counts, merchant labels, or fulfillment modes only. Organizer leftover copies are labeled organizer-private. Review packets stay private and unsigned.

- **Keyboard leftover-fill minimum copy.** `5` copies leftover-fill offer minimum units through the leftover-fill minimum copy control. Same organizer-private count-only Markdown as the leftover-fill minimum button. Ignored while typing. Help lists it. Distinct from `$` leftover-fill label and `*` leftover-fill pickup. Not a merchant export.
- **Keyboard leftover-fill minimum copy jump.** `6` focuses the leftover-fill minimum copy control, or the leftover heading if that control is missing. Ignored while typing. Help lists it. Does not copy. Distinct from `^` leftover-fill label copy jump.
- **Keyboard hide first tertiary-fill buyer jump.** `7` focuses hide first tertiary-fill buyer, or the buyer list if that control is missing. Ignored while typing. Help lists it. Distinct from `%` hide leftover-fill buyers and backtick hide last leftover-fill buyer.
- **Basketball carnival lunch.** A synthetic carnival lunch preset with mixed basketball pie, bench salad, and bench water variants, mixed quantities, a hall pickup leftover-fill offer, and a court-side delivery offer. Distinct from neighbourhood coffee, studio chairs, pantry staples, office pantry, hardware tools, garden seed, school fete catering, office fruit box, coffee price ladder, library photocopy paper, sports club match-day kit, surf club first-aid kit, theatre wardrobe kit, community choir folders, scout camp, school excursion lunch, netball canteen lunch, swimming carnival lunch, athletics carnival lunch, cricket carnival lunch, and tennis carnival lunch. Not live inventory.
- **Hide first tertiary-fill buyer.** Display filter hides only the first tertiary-fill selected buyer id. Winner-allocated, leftover-fill, unserved, and other tertiary-fill buyers stay visible. Matching is unchanged. The last hide choice is kept in workspace JSON. Older files omit it and show every buyer. Distinct from `hideExcludedBuyers`, `hideUnwinnableOffers`, `hideCoveredLeftoverRows`, `hideTertiaryLeftoverRow`, `hideLeftoverFillRow`, `hideZeroRemainingCapacityOffers`, `hideOffersWithRemainingCapacity`, `hideFullyFilledBuyers`, `hideBuyersWithLeftover`, `hideUnservedBuyers`, `hideLeftoverOnlyBuyers`, `hideWinnerAllocatedBuyers`, `hideBuyersFilledByLeftoverFill`, `hideLastBuyerFilledByLeftoverFill`, and `hideFirstBuyerFilledByLeftoverFill`.
- **Leftover print leftover-fill minimum.** Leftover print (`body.print-leftover`) includes leftover-fill offer minimum units as a merchant-safe count when leftover fill exists, or an honest empty line. Buyer identities stay out. Reuses leftover-fill minimum wording.
- **1.4.0 review packets kept.** Allocated coverage, sole-offer dependency, unserved reasons, same-cohort alternatives, withdrawal stress, shipping exposure, delivery slack, bounded capacity and minimum previews, and private review packet export/inspect remain.

## Earlier changelog (1.4.15)

Leftover-fill label copy through dollar, leftover-fill label copy jump, hide last leftover-fill buyer jump, Tennis carnival lunch, hide first leftover-fill buyer, leftover print leftover-fill label, kept on top of 1.4.0 review packets and 1.4.1/1.4.2/1.4.3/1.4.4/1.4.5/1.4.6/1.4.7/1.4.8/1.4.9/1.4.10/1.4.11/1.4.12/1.4.13/1.4.14 tools. This remains an offline simulator. Residual coverage is leftover planning, not checkout. Merchant views, JSON, CSV, overlap Markdown, leftover print, winning merchant copy, winning fulfillment copy, and remaining-capacity copy still receive aggregates, counts, merchant labels, or fulfillment modes only. Organizer leftover copies are labeled organizer-private. Review packets stay private and unsigned.

- **Keyboard leftover-fill label copy.** `$` copies leftover-fill offer label through the existing leftover-fill label copy control. Same organizer-private merchant-and-variant Markdown as the leftover-fill label button. Ignored while typing. Help lists it. Distinct from `*` leftover-fill pickup. Not a merchant export.
- **Keyboard leftover-fill label copy jump.** `^` focuses the leftover-fill label copy control, or the leftover heading if that control is missing. Ignored while typing. Help lists it. Does not copy. Distinct from `&` leftover-fill pickup copy jump.
- **Keyboard hide last leftover-fill buyer jump.** Backtick focuses hide last leftover-fill buyer, or the buyer list if that control is missing. Ignored while typing. Help lists it. Distinct from `%` hide leftover-fill buyers.
- **Tennis carnival lunch.** A synthetic carnival lunch preset with mixed tennis pie, court salad, and court water variants, mixed quantities, a hall pickup leftover-fill offer, and a court-side delivery offer. Distinct from neighbourhood coffee, studio chairs, pantry staples, office pantry, hardware tools, garden seed, school fete catering, office fruit box, coffee price ladder, library photocopy paper, sports club match-day kit, surf club first-aid kit, theatre wardrobe kit, community choir folders, scout camp, school excursion lunch, netball canteen lunch, swimming carnival lunch, athletics carnival lunch, and cricket carnival lunch. Not live inventory.
- **Hide first leftover-fill buyer.** Display filter hides only the first leftover-fill selected buyer id. Winner-allocated, unserved, and other leftover-fill buyers stay visible. Matching is unchanged. The last hide choice is kept in workspace JSON. Older files omit it and show every buyer. Distinct from `hideExcludedBuyers`, `hideUnwinnableOffers`, `hideCoveredLeftoverRows`, `hideTertiaryLeftoverRow`, `hideLeftoverFillRow`, `hideZeroRemainingCapacityOffers`, `hideOffersWithRemainingCapacity`, `hideFullyFilledBuyers`, `hideBuyersWithLeftover`, `hideUnservedBuyers`, `hideLeftoverOnlyBuyers`, `hideWinnerAllocatedBuyers`, `hideBuyersFilledByLeftoverFill`, and `hideLastBuyerFilledByLeftoverFill`.
- **Leftover print leftover-fill label.** Leftover print (`body.print-leftover`) includes leftover-fill merchant and variant as a merchant-safe offer label when leftover fill exists, or an honest empty line. Buyer identities stay out. Reuses leftover-fill label wording.
- **1.4.0 review packets kept.** Allocated coverage, sole-offer dependency, unserved reasons, same-cohort alternatives, withdrawal stress, shipping exposure, delivery slack, bounded capacity and minimum previews, and private review packet export/inspect remain.

## Earlier changelog (1.4.14)

Leftover-fill pickup copy through asterisk, leftover-fill pickup copy jump, hide leftover-fill buyers jump, Cricket carnival lunch, hide last leftover-fill buyer, leftover print leftover-fill remaining capacity, and leftover-fill label copy, kept on top of 1.4.0 review packets and 1.4.1/1.4.2/1.4.3/1.4.4/1.4.5/1.4.6/1.4.7/1.4.8/1.4.9/1.4.10/1.4.11/1.4.12/1.4.13 tools. This remains an offline simulator. Residual coverage is leftover planning, not checkout. Merchant views, JSON, CSV, overlap Markdown, leftover print, winning merchant copy, winning fulfillment copy, and remaining-capacity copy still receive aggregates, counts, merchant labels, or fulfillment modes only. Organizer leftover copies are labeled organizer-private. Review packets stay private and unsigned.

- **Keyboard leftover-fill pickup copy.** `*` copies leftover-fill pickup days through the existing leftover-fill pickup copy control. Same organizer-private count-only Markdown as the leftover-fill pickup button. Ignored while typing. Help lists it. Distinct from `(` leftover-fill delivery. Not a merchant export.
- **Keyboard leftover-fill pickup copy jump.** `&` focuses the leftover-fill pickup copy control, or the leftover heading if that control is missing. Ignored while typing. Help lists it. Does not copy. Distinct from `)` leftover-fill delivery copy jump.
- **Keyboard hide leftover-fill buyers jump.** `%` focuses hide leftover-fill buyers, or the buyer list if that control is missing. Ignored while typing. Help lists it. Distinct from `#` hide winner-allocated buyers.
- **Cricket carnival lunch.** A synthetic carnival lunch preset with mixed cricket pie, pitch salad, and pitch water variants, mixed quantities, a hall pickup leftover-fill offer, and a pitch-side delivery offer. Distinct from neighbourhood coffee, studio chairs, pantry staples, office pantry, hardware tools, garden seed, school fete catering, office fruit box, coffee price ladder, library photocopy paper, sports club match-day kit, surf club first-aid kit, theatre wardrobe kit, community choir folders, scout camp, school excursion lunch, netball canteen lunch, swimming carnival lunch, and athletics carnival lunch. Not live inventory.
- **Hide last leftover-fill buyer.** Display filter hides only the last leftover-fill selected buyer id. Winner-allocated, unserved, and other leftover-fill buyers stay visible. Matching is unchanged. The last hide choice is kept in workspace JSON. Older files omit it and show every buyer. Distinct from `hideExcludedBuyers`, `hideUnwinnableOffers`, `hideCoveredLeftoverRows`, `hideTertiaryLeftoverRow`, `hideLeftoverFillRow`, `hideZeroRemainingCapacityOffers`, `hideOffersWithRemainingCapacity`, `hideFullyFilledBuyers`, `hideBuyersWithLeftover`, `hideUnservedBuyers`, `hideLeftoverOnlyBuyers`, `hideWinnerAllocatedBuyers`, and `hideBuyersFilledByLeftoverFill`.
- **Leftover print leftover-fill remaining capacity.** Leftover print (`body.print-leftover`) includes leftover-fill remaining capacity as a merchant-safe count when leftover fill exists, or an honest empty line. Buyer identities stay out. Reuses leftover-fill remaining-capacity wording.
- **Leftover-fill label copy.** Copy leftover-fill offer label as organizer-private one-line Markdown. Merchant and variant only. Honest empty when leftover fill is missing. Clipboard uses a textarea fallback. Distinct from leftover-fill merchant copy and leftover-fill pickup copy. This is not a merchant export. The control stays off the merchant panel.
- **1.4.0 review packets kept.** Allocated coverage, sole-offer dependency, unserved reasons, same-cohort alternatives, withdrawal stress, shipping exposure, delivery slack, bounded capacity and minimum previews, and private review packet export/inspect remain.

## Earlier changelog (1.4.13)

Leftover-fill delivery copy through open-paren, leftover-fill delivery copy jump, hide winner-allocated buyers jump, Athletics carnival lunch, hide leftover-fill buyers, leftover print leftover-fill pickup days, and leftover-fill pickup copy, kept on top of 1.4.0 review packets and 1.4.1/1.4.2/1.4.3/1.4.4/1.4.5/1.4.6/1.4.7/1.4.8/1.4.9/1.4.10/1.4.11/1.4.12 tools. This remains an offline simulator. Residual coverage is leftover planning, not checkout. Merchant views, JSON, CSV, overlap Markdown, leftover print, winning merchant copy, winning fulfillment copy, and remaining-capacity copy still receive aggregates, counts, merchant labels, or fulfillment modes only. Organizer leftover copies are labeled organizer-private. Review packets stay private and unsigned.

- **Keyboard leftover-fill delivery copy.** `(` copies leftover-fill delivery days through the existing leftover-fill delivery copy control. Same organizer-private count-only Markdown as the leftover-fill delivery button. Ignored while typing. Help lists it. Distinct from `~` leftover-fill fulfillment. Not a merchant export.
- **Keyboard leftover-fill delivery copy jump.** `)` focuses the leftover-fill delivery copy control, or the leftover heading if that control is missing. Ignored while typing. Help lists it. Does not copy.
- **Keyboard hide winner-allocated buyers jump.** `#` focuses hide winner-allocated buyers, or the buyer list if that control is missing. Ignored while typing. Help lists it. Distinct from `@` hide leftover-only buyers.
- **Athletics carnival lunch.** A synthetic carnival lunch preset with mixed athletics pie, track salad, and track water variants, mixed quantities, a hall pickup offer, and a track-side delivery offer. Distinct from neighbourhood coffee, studio chairs, pantry staples, office pantry, hardware tools, garden seed, school fete catering, office fruit box, coffee price ladder, library photocopy paper, sports club match-day kit, surf club first-aid kit, theatre wardrobe kit, community choir folders, scout camp, school excursion lunch, netball canteen lunch, and swimming carnival lunch. Not live inventory.
- **Hide leftover-fill buyers.** Display filter hides organizer buyer rows in leftover-fill selected buyer ids. Winner-allocated and unserved buyers stay visible. Matching is unchanged. The last hide choice is kept in workspace JSON. Older files omit it and show every buyer. Distinct from `hideExcludedBuyers`, `hideUnwinnableOffers`, `hideCoveredLeftoverRows`, `hideTertiaryLeftoverRow`, `hideLeftoverFillRow`, `hideZeroRemainingCapacityOffers`, `hideOffersWithRemainingCapacity`, `hideFullyFilledBuyers`, `hideBuyersWithLeftover`, `hideUnservedBuyers`, `hideLeftoverOnlyBuyers`, and `hideWinnerAllocatedBuyers`.
- **Leftover print leftover-fill pickup days.** Leftover print (`body.print-leftover`) includes leftover-fill pickup days as a merchant-safe count line when leftover fill exists and leftover fill is pickup, or an honest empty line. Buyer identities stay out.
- **Leftover-fill pickup copy.** Copy leftover-fill pickup days as organizer-private one-line Markdown. Count only (days) when leftover fill is pickup. Honest empty when leftover fill is missing or leftover fill is shipping. Clipboard uses a textarea fallback. Distinct from leftover-fill delivery copy and leftover-fill fulfillment copy. This is not a merchant export. The control stays off the merchant panel.
- **1.4.0 review packets kept.** Allocated coverage, sole-offer dependency, unserved reasons, same-cohort alternatives, withdrawal stress, shipping exposure, delivery slack, bounded capacity and minimum previews, and private review packet export/inspect remain.

## Earlier changelog (1.4.12)

Leftover-fill fulfillment copy through tilde, leftover-fill fulfillment copy jump, hide leftover-only buyers jump, Swimming carnival lunch, hide winner-allocated buyers, leftover print leftover-fill delivery days, and leftover-fill delivery copy, kept on top of 1.4.0 review packets and 1.4.1/1.4.2/1.4.3/1.4.4/1.4.5/1.4.6/1.4.7/1.4.8/1.4.9/1.4.10/1.4.11 tools. This remains an offline simulator. Residual coverage is leftover planning, not checkout. Merchant views, JSON, CSV, overlap Markdown, leftover print, winning merchant copy, winning fulfillment copy, and remaining-capacity copy still receive aggregates, counts, merchant labels, or fulfillment modes only. Organizer leftover copies are labeled organizer-private. Review packets stay private and unsigned.

- **Keyboard leftover-fill fulfillment copy.** `~` copies leftover-fill fulfillment through the existing leftover-fill fulfillment copy control. Same organizer-private pickup-or-shipping Markdown as the leftover-fill fulfillment button. Ignored while typing. Help lists it. Distinct from `}` leftover-fill remaining capacity and `"` leftover fill merchant. Not a merchant export.
- **Keyboard leftover-fill fulfillment copy jump.** `!` focuses the leftover-fill fulfillment copy control, or the leftover heading if that control is missing. Ignored while typing. Help lists it. Does not copy.
- **Keyboard hide leftover-only buyers jump.** `@` focuses hide leftover-only buyers, or the buyer list if that control is missing. Ignored while typing. Help lists it. Distinct from `|` hide unserved buyers.
- **Swimming carnival lunch.** A synthetic carnival lunch preset with mixed carnival pie, lane salad, and pool water variants, mixed quantities, a hall pickup offer, and a pool-deck delivery offer. Distinct from neighbourhood coffee, studio chairs, pantry staples, office pantry, hardware tools, garden seed, school fete catering, office fruit box, coffee price ladder, library photocopy paper, sports club match-day kit, surf club first-aid kit, theatre wardrobe kit, community choir folders, scout camp, school excursion lunch, and netball canteen lunch. Not live inventory.
- **Hide winner-allocated buyers.** Display filter hides organizer buyer rows that received winner units. Leftover-only and unserved buyers stay visible. Matching is unchanged. The last hide choice is kept in workspace JSON. Older files omit it and show every buyer. Distinct from `hideExcludedBuyers`, `hideUnwinnableOffers`, `hideCoveredLeftoverRows`, `hideTertiaryLeftoverRow`, `hideLeftoverFillRow`, `hideZeroRemainingCapacityOffers`, `hideOffersWithRemainingCapacity`, `hideFullyFilledBuyers`, `hideBuyersWithLeftover`, `hideUnservedBuyers`, and `hideLeftoverOnlyBuyers`.
- **Leftover print leftover-fill delivery days.** Leftover print (`body.print-leftover`) includes leftover-fill delivery days as a merchant-safe count line when leftover fill exists, or an honest empty line. Buyer identities stay out.
- **Leftover-fill delivery copy.** Copy leftover-fill delivery days as organizer-private one-line Markdown. Count only (days). Honest empty when leftover fill is missing. Clipboard uses a textarea fallback. Distinct from leftover-fill fulfillment copy and leftover-fill remaining copy. This is not a merchant export. The control stays off the merchant panel.
- **1.4.0 review packets kept.** Allocated coverage, sole-offer dependency, unserved reasons, same-cohort alternatives, withdrawal stress, shipping exposure, delivery slack, bounded capacity and minimum previews, and private review packet export/inspect remain.

## Earlier changelog (1.4.11)

Leftover-fill remaining copy through close-brace, leftover-fill remaining copy jump, hide unserved buyers jump, Netball canteen lunch, hide leftover-only buyers, leftover print leftover-fill fulfillment, and leftover-fill fulfillment copy, kept on top of 1.4.0 review packets and 1.4.1/1.4.2/1.4.3/1.4.4/1.4.5/1.4.6/1.4.7/1.4.8/1.4.9/1.4.10 tools. This remains an offline simulator. Residual coverage is leftover planning, not checkout. Merchant views, JSON, CSV, overlap Markdown, leftover print, winning merchant copy, winning fulfillment copy, and remaining-capacity copy still receive aggregates, counts, merchant labels, or fulfillment modes only. Organizer leftover copies are labeled organizer-private. Review packets stay private and unsigned.

- **Keyboard leftover-fill remaining-capacity copy.** `}` copies leftover-fill remaining capacity through the existing leftover-fill remaining copy control. Same organizer-private remaining-capacity Markdown as the leftover-fill remaining button. Ignored while typing. Help lists it. Distinct from `"` leftover fill merchant and `=` hide buyers with leftover. Not a merchant export.
- **Keyboard leftover-fill remaining copy jump.** `+` focuses the leftover-fill remaining copy control, or the leftover heading if that control is missing. Ignored while typing. Help lists it.
- **Keyboard hide unserved buyers jump.** `|` focuses hide unserved buyers, or the buyer list if that control is missing. Ignored while typing. Help lists it. Distinct from `=` hide buyers with leftover.
- **Netball canteen lunch.** A synthetic canteen lunch preset with mixed meat pie, garden salad, and still water variants, mixed quantities, a hall pickup offer, and a gate delivery offer. Distinct from neighbourhood coffee, studio chairs, pantry staples, office pantry, hardware tools, garden seed, school fete catering, office fruit box, coffee price ladder, library photocopy paper, sports club match-day kit, surf club first-aid kit, theatre wardrobe kit, community choir folders, scout camp, and school excursion lunch. Not live inventory.
- **Hide leftover-only buyers.** Display filter hides organizer buyer rows with zero winner units and leftover fill and/or tertiary units greater than zero. Unserved buyers are not leftover-only. Matching is unchanged. The last hide choice is kept in workspace JSON. Older files omit it and show every buyer. Distinct from `hideExcludedBuyers`, `hideUnwinnableOffers`, `hideCoveredLeftoverRows`, `hideTertiaryLeftoverRow`, `hideLeftoverFillRow`, `hideZeroRemainingCapacityOffers`, `hideOffersWithRemainingCapacity`, `hideFullyFilledBuyers`, `hideBuyersWithLeftover`, and `hideUnservedBuyers`.
- **Leftover print leftover-fill fulfillment.** Leftover print (`body.print-leftover`) includes leftover-fill fulfillment as pickup or shipping when leftover fill exists, or an honest empty line. Buyer identities stay out.
- **Leftover-fill fulfillment copy.** Copy leftover-fill fulfillment as organizer-private one-line Markdown. Pickup or shipping on the leftover-fill offer, or honest empty. Clipboard uses a textarea fallback. Distinct from leftover-fill remaining copy, leftover-fill merchant copy, leftover fill units, and winning fulfillment copy. This is not a merchant export. The control stays off the merchant panel.
- **1.4.0 review packets kept.** Allocated coverage, sole-offer dependency, unserved reasons, same-cohort alternatives, withdrawal stress, shipping exposure, delivery slack, bounded capacity and minimum previews, and private review packet export/inspect remain.

## Earlier changelog (1.4.10)

Leftover-fill merchant copy through quote, leftover-fill merchant copy jump, hide remaining-capacity-positive offers jump, school excursion lunch, hide unserved buyers, leftover print leftover-fill remaining capacity, and leftover-fill remaining capacity copy, kept on top of 1.4.0 review packets and 1.4.1/1.4.2/1.4.3/1.4.4/1.4.5/1.4.6/1.4.7/1.4.8/1.4.9 tools. This remains an offline simulator. Residual coverage is leftover planning, not checkout. Merchant views, JSON, CSV, overlap Markdown, leftover print, winning merchant copy, winning fulfillment copy, and remaining-capacity copy still receive aggregates, counts, merchant labels, or fulfillment modes only. Organizer leftover copies are labeled organizer-private. Review packets stay private and unsigned.

- **Keyboard leftover-fill merchant copy.** `"` copies leftover-fill merchant through the existing leftover-fill merchant copy control. Same organizer-private merchant-label Markdown as the leftover-fill merchant button. Ignored while typing. Help lists it. Distinct from `:` uncovered leftover units and `'` leftover fill units. Not a merchant export.
- **Keyboard leftover-fill merchant copy jump.** `_` focuses the leftover-fill merchant copy control, or the leftover heading if that control is missing. Ignored while typing. Help lists it.
- **Keyboard hide offers with remaining capacity jump.** `{` focuses hide offers with remaining capacity, or the offers list if that control is missing. Ignored while typing. Help lists it. Distinct from `=` hide buyers with leftover.
- **School excursion lunch.** A synthetic excursion lunch preset with mixed sandwich, fruit cup, and water crate variants, mixed quantities, a hall pickup offer, and a coach-stop delivery offer. Distinct from neighbourhood coffee, studio chairs, pantry staples, office pantry, hardware tools, garden seed, school fete catering, office fruit box, coffee price ladder, library photocopy paper, sports club match-day kit, surf club first-aid kit, theatre wardrobe kit, community choir folders, and scout camp. Not live inventory.
- **Hide unserved buyers.** Display filter hides organizer buyer rows with zero allocated units after the winner. Matching is unchanged. The last hide choice is kept in workspace JSON. Older files omit it and show every buyer. Distinct from `hideExcludedBuyers`, `hideUnwinnableOffers`, `hideCoveredLeftoverRows`, `hideTertiaryLeftoverRow`, `hideLeftoverFillRow`, `hideZeroRemainingCapacityOffers`, `hideOffersWithRemainingCapacity`, `hideFullyFilledBuyers`, and `hideBuyersWithLeftover`.
- **Leftover print leftover-fill remaining capacity.** Leftover print (`body.print-leftover`) includes leftover-fill merchant label and leftover-fill remaining capacity as a merchant-safe count when leftover fill exists. Buyer identities stay out.
- **Leftover-fill remaining capacity copy.** Copy leftover-fill remaining capacity as organizer-private one-line Markdown. Remaining capacity on the leftover-fill offer, or honest empty. Clipboard uses a textarea fallback. Distinct from leftover-fill merchant copy, leftover fill units, uncovered leftover units, and winning remaining-capacity copy. This is not a merchant export. The control stays off the merchant panel.
- **1.4.0 review packets kept.** Allocated coverage, sole-offer dependency, unserved reasons, same-cohort alternatives, withdrawal stress, shipping exposure, delivery slack, bounded capacity and minimum previews, and private review packet export/inspect remain.

## Earlier changelog (1.4.9)

Uncovered leftover unit-count copy through colon, uncovered leftover units copy jump, hide buyers with leftover jump, scout camp kit, hide offers with remaining capacity, leftover print leftover-fill merchant label, and leftover-fill merchant copy, kept on top of 1.4.0 review packets and 1.4.1/1.4.2/1.4.3/1.4.4/1.4.5/1.4.6/1.4.7/1.4.8 tools. This remains an offline simulator. Residual coverage is leftover planning, not checkout. Merchant views, JSON, CSV, overlap Markdown, leftover print, winning merchant copy, winning fulfillment copy, and remaining-capacity copy still receive aggregates, counts, merchant labels, or fulfillment modes only. Organizer leftover copies are labeled organizer-private. Review packets stay private and unsigned.

- **Keyboard uncovered leftover unit-count copy.** `:` copies uncovered leftover units through the existing uncovered leftover units copy control. Same organizer-private count-only Markdown as the uncovered leftover units button. Ignored while typing. Help lists it. Not a merchant export.
- **Keyboard uncovered leftover units copy jump.** `-` focuses the uncovered leftover units copy control, or the leftover heading if that control is missing. Ignored while typing. Help lists it.
- **Keyboard hide buyers with leftover jump.** `=` focuses hide buyers with leftover, or the buyer list if that control is missing. Ignored while typing. Help lists it.
- **Scout camp kit.** A synthetic camp kit preset with mixed synthetic compass, mess-tin, and groundsheet variants, mixed quantities, a scout-hall pickup offer, and a campsite delivery offer. Distinct from neighbourhood coffee, studio chairs, pantry staples, office pantry, hardware tools, garden seed, school fete catering, office fruit box, coffee price ladder, library photocopy paper, sports club match-day kit, surf club first-aid kit, theatre wardrobe kit, and community choir folders. Not live inventory.
- **Hide offers with remaining capacity.** Display filter hides offers whose remaining capacity after the winner is greater than zero. Inverse of `hideZeroRemainingCapacityOffers`. Matching is unchanged. The last hide choice is kept in workspace JSON. Older files omit it and show every offer. Distinct from `hideExcludedBuyers`, `hideUnwinnableOffers`, `hideCoveredLeftoverRows`, `hideTertiaryLeftoverRow`, `hideLeftoverFillRow`, `hideZeroRemainingCapacityOffers`, `hideFullyFilledBuyers`, and `hideBuyersWithLeftover`.
- **Leftover print leftover-fill merchant label.** Leftover print (`body.print-leftover`) includes leftover-fill merchant label as a merchant-safe line when leftover fill exists. Buyer identities stay out.
- **Leftover-fill merchant copy.** Copy leftover-fill merchant label as organizer-private one-line Markdown. Merchant label only. Clipboard uses a textarea fallback. Distinct from leftover fill copy, leftover fill units, uncovered leftover units, and winning merchant copy. This is not a merchant export. The control stays off the merchant panel.
- **1.4.0 review packets kept.** Allocated coverage, sole-offer dependency, unserved reasons, same-cohort alternatives, withdrawal stress, shipping exposure, delivery slack, bounded capacity and minimum previews, and private review packet export/inspect remain.

## Earlier changelog (1.4.8)

Leftover fill unit-count copy through apostrophe, leftover-fill-units copy jump, hide fully filled buyers jump, community choir folders, hide buyers with leftover, leftover print uncovered leftover unit-count, and uncovered leftover unit-count copy, kept on top of 1.4.0 review packets and 1.4.1/1.4.2/1.4.3/1.4.4/1.4.5/1.4.6/1.4.7 tools. This remains an offline simulator. Residual coverage is leftover planning, not checkout. Merchant views, JSON, CSV, overlap Markdown, leftover print, winning merchant copy, winning fulfillment copy, and remaining-capacity copy still receive aggregates, counts, merchant labels, or fulfillment modes only. Organizer leftover copies are labeled organizer-private. Review packets stay private and unsigned.

- **Keyboard leftover fill unit-count copy.** `'` copies leftover fill unit-count through the existing leftover-fill-units copy control. Same organizer-private count-only Markdown as the leftover-fill-units button. Honest empty when leftover fill is missing. Ignored while typing. Help lists it.
- **Keyboard leftover-fill-units copy jump.** `<` focuses the leftover-fill-units copy control, or the leftover heading if that control is missing. Ignored while typing. Help lists it.
- **Keyboard hide fully filled buyers jump.** `>` focuses hide fully filled buyers, or the buyer list if that control is missing. Ignored while typing. Help lists it.
- **Community choir folders.** A synthetic SATB folder preset with mixed soprano, alto, tenor, and bass folder variants, mixed quantities, a hall pickup offer, and a rehearsal-room delivery offer. Distinct from neighbourhood coffee, studio chairs, pantry staples, office pantry, hardware tools, garden seed, school fete catering, office fruit box, coffee price ladder, library photocopy paper, sports club match-day kit, surf club first-aid kit, and theatre wardrobe kit. Not live inventory.
- **Hide buyers with leftover.** Display filter hides organizer buyer rows that still have leftover after the winner. Inverse of `hideFullyFilledBuyers`. Matching is unchanged. The last hide choice is kept in workspace JSON. Older files omit it and show every buyer. Distinct from `hideExcludedBuyers`, `hideUnwinnableOffers`, `hideCoveredLeftoverRows`, `hideTertiaryLeftoverRow`, `hideLeftoverFillRow`, `hideZeroRemainingCapacityOffers`, and `hideFullyFilledBuyers`.
- **Leftover print uncovered leftover unit count.** Leftover print (`body.print-leftover`) includes uncovered leftover unit-count as a count line when leftover exists. Merchant label stays. Buyer identities stay out.
- **Uncovered leftover unit-count copy.** Copy uncovered leftover units as organizer-private one-line Markdown. Count only. Clipboard uses a textarea fallback. Distinct from uncovered leftover counts copy and leftover fill unit-count copy. This is not a merchant export.
- **1.4.0 review packets kept.** Allocated coverage, sole-offer dependency, unserved reasons, same-cohort alternatives, withdrawal stress, shipping exposure, delivery slack, bounded capacity and minimum previews, and private review packet export/inspect remain.

## Earlier changelog (1.4.7)

Leftover fill copy through semicolon, requested-units copy jump, leftover print jump, theatre wardrobe kit, hide fully filled buyers, leftover-fill unit-count print, and leftover fill unit-count copy, kept on top of 1.4.0 review packets and 1.4.1/1.4.2/1.4.3/1.4.4/1.4.5/1.4.6 tools. This remains an offline simulator. Residual coverage is leftover planning, not checkout. Merchant views, JSON, CSV, overlap Markdown, leftover print, winning merchant copy, winning fulfillment copy, and remaining-capacity copy still receive aggregates, counts, merchant labels, or fulfillment modes only. Organizer leftover copies are labeled organizer-private. Review packets stay private and unsigned.

- **Keyboard leftover fill copy.** `;` copies leftover fill through the existing leftover-fill copy control. Same organizer-private one-liner as `y`. Merchant label and leftover counts. Honest empty when none. Ignored while typing. Help lists it.
- **Keyboard requested units copy jump.** `[` focuses the requested-units copy control, or the requested units heading if that control is missing. Ignored while typing. Help lists it.
- **Keyboard leftover print jump.** `]` focuses the leftover print leftover-fill control, or leftover heading if missing. Does not jump to remaining-capacity; `.` already does that. Ignored while typing. Help lists it.
- **Theatre wardrobe kit.** A synthetic costume wardrobe preset with mixed period costume, rehearsal blacks, and quick-change cloak variants, mixed quantities, a stage-door delivery offer, and a green-room pickup offer. Distinct from neighbourhood coffee, studio chairs, pantry staples, office pantry, hardware tools, garden seed, school fete catering, office fruit box, coffee price ladder, library photocopy paper, sports club match-day kit, and surf club first-aid kit. Not live inventory.
- **Hide fully filled buyers.** Display filter hides organizer buyer rows with no leftover after the winner. Matching is unchanged. The last hide choice is kept in workspace JSON. Older files omit it and show every buyer. Distinct from `hideExcludedBuyers`, `hideUnwinnableOffers`, `hideCoveredLeftoverRows`, `hideTertiaryLeftoverRow`, `hideLeftoverFillRow`, and `hideZeroRemainingCapacityOffers`.
- **Leftover print leftover-fill unit count.** Leftover print (`body.print-leftover`) includes leftover-fill unit-count when leftover fill exists. Merchant label stays. Buyer identities stay out.
- **Leftover fill unit-count copy.** Copy leftover fill units as organizer-private one-line Markdown. Count only. Clipboard uses a textarea fallback. This is not a merchant export.
- **1.4.0 review packets kept.** Allocated coverage, sole-offer dependency, unserved reasons, same-cohort alternatives, withdrawal stress, shipping exposure, delivery slack, bounded capacity and minimum previews, and private review packet export/inspect remain.

## Earlier changelog (1.4.6)

Remaining-capacity copy and jump, leftover-fill copy jump, surf club first-aid kit, remaining-capacity display filter, leftover-fill merchant print, and requested-units copy, kept on top of 1.4.0 review packets and 1.4.1/1.4.2/1.4.3/1.4.4/1.4.5 tools. This remains an offline simulator. Residual coverage is leftover planning, not checkout. Merchant views, JSON, CSV, overlap Markdown, leftover print, winning merchant copy, winning fulfillment copy, and remaining-capacity copy still receive aggregates, counts, merchant labels, or fulfillment modes only. Organizer leftover copies are labeled organizer-private. Review packets stay private and unsigned.

- **Keyboard remaining capacity copy.** `,` copies remaining capacity on the unlocked winner through the existing remaining-capacity copy control. Merchant-safe one-liner. Honest empty when none unlocked. Ignored while typing. Help lists it.
- **Keyboard remaining capacity jump.** `.` focuses the remaining-capacity copy control, or the winner heading if that control is missing. Ignored while typing. Help lists it.
- **Keyboard leftover fill copy jump.** `/` without Shift focuses the leftover-fill copy control. `y` still copies leftover fill. Shift+/ stays `?` help. Ignored while typing. Help lists it.
- **Surf club first-aid kit.** A synthetic first-aid preset with mixed bandage, ice-pack, and saline variants, mixed quantities, a clubhouse pickup offer, and a beach delivery offer. Distinct from neighbourhood coffee, studio chairs, pantry staples, office pantry, hardware tools, garden seed, school fete catering, office fruit box, coffee price ladder, library photocopy paper, and sports club match-day kit. Not live inventory.
- **Hide zero remaining-capacity offers.** Display filter hides offers whose remaining capacity after the winner is zero, or unwinnable offers already allocated. Matching is unchanged. The last hide choice is kept in workspace JSON. Older files omit it and show every offer. Distinct from `hideExcludedBuyers`, `hideUnwinnableOffers`, `hideCoveredLeftoverRows`, `hideTertiaryLeftoverRow`, and `hideLeftoverFillRow`.
- **Leftover print leftover-fill merchant.** Leftover print (`body.print-leftover`) includes the leftover-fill merchant label when leftover fill exists. Merchant label only. Buyer identities stay out.
- **Requested units copy.** Copy requested units as organizer-private one-line Markdown. Count only. Clipboard uses a textarea fallback. This is not a merchant export.
- **1.4.0 review packets kept.** Allocated coverage, sole-offer dependency, unserved reasons, same-cohort alternatives, withdrawal stress, shipping exposure, delivery slack, bounded capacity and minimum previews, and private review packet export/inspect remain.

## Earlier changelog (1.4.5)

Leftover fill copy and jump, sports club match-day kit, leftover fill display filter, leftover print requested units, and remaining-capacity copy, kept on top of 1.4.0 review packets and 1.4.1/1.4.2/1.4.3/1.4.4 review UI. This remains an offline simulator. Residual coverage is leftover planning, not checkout. Merchant views, JSON, CSV, overlap Markdown, leftover print, winning merchant copy, winning fulfillment copy, and remaining-capacity copy still receive aggregates, counts, merchant labels, or fulfillment modes only. Organizer leftover copies are labeled organizer-private. Review packets stay private and unsigned.

- **Keyboard leftover fill copy.** `y` copies leftover fill (the secondary leftover offer, not tertiary) as organizer-private one-line Markdown: merchant label, leftover buyer-count, leftover unit-count. Honest empty when no leftover fill. Clipboard uses a textarea fallback. This is not a merchant export.
- **Keyboard leftover fill jump.** `z` focuses the leftover-fill row when leftover exists and the row is visible, otherwise the leftover heading. Ignored while typing. Organizer surface only. Help lists it.
- **Sports club match-day kit.** A synthetic match-day kit preset with mixed jersey, shorts, and water-crate variants, mixed quantities, a clubhouse pickup offer, and a field delivery offer. Distinct from neighbourhood coffee, studio chairs, pantry staples, office pantry, hardware tools, garden seed, school fete catering, office fruit box, coffee price ladder, and library photocopy paper. Not live inventory.
- **Hide leftover fill row.** Display filter hides the leftover-fill (secondary leftover) coverage row when present. Matching is unchanged. The last hide choice is kept in workspace JSON. Older files omit it and show every leftover row. Distinct from `hideExcludedBuyers`, `hideUnwinnableOffers`, `hideCoveredLeftoverRows`, and `hideTertiaryLeftoverRow`.
- **Leftover print requested units.** Leftover print (`body.print-leftover`) keeps the winner as a merchant label and uncovered leftover counts, and includes requested units as a count (the group requested-units total). Counts only. Buyer identities stay out.
- **Winning remaining capacity copy.** Copy remaining capacity on the unlocked winner as a merchant-safe one-liner. Honest empty when none unlocked. Clipboard uses a textarea fallback. Buyer identities stay out.
- **1.4.0 review packets kept.** Allocated coverage, sole-offer dependency, unserved reasons, same-cohort alternatives, withdrawal stress, shipping exposure, delivery slack, bounded capacity and minimum previews, and private review packet export/inspect remain.

## Earlier changelog (1.4.4)

Library photocopy paper, leftover unspent item headroom copy, uncovered leftover jump, leftover print uncovered counts, winning fulfillment copy, and a display filter for the tertiary leftover row, kept on top of 1.4.0 review packets and 1.4.1/1.4.2/1.4.3 review UI. This remains an offline simulator. Residual coverage is leftover planning, not checkout. Merchant views, JSON, CSV, overlap Markdown, leftover print, winning merchant copy, and winning fulfillment copy still receive aggregates, counts, merchant labels, or fulfillment modes only. Organizer leftover copies are labeled organizer-private. Review packets stay private and unsigned.

- **Keyboard uncovered leftover coverage jump.** `d` focuses the first uncovered leftover coverage row when it is visible, otherwise the leftover heading. Ignored while typing. Organizer surface only.
- **Keyboard group headroom.** `g` focuses group headroom. Ignored while typing.
- **Keyboard leftover headroom copy.** `i` copies leftover unspent item headroom as organizer-private one-line Markdown. Currency and included-buyer counts only. Not a rebate. Clipboard uses a textarea fallback. This is not a merchant export.
- **Keyboard requested units.** `q` focuses requested units. Ignored while typing.
- **Keyboard private buyer report.** `x` focuses Export private buyer report. Ignored while typing. That export stays organizer-private.
- **Library photocopy paper.** A synthetic library paper preset with mixed A4 and A3 ream sizes, mixed quantities, a desk delivery offer, and a lobby pickup offer. Distinct from coffee, studio chairs, pantry staples, office pantry, hardware tools, garden seed, school fete catering, office fruit box, and the price ladder. Not live inventory.
- **Hide tertiary leftover row.** Display filter hides the tertiary leftover-coverage row when present. Matching is unchanged. The last hide choice is kept in workspace JSON. Older files omit it and show every leftover row. Distinct from `hideExcludedBuyers`, `hideUnwinnableOffers`, and `hideCoveredLeftoverRows`.
- **Leftover print uncovered leftover counts.** Leftover print (`body.print-leftover`) keeps the winner as a merchant label and includes uncovered leftover buyer-count and unit-count. Counts only. Buyer identities stay out.
- **Winning fulfillment copy.** Copy pickup or shipping for the unlocked winner as a merchant-safe one-liner. Honest empty when none unlocked. Clipboard uses a textarea fallback. Buyer identities stay out.
- **1.4.0 review packets kept.** Allocated coverage, sole-offer dependency, unserved reasons, same-cohort alternatives, withdrawal stress, shipping exposure, delivery slack, bounded capacity and minimum previews, and private review packet export/inspect remain.

## Earlier changelog (1.4.3)

Organizer leftover counts, office fruit box, leftover display filter, merchant-label copy, leftover print overlap counts, and keyboard jumps, kept on top of 1.4.0 review packets and 1.4.1/1.4.2 review UI. This remains an offline simulator. Residual coverage is leftover planning, not checkout. Merchant views, JSON, CSV, overlap Markdown, leftover print, and winning merchant copy still receive aggregates, counts, or merchant labels only. Organizer leftover copies are labeled organizer-private. Review packets stay private and unsigned.

- **Uncovered leftover counts copy.** Copy uncovered leftover buyer and unit counts as organizer-private Markdown. Clipboard uses a textarea fallback. This is not a merchant export.
- **Office fruit box.** A synthetic weekly fruit preset with citrus mix, apple crate, and mixed seasonal variants, mixed quantities, and a lobby pickup offer. Distinct from coffee, studio chairs, pantry staples, office pantry, hardware tools, garden seed, the price ladder, and school fete catering. Not live inventory.
- **Keyboard leftover copy.** `c` copies leftover residual coverage as organizer-private Markdown.
- **Keyboard tertiary leftover jump.** `t` focuses tertiary leftover fill when it exists, otherwise the leftover heading. Ignored while typing.
- **Keyboard fulfillment filter.** `f` focuses the existing offer fulfillment filter, opening Merchant table as needed. Ignored while typing.
- **Keyboard leftover headroom.** `h` focuses unspent item headroom after the winner. Ignored while typing.
- **Keyboard review jump.** `k` opens Review buyer coverage if it is closed and focuses the summary. Ignored while typing. 1.4.0 review packets remain.
- **Hide covered leftover rows.** Display filter hides leftover-coverage table rows that residual fill already covered. Matching is unchanged. The last hide choice is kept in workspace JSON. Older files omit it and show every leftover row. Distinct from `hideExcludedBuyers` and `hideUnwinnableOffers`.
- **Winning merchant label copy.** Copy the unlocked merchant name only. Honest empty when none unlocked. Clipboard uses a textarea fallback. Buyer identities stay out.
- **Leftover print overlap counts.** Leftover print (`body.print-leftover`) keeps the winner as a merchant label and includes merchant-safe variant overlap buyer, unit, and offer counts. Buyer identities stay out.
- **1.4.0 review packets kept.** Allocated coverage, sole-offer dependency, unserved reasons, same-cohort alternatives, withdrawal stress, shipping exposure, delivery slack, bounded capacity and minimum previews, and private review packet export/inspect remain.

## Earlier changelog (1.4.2)

Organizer leftover copy, leftover print, school fete catering, overlap Markdown, and a display filter for locked offers, kept on top of 1.4.1 review packets. This remains an offline simulator. Residual coverage is leftover planning, not checkout. Merchant views, JSON, CSV, overlap Markdown, and merchant-facing print still receive aggregates and counts only. Organizer leftover and winner inspector copies are labeled organizer-private. Review packets stay private and unsigned.

- **Leftover coverage copy.** Copy leftover residual coverage as organizer-private Markdown: buyer counts and units after the winner, leftover fill, and tertiary fill. Clipboard uses a textarea fallback. This is not a merchant export.
- **School fete catering.** A synthetic bulk catering preset with sausage sizzle, cake stall, and drinks cooler variants, mixed quantities, and a hall pickup offer. Distinct from coffee, studio chairs, pantry staples, office pantry, hardware tools, garden seed, and the price ladder. Not live inventory.
- **Keyboard leftover print.** `p` prints an organizer leftover one-pager: leftover table plus the winning merchant label only. Ignored while typing. Help lists it.
- **Keyboard overlap jump.** `v` focuses the variant overlap region. Merchant overlap stays counts and aggregates only.
- **Overlap Markdown copy.** Copy the variant overlap matrix as Markdown of counts only. Labels, IDs, budgets, and allocations stay out. Clipboard uses a textarea fallback. CSV copy still exists.
- **Keyboard uncovered leftover jump.** `j` focuses the first stranded leftover buyer row when leftover exists, otherwise the leftover heading. Organizer surface only. Leftover buyer rows are print-private.
- **Winner inspector summary copy.** Copy the winning offer label, leftover buyers and units, and residual coverage as organizer-private Markdown. Buyer identities stay out of merchant views.
- **Hide unwinnable offers.** Display filter hides locked and zero-unlock offers. Matching is unchanged. Restore shows them again. Merchant views still show counts only. The last hide choice is kept in workspace JSON. Older files omit it and show every offer. `hideExcludedBuyers` is unchanged.
- **Keyboard buyer paste.** `s` focuses the buyer CSV or TSV paste control when you are not typing. Help lists it.
- **1.4.1 review packets kept.** Allocated coverage, sole-offer dependency, unserved reasons, same-cohort alternatives, withdrawal stress, shipping exposure, delivery slack, bounded capacity and minimum previews, and private review packet export/inspect remain.

## Earlier changelog (1.4.1)

Organizer tools for private buyer tables, leftover jumps, pasted intake, and merchant-safe compares, kept on top of 1.4.0 review packets. This remains an offline simulator. Residual coverage is leftover planning, not checkout. Merchant views, JSON, CSV, overlap, briefing, winner markdown, print one-pager, and merchant-facing compares still receive aggregates and counts only. Organizer review packets stay private and unsigned.

- **Organizer buyer CSV export.** Write `label, category, quantity, max unit price, latest delivery days, variants`, with optional max order total. Formula prefixes are escaped. This is private organizer data, not a merchant export. The file round-trips through import.
- **Keyboard leftover jump.** `l` focuses residual coverage when you are not typing. Help lists it.
- **Paste buyers.** Paste TSV or CSV into a textarea. A tab on the first line is read as TSV. The same checks as buyer CSV import apply. Valid paste replaces buyers, keeps offers, and is undoable.
- **Offer identity compare.** Compare two room JSON files by offer id. Shared ids report unit and buyer counts. Missing ids are listed and are not filled with zeros. The open room stays in place. Buyer labels stay out.
- **Hide excluded buyers.** Organizer display filter. Matching stays unchanged. Merchant views still show counts only. The last hide choice is kept in workspace JSON. Older files omit it and show every buyer.
- **Overlap Markdown copy.** Copy the variant overlap matrix as Markdown. Cells and totals are counts only. CSV copy still exists.
- **Keyboard add offer.** `a` opens the merchant table and clicks Add offer when you are not typing.
- **Community garden bulk seed.** A synthetic preset with shared seed and soil variants, mixed quantities, and a pickup offer. Distinct from coffee, office pantry, and hardware tools.
- **Exclusion counts copy.** Copy the inspected offer's merchant-safe exclusion reason counts as Markdown. Counts only.
- **1.4.0 review packets kept.** Allocated coverage, sole-offer dependency, unserved reasons, same-cohort alternatives, withdrawal stress, shipping exposure, delivery slack, bounded capacity and minimum previews, and private review packet export/inspect remain.

## Earlier changelog (1.4.0)

The Buyer room now includes an on-demand organizer review flow: allocated coverage, sole-offer dependency, unserved reasons, same-cohort alternatives, withdrawal stress, shipping exposure, delivery slack, and bounded capacity/minimum previews. Export a private review packet and inspect it later without replacing the current room. Its input snapshot and recomputed results must match; it is an unsigned consistency record, not proof of authorship or merchant consent.

## Earlier changelog (1.3.3)

Organizer tools for merchant-safe offer export, list hygiene, empty-offer recovery, and honest local caps. This remains an offline simulator. Merchant views, JSON, CSV, overlap, briefing, and winner markdown still receive aggregates and counts only.

- **Offer CSV export.** Write `name, capacity, unit price, shipping, fulfillment, variants`. Formula prefixes are escaped. Buyer labels, IDs, budgets, and allocations stay out. Quantity tiers stay in JSON export.
- **Keyboard jump.** `b` focuses the buyer list when you are not typing. `o` still focuses offers. `w` focuses the winner and inspector summary.
- **Buyer variant filter.** Show organizer buyer rows by an accepted variant. Saved buyers and matching stay unchanged. Merchant views still show counts only.
- **Offer sort.** Preview unit price low-high or capacity high-low without changing ids. Apply writes the new order and is undoable.
- **Winner aggregates copy.** Copy merchant, units, included-buyer counts, and residual fills as Markdown. The room title and private buyer rows stay out.
- **Empty-offer recovery.** Deleting every offer leaves a valid empty list. Restore example offers reloads neighbourhood bids, keeps buyers, and is undoable.
- **Local matching cap.** Organizer warning at 40 buyers or 40 offers. That bound keeps the exact allocator responsive. It is not a server quota.
- **Buyers by variant.** Organizer counts of who accepts each variant, with units. Merchant views still show counts only.
- **Fulfillment filter memory.** The last all/shipping/pickup choice is stored in workspace JSON. Older files that omit it still open as all.

## Changelog (1.3.2)

Organizer tools for offer intake, list hygiene, honest compares, and a quieter print page. This remains an offline simulator. Merchant views still receive aggregates and counts only.

- **Offer CSV import.** Import `name, capacity, unit price, shipping, fulfillment, variants`. Formula prefixes are neutralized. Invalid rows name the offer and field. Buyers stay in place.
- **Buyer sort.** Preview label A-Z or quantity high-low without changing ids or saved order. Apply writes the new order and is undoable.
- **Overlap CSV copy.** Copy the variant overlap matrix as formula-safe CSV. Cells and totals are counts only.
- **Keyboard jump.** `o` focuses the offers list when you are not typing. `m` still focuses the merchant inspector.
- **Fulfillment filter.** Show all, shipping, or pickup offer rows on screen. Saved offers and matching stay unchanged.
- **Print one-pager.** Hide coach, help, and private buyer rows. Keep the title, winner aggregates, residual or tertiary fill, and heatmap.
- **Restore last removed buyer.** One session slot. Restore is undoable and keeps the original id when it is free.
- **Mixed-currency warning.** Compared rooms that use different currencies omit landed totals instead of converting them.
- **Duplicate room snapshot.** Copy the open room into a named snapshot with a unique `(copy)` title suffix for later compares.
- **Winner budget leftover.** Organizer-only sum of unused item-ceiling headroom after the winner. Merchant JSON stays aggregates.

## Changelog (1.3.1)

Organizer and merchant workbench updates for leftover planning, share hygiene, and empty-room recovery. This remains an offline simulator. Residual coverage is still a planning aid, not a dual checkout or split invoice.

- **Tertiary residual fill.** After the winner and the next leftover offer, a third distinct offer is tried on remaining whole buyers. Quantities are never split. Organizer briefing names the tertiary fill as aggregates. Compare rooms report leftover and unfilled counts.
- **Variant overlap matrix.** Merchants see how many buyers accept each offered variant, including pairwise overlap. Cells are counts only. Labels, IDs, budgets, and allocations stay out.
- **Redacted share link.** Optional hash uses Buyer 1 through N labels. The default share link still encodes saved private labels. Standalone `file://` pages keep pointing to JSON export.
- **Delivery heatmap CSV.** Aggregate deadline buckets with buyer and unit counts. Formula-like text is escaped the same way as other CSV exports.
- **Keyboard jump.** `m` focuses the merchant inspector region when you are not typing. `?` help lists it with undo, redo, export, and add buyer.
- **Copy as pickup.** One click clones a shipping offer with fulfillment set to pickup and shipping charged at 0. The original offer is unchanged.
- **Empty-room recovery.** Deleting every buyer leaves a valid empty room. Restore neighbourhood example reloads the coffee scenario and is undoable.

## Changelog (1.3.0)

Pooled-purchase workbench updates for organizers who need leftover fill, quantity-ladder planning, and private-label hygiene. This remains an offline simulator. Residual coverage is a planning aid, not a dual checkout.

- **Residual coverage.** After the winning offer is chosen, leftover whole-buyer demand can be tested against the next-best other offer with the same exact allocator. A buyer's quantity is never split. The buyer room shows Unfilled after winner, and merchant JSON keeps only aggregates.
- **Units to the next cheaper tier.** The inspector reports additional whole units, and which excluded buyers could supply them. Merchant-facing tables show counts only. Unreachable bands name capacity or compatibility limits.
- **Buyer CSV import.** Import `label, category, quantity, max unit price, latest delivery days, variants`, with optional max order total. Spreadsheet formula prefixes are neutralized like CSV export. Invalid rows name the buyer and field.
- **Screenshot mode.** Replace organizer labels with Buyer 1 through N without changing the saved room. Export redacted JSON when you need a shareable copy without private names.
- **First-run coach and shortcuts.** A dismissible overlay (skipped for share links) explains pooling, comparing, and inspecting exclusions. `?` opens help, `u`/`r` undo and redo, `e` exports JSON, `n` adds a buyer. Keys are ignored while typing.
- **Capacity leftover bar and delivery heatmap.** SVG plus text equivalents for filled units versus capacity versus the next-tier mark, and aggregate delivery-deadline buckets.
- **Pickup or shipping.** Offer field `fulfillment` is `shipping` or `pickup`. Legacy JSON without the field still ships. Pickup charges zero shipping in matching, budgets, and landed totals. Unknown values are rejected. Quantity `tiers` are unchanged.
- **Organizer briefing.** Markdown export of title, currency, winner aggregates, excluded count, next-tier gap, and residual coverage, without private buyer rows.
- **Office pantry and hardware tools presets.** Distinct categories and variants, including a pickup hardware offer.
- **Exclusion grouping.** Inspector counts price, delivery, variant, budget, capacity leftover, and quantity-versus-capacity reasons while keeping per-buyer detail.
- **Workbench extras.** Compare the open room with two snapshots, copy an offer as a new tier set, and export merchant-safe residual coverage.

### Decision workspace improvements (09-09-2026)

- Set an optional **Max order total** for each buyer to include shipping in eligibility. Blank preserves the existing item-only budget. Each tier checks this ceiling independently.
- **Undo / Redo** preserves up to 50 valid room states during the session. Invalid edits are not saved; Undo restores the last valid state. A new valid edit clears the redo branch.
- Save up to **12 named snapshots** using the room title. Snapshots are independent of current edits and survive reload when browser storage is available. Export individual rooms as JSON for portable backups. Corrupt snapshot storage is preserved and snapshot writes are disabled for that session.
- **Copy** buyers or offers to create independent alternatives, including variants and tiers, with unique IDs.
- Pin a **baseline**, then adjust a room or load a snapshot. Compare participation, winning merchant, and landed totals. Different demand is explicitly flagged; different currencies suppress cost comparisons. A lower total for fewer buyers is not savings.
- Export the **aggregate merchant report** as JSON, without buyer IDs, labels, budgets, or allocations. Export a separate **private buyer CSV** for the inspected offer, including excluded orders and reasons. Formula-like text is escaped; numeric results retain up to eight decimal places.
- The offer chart scrolls with readable rows for all 40 offers and marks the selected price band's minimum. Keyboard users can scroll the chart and retain focus after removing rows.

Autosave status remains visible. Storage failures require JSON export before closing. An unreadable existing autosave is preserved until you explicitly reset the room; current edits can still be exported. Replacing an invalid draft asks before discarding it; valid preset, import, and reset replacements can be undone. Undo history and the pinned baseline are session-only. Share links and full scenario JSON contain private buyer records, unlike the aggregate report. This remains an offline simulator without live inventory, checkout, or merchant authentication.

The standalone module also fixes a duplicate helper declaration that previously prevented the generated GUI from starting. Its generated script now receives a real JavaScript module syntax check in the test suite.

### Earlier quantity price ladders (27-08-2026)

Merchant offers now support up to eight quantity discounts. The exact allocator checks each price at a quantity that can actually be filled, including buyers who can afford only the discounted price. Demand that does not fit capacity cannot unlock a discount.

Open **Price ladder** to see 20 units qualify at A$20 each, with A$9 total shipping. Inspect the price-band table, the A$160 discount against the declared base price, and each included buyer's landed cost. One buyer's shipping exceeds their item-price ceiling and is explicitly flagged. These are synthetic scenario results, not live quotes or guaranteed savings.

The merchant table shows aggregate outcomes only. Existing scenarios, JSON files, share links, and local saves remain compatible. No dependency or installation was added.

## Open the GUI without installing anything

Download [standalone.html](standalone.html), then double-click it. The complete GUI, model, styles, and synthetic examples are inside that one file. It makes no network requests.

The standalone file supports local editing, autosave when the browser permits it, and JSON import and export. A `file://` link points to a path on your own device, so the standalone GUI directs sharing through Export JSON instead of producing a misleading link.

## Launch from the repository

Requirements: Node.js 20 or newer.

On Windows, double-click `launch-windows.cmd`. It starts the local server and opens Common Cart in your default browser. Keep the launcher window open while using the app; press `Ctrl+C` or close the window to stop it.

On Windows, macOS, or Linux, run:

```bash
npm run launch
```

The command opens `http://127.0.0.1:4173` after the server is ready. The listener is loopback IPv4 only. Set `PORT` to an integer from 1 through 65535 to use a different loopback port; omit it to use 4173. Empty or invalid `PORT` values are rejected before the server starts. Pass `--no-open` or set `NO_BROWSER=1` to print the address without opening a browser. If automatic browser opening is unavailable, copy the printed address into a browser. No package installation, account, API key, build, or network connection is required.

For server-only use, run `npm start` and open the printed address manually. `npm start` uses the same `PORT` default and validation.

Run the full local check:

```bash
npm run check
```

## What you can do

- Start from coffee, office chair, community pantry, office pantry bulk, hardware tools, community garden bulk seed, school fete catering, office fruit box, library photocopy paper, sports club match-day kit, surf club first-aid kit, theatre wardrobe kit, community choir folders, or price-ladder examples.
- Add buyers with a category, quantity, price ceiling, delivery limit, and accepted variants, or import those columns from CSV. Sort the organizer list by label or quantity, then apply when you want that order saved. Filter the organizer list by accepted variant without changing matching.
- Add merchant bids with a price, minimum order, delivery time, capacity, shipping cost, and shipping or pickup fulfillment, or import and export name, capacity, unit price, shipping, fulfillment, and variants as CSV. Sort offers by unit price or capacity, then apply when you want that order saved.
- Filter the on-screen offer list by fulfillment without changing saved bids. The last fulfillment filter is kept with local snapshots.
- Edit quantity price tiers and inspect each band's whole-order feasibility, including units still needed for the next cheaper band.
- Review item cost, shipping, landed cost, and ceiling headroom per included buyer locally.
- Compare qualified offers by fulfilled units, group headroom, buyers included, and landed cost.
- Inspect why each buyer order is included, blocked by the minimum, left out by capacity, or incompatible with an offer, grouped by reason.
- See leftover demand after the winner, then a second and third distinct offer, as a planning aid, not a second checkout.
- See which local buyer labels each qualified offer includes; merchant-facing views stay aggregated. Screenshot mode can hide labels. A redacted share link uses Buyer 1 through N.
- Inspect a merchant view that contains aggregate ranges, a variant overlap matrix, and a delivery heatmap, rather than individual records. Copy overlap counts as formula-safe CSV or as Markdown. Copy winner aggregates as Markdown. Copy leftover coverage, leftover fill, leftover fill unit-count, uncovered leftover counts, uncovered leftover unit-count, leftover unspent item headroom, requested units, and the winner inspector summary as organizer-private Markdown. Copy the winning merchant label only. Copy the winning fulfillment mode as pickup or shipping. Copy remaining capacity on the unlocked winner.
- Import or export a scenario as JSON, duplicate the room as a compare snapshot, export an organizer briefing, export heatmap CSV, export offer CSV, or encode the current scenario in a share link. Press `p` for an organizer leftover one-pager, `v` for variant overlap, `j` for uncovered leftover, `d` for uncovered leftover coverage, `s` for buyer paste, `f` for the fulfillment filter, `h` for leftover headroom, `i` for leftover headroom copy, `k` for the organizer review panel, `c` for leftover coverage copy, `t` for tertiary leftover fill, `y` or `;` for leftover fill copy, `z` for leftover fill, `/` for leftover fill copy jump, `g` for group headroom, `q` for requested units, `[` for requested units copy jump, `x` for Export private buyer report, `,` for remaining capacity copy, `.` for remaining capacity copy jump, `]` for leftover print jump, `'` for leftover fill unit-count copy, `<` for leftover-fill-units copy jump, and `>` for hide fully filled buyers.
- Hide locked offers, offers with no remaining capacity after the winner, fully filled buyers, buyers that still have leftover after the winner, fully covered leftover-coverage rows, the leftover-fill coverage row, or the tertiary leftover-coverage row on screen without changing matching. Restore shows locked offers, remaining-capacity offers, fully filled buyers, and buyers with leftover again.
- Recover an empty buyer room with the neighbourhood example, restore example offers without changing buyers, or restore the last buyer removed in this session. Undo returns to the previous valid list.
- Work entirely in the browser with local autosave.

## How matching works

A buyer is compatible with an offer only when all four tests pass:

1. The product category matches.
2. The offered variant is accepted.
3. The unit price is no higher than the buyer's ceiling.
4. Delivery is no later than the buyer's limit.

The allocator performs an exact bounded search for the greatest whole-buyer quantity within merchant capacity. For tiered offers, each price is evaluated inside its own quantity band. The selected cohort must reach that band's minimum without reaching the next band's threshold. The feasible band with the most units wins. Buyers' ceilings cover item prices, not shipping; pickup fulfillment charges 0 shipping during matching. Landed-cost overruns are shown separately without changing legacy eligibility. See [MODEL.md](./MODEL.md) for formulas, bounds, ranking rules, residual coverage, and limitations.

## Repository map

```text
.
|-- index.html              Browser interface
|-- styles.css              Responsive visual system
|-- src/
|   |-- app.js              UI, local storage, import, export, and charts
|   |-- model.js            Pure validation, matching, ranking, and aggregation
|   `-- model.d.ts          Public model types
|-- tests/
|   |-- model.test.mjs      Deterministic matching and validation tests
|   |-- tiers.test.mjs      Quantity-band allocator tests
|   |-- residual.test.mjs   Leftover coverage after the winner
|   |-- next-tier.test.mjs  Units needed for the next cheaper band
|   |-- csv-import.test.mjs Buyer CSV import
|   |-- offer-csv.test.mjs  Merchant offer CSV import and export
|   |-- overlap.test.mjs    Merchant variant overlap counts
|   |-- heatmap.test.mjs    Delivery deadline buckets and CSV
|   |-- empty-room.test.mjs Empty buyer rooms and undoable restore
|   |-- empty-offers.test.mjs Empty offer lists and example restore
|   |-- restore-buyer.test.mjs Last removed buyer restore
|   |-- buyer-sort.test.mjs Organizer buyer sort preview and apply
|   |-- offer-sort.test.mjs Offer sort by unit price or capacity
|   |-- buyer-variant-filter.test.mjs Organizer accepted-variant filter
|   |-- buyer-variant-counts.test.mjs Organizer variant counts
|   |-- leftover-markdown.test.mjs Leftover coverage Markdown
|   |-- leftover-jump.test.mjs Organizer leftover jump
|   |-- winner-inspector-markdown.test.mjs Winner inspector Markdown
|   |-- hide-unwinnable-offers.test.mjs Hide locked offers
|   |-- hide-covered-leftover.test.mjs Hide covered leftover rows
|   |-- hide-tertiary-leftover.test.mjs Hide tertiary leftover row
|   |-- hide-leftover-fill.test.mjs Hide leftover fill coverage row
|   |-- hide-zero-remaining-capacity.test.mjs Hide zero remaining-capacity offers
|   |-- hide-offers-with-remaining-capacity.test.mjs Hide remaining-capacity-positive offers
|   |-- hide-fully-filled-buyers.test.mjs Hide fully filled buyers
|   |-- hide-buyers-with-leftover.test.mjs Hide buyers with leftover
|   |-- hide-unserved-buyers.test.mjs Hide unserved buyers
|   |-- winner-markdown.test.mjs Winner aggregate Markdown copy
|   |-- entry-cap.test.mjs  Local 40-buyer and 40-offer cap warning
|   |-- duplicate-room.test.mjs Room copy snapshots
|   |-- print.test.mjs      Print one-pager privacy
|   |-- shortcuts.test.mjs  Keyboard help for inspector, offers, buyers, and winner
|   |-- standalone.test.mjs Single-file build checks
|-- standalone.html         No-install, single-file GUI
|-- launch-windows.cmd      One-click Windows GUI launcher
|-- scripts/build-standalone.mjs  Deterministic single-file builder
|-- scripts/launch.mjs      Cross-platform GUI launcher
|-- scripts/dev-server.mjs  Dependency-free local server
|-- scripts/listen-config.mjs     Shared PORT default and validation
|-- MODEL.md                Model contract and limitations
|-- SECURITY.md             Security and privacy boundary
`-- CONTRIBUTING.md         Contribution workflow
```

The [root CI workflow](../../.github/workflows/common-cart.yml) runs this component's integrity check.

## Data and privacy

Common Cart makes no network requests. Scenarios are held in browser memory and local storage. Export and link sharing happen only when requested. A share link contains the full scenario, including buyer labels, so review it before sending. Screenshot mode, redacted JSON, and the optional redacted share link replace labels with Buyer 1 through N. Merchant reports, residual coverage JSON, heatmap CSV, variant overlap, overlap CSV, overlap Markdown, offer CSV export, winner aggregate Markdown, leftover print meant for merchant-safe pages, leftover print overlap counts, leftover print uncovered leftover counts, leftover print uncovered leftover unit counts, leftover print requested units, leftover print leftover-fill merchant labels, leftover print leftover-fill unit counts, leftover print leftover-fill remaining capacity, winning merchant label Markdown, winning fulfillment Markdown, winning remaining capacity Markdown, and organizer briefings omit private buyer rows. Leftover coverage Markdown, leftover fill Markdown, leftover fill unit-count Markdown, leftover fill merchant Markdown, leftover fill remaining capacity Markdown, uncovered leftover counts Markdown, uncovered leftover unit-count Markdown, leftover unspent item headroom Markdown, requested units Markdown, and winner inspector summary Markdown are organizer-private and still omit buyer IDs, labels, budgets, and allocations. Offer CSV import does not read buyer columns. Winner budget leftover is organizer-only and is not written into merchant JSON. Organizer leftover buyer rows stay in the buyer room and are hidden from leftover print. Organizer buyer-variant counts are counts only; merchant views still show counts only. Export private buyer report is organizer-private.

The merchant view is an interface boundary, not a formal privacy guarantee. Small cohorts and unusual constraints can still reveal information.

## Non-goals

Common Cart is not a marketplace, procurement recommendation, live price feed, or payment service. It does not verify merchants, reserve inventory, collect identities, execute purchases, allocate disputes, or claim an economically optimal match.

## Contributing

Bug reports and focused pull requests are welcome. Read [CONTRIBUTING.md](./CONTRIBUTING.md) and [SECURITY.md](./SECURITY.md) first.

## License

MIT. See [LICENSE](./LICENSE).

## Organizer review

Open **Review buyer coverage and offer resilience** inside Buyer room. Select a question and run it on demand. Buyer option coverage counts actual whole-order allocations across qualified offers. Results clear after edits, stay out of Merchant table and printing, and respect screenshot-mode labels. These are declared-input alternatives, not reserved orders.

### Sole-offer dependency

Count buyers whose current whole-order allocation is available from only one qualified offer, and sum their units. This measures current option dependency, not post-withdrawal rematching.

### Unserved buyer reasons

For buyers served by no qualified offer, count current exclusion reasons across offers. These are evaluated-band diagnostics, not promises that one relaxed constraint will solve the order.

### Same-cohort offer alternatives

Compare qualified offers only when their selected buyer-ID sets are identical. An alternative dominates on these two declared measures only when total landed cost and delivery days are both no worse and at least one is strictly better. Equal offers are retained.

### Winner withdrawal stress

Remove each included buyer from the current winner in turn and rerun the existing whole-order allocator on that same offer. Report originally served units retained/lost among the other buyers. No cross-merchant or behavioral prediction is made.

### Shipping exposure and headroom

Sum actual charged shipping across allocated buyers and divide by landed total when nonzero. Least remaining ceiling is the minimum of item-ceiling total and optional order budget, minus actual landed cost. Pickup charges zero shipping.

### Delivery slack by included order

Delivery slack equals latestDeliveryDays minus offer.deliveryDays for each actual included order. Counts repeat a buyer across alternative offers and must not be summed as unique demand.

### Capacity increase previews

On demand, rerun at most five ranked offers at up to three distinct capacity increases (10%, 25%, 50%, rounded up). Capacity is bounded at 5,000. These finite previews do not search for an optimal capacity or change merchant terms.

### Minimum-order relaxation preview

Reevaluate each offer with only its base minimum changed to one unit. Capacity, shipping, prices, and quantity-tier thresholds remain unchanged. The preview does not assert merchant acceptance.
