import test from "node:test";
import assert from "node:assert/strict";
import { readFile } from "node:fs/promises";
import { clonePreset, computeResidualCoverage, evaluateMarket, validateScenario } from "../src/model.js";

test("team-sprint cycling carnival lunch leftover fill is hall pickup and stays distinct from scratch cycling carnival lunch", () => {
  const teamSprintCycling = evaluateMarket(clonePreset("teamSprintCyclingCarnivalLunch"));
  const pursuitCycling = evaluateMarket(clonePreset("individualPursuitCyclingCarnivalLunch"));
  const scratchCycling = evaluateMarket(clonePreset("scratchCyclingCarnivalLunch"));
  const pointsRaceCycling = evaluateMarket(clonePreset("pointsRaceCyclingCarnivalLunch"));
  const madisonCycling = evaluateMarket(clonePreset("madisonCyclingCarnivalLunch"));
  const keirinCycling = evaluateMarket(clonePreset("keirinCyclingCarnivalLunch"));
  const hillClimbCycling = evaluateMarket(clonePreset("hillClimbCyclingCarnivalLunch"));
  const timeTrialCycling = evaluateMarket(clonePreset("timeTrialCyclingCarnivalLunch"));
  const criteriumCycling = evaluateMarket(clonePreset("criteriumCyclingCarnivalLunch"));
  const roadCycling = evaluateMarket(clonePreset("roadCyclingCarnivalLunch"));
  const gravelCycling = evaluateMarket(clonePreset("gravelCyclingCarnivalLunch"));
  const trackCycling = evaluateMarket(clonePreset("trackCyclingCarnivalLunch"));
  const cycloCross = evaluateMarket(clonePreset("cycloCrossCarnivalLunch"));
  const bmx = evaluateMarket(clonePreset("bmxCarnivalLunch"));
  const mountainBike = evaluateMarket(clonePreset("mountainBikeCarnivalLunch"));
  const cycling = evaluateMarket(clonePreset("cyclingCarnivalLunch"));
  const triathlon = evaluateMarket(clonePreset("triathlonCarnivalLunch"));
  const surf = evaluateMarket(clonePreset("surfCarnivalLunch"));
  const dragonBoat = evaluateMarket(clonePreset("dragonBoatCarnivalLunch"));
  const kayaking = evaluateMarket(clonePreset("kayakingCarnivalLunch"));
  const canoeing = evaluateMarket(clonePreset("canoeingCarnivalLunch"));
  const sailing = evaluateMarket(clonePreset("sailingCarnivalLunch"));
  const rowing = evaluateMarket(clonePreset("rowingCarnivalLunch"));
  const waterPolo = evaluateMarket(clonePreset("waterPoloCarnivalLunch"));
  const softball = evaluateMarket(clonePreset("softballCarnivalLunch"));
  const baseball = evaluateMarket(clonePreset("baseballCarnivalLunch"));
  const hockey = evaluateMarket(clonePreset("hockeyCarnivalLunch"));
  const rugby = evaluateMarket(clonePreset("rugbyCarnivalLunch"));
  const soccer = evaluateMarket(clonePreset("soccerCarnivalLunch"));
  assert.ok(teamSprintCycling.winner);
  assert.equal(teamSprintCycling.winner.offer.merchant, "Court-side Team-sprint Delivery");
  assert.equal(teamSprintCycling.winner.offer.fulfillment, "shipping");
  assert.equal(teamSprintCycling.winner.fulfilledUnits, 50);
  assert.deepEqual(teamSprintCycling.winner.selectedBuyerIds, ["B01", "B02", "B05"]);
  assert.equal(teamSprintCycling.scenario.title, "Team-sprint cycling carnival lunch");
  assert.notEqual(teamSprintCycling.scenario.title, clonePreset("teamPursuitCyclingCarnivalLunch").title);
  assert.notEqual(teamSprintCycling.scenario.title, clonePreset("individualPursuitCyclingCarnivalLunch").title);
  assert.notEqual(teamSprintCycling.scenario.title, clonePreset("scratchCyclingCarnivalLunch").title);
  assert.notEqual(teamSprintCycling.scenario.title, clonePreset("pointsRaceCyclingCarnivalLunch").title);
  assert.notEqual(teamSprintCycling.scenario.title, clonePreset("omniumCyclingCarnivalLunch").title);
  assert.notEqual(teamSprintCycling.scenario.title, clonePreset("madisonCyclingCarnivalLunch").title);
  assert.notEqual(teamSprintCycling.scenario.title, keirinCycling.scenario.title);
  assert.notEqual(teamSprintCycling.scenario.title, hillClimbCycling.scenario.title);
  assert.notEqual(teamSprintCycling.scenario.title, timeTrialCycling.scenario.title);
  assert.notEqual(teamSprintCycling.scenario.title, criteriumCycling.scenario.title);
  assert.notEqual(teamSprintCycling.scenario.title, roadCycling.scenario.title);
  assert.notEqual(teamSprintCycling.scenario.title, gravelCycling.scenario.title);
  assert.notEqual(teamSprintCycling.scenario.title, trackCycling.scenario.title);
  assert.notEqual(teamSprintCycling.scenario.title, cycloCross.scenario.title);
  assert.notEqual(teamSprintCycling.scenario.title, bmx.scenario.title);
  assert.notEqual(teamSprintCycling.scenario.title, mountainBike.scenario.title);
  assert.notEqual(teamSprintCycling.scenario.title, cycling.scenario.title);
  assert.notEqual(teamSprintCycling.scenario.title, triathlon.scenario.title);
  assert.notEqual(teamSprintCycling.scenario.title, surf.scenario.title);
  assert.notEqual(teamSprintCycling.scenario.title, dragonBoat.scenario.title);
  assert.notEqual(teamSprintCycling.scenario.title, kayaking.scenario.title);
  assert.notEqual(teamSprintCycling.scenario.title, canoeing.scenario.title);
  assert.notEqual(teamSprintCycling.scenario.title, sailing.scenario.title);
  assert.notDeepEqual(clonePreset("teamSprintCyclingCarnivalLunch"), clonePreset("teamPursuitCyclingCarnivalLunch"));
  assert.notDeepEqual(clonePreset("teamSprintCyclingCarnivalLunch"), clonePreset("individualPursuitCyclingCarnivalLunch"));
  assert.notDeepEqual(clonePreset("teamSprintCyclingCarnivalLunch"), clonePreset("scratchCyclingCarnivalLunch"));
  assert.notDeepEqual(clonePreset("teamSprintCyclingCarnivalLunch"), clonePreset("madisonCyclingCarnivalLunch"));
  assert.notDeepEqual(clonePreset("teamSprintCyclingCarnivalLunch"), clonePreset("keirinCyclingCarnivalLunch"));
  assert.notDeepEqual(clonePreset("teamSprintCyclingCarnivalLunch"), clonePreset("hillClimbCyclingCarnivalLunch"));
  assert.notDeepEqual(clonePreset("teamSprintCyclingCarnivalLunch"), clonePreset("timeTrialCyclingCarnivalLunch"));
  assert.notDeepEqual(clonePreset("teamSprintCyclingCarnivalLunch"), clonePreset("criteriumCyclingCarnivalLunch"));
  assert.notDeepEqual(clonePreset("teamSprintCyclingCarnivalLunch"), clonePreset("roadCyclingCarnivalLunch"));
  assert.notDeepEqual(clonePreset("teamSprintCyclingCarnivalLunch"), clonePreset("gravelCyclingCarnivalLunch"));
  assert.notDeepEqual(clonePreset("teamSprintCyclingCarnivalLunch"), clonePreset("trackCyclingCarnivalLunch"));
  assert.notDeepEqual(clonePreset("teamSprintCyclingCarnivalLunch"), clonePreset("cycloCrossCarnivalLunch"));
  assert.notDeepEqual(clonePreset("teamSprintCyclingCarnivalLunch"), clonePreset("bmxCarnivalLunch"));
  assert.notDeepEqual(clonePreset("teamSprintCyclingCarnivalLunch"), clonePreset("mountainBikeCarnivalLunch"));
  assert.notDeepEqual(clonePreset("teamSprintCyclingCarnivalLunch"), clonePreset("cyclingCarnivalLunch"));
  assert.notDeepEqual(clonePreset("teamSprintCyclingCarnivalLunch"), clonePreset("triathlonCarnivalLunch"));
  assert.notDeepEqual(clonePreset("teamSprintCyclingCarnivalLunch"), clonePreset("surfCarnivalLunch"));
  assert.notDeepEqual(clonePreset("teamSprintCyclingCarnivalLunch"), clonePreset("dragonBoatCarnivalLunch"));
  assert.notDeepEqual(clonePreset("teamSprintCyclingCarnivalLunch"), clonePreset("kayakingCarnivalLunch"));
  assert.notDeepEqual(clonePreset("teamSprintCyclingCarnivalLunch"), clonePreset("canoeingCarnivalLunch"));
  assert.notDeepEqual(clonePreset("teamSprintCyclingCarnivalLunch"), clonePreset("sailingCarnivalLunch"));
  assert.notDeepEqual(clonePreset("teamSprintCyclingCarnivalLunch"), clonePreset("rowingCarnivalLunch"));
  assert.notDeepEqual(clonePreset("teamSprintCyclingCarnivalLunch"), clonePreset("waterPoloCarnivalLunch"));
  assert.notDeepEqual(clonePreset("teamSprintCyclingCarnivalLunch"), clonePreset("softballCarnivalLunch"));
  assert.notDeepEqual(clonePreset("teamSprintCyclingCarnivalLunch"), clonePreset("baseballCarnivalLunch"));
  assert.notDeepEqual(clonePreset("teamSprintCyclingCarnivalLunch"), clonePreset("hockeyCarnivalLunch"));
  assert.notDeepEqual(clonePreset("teamSprintCyclingCarnivalLunch"), clonePreset("rugbyCarnivalLunch"));
  assert.notDeepEqual(clonePreset("teamSprintCyclingCarnivalLunch"), clonePreset("soccerCarnivalLunch"));
  assert.notDeepEqual(clonePreset("teamSprintCyclingCarnivalLunch"), clonePreset("pointsRaceCyclingCarnivalLunch"));
  assert.notDeepEqual(clonePreset("teamSprintCyclingCarnivalLunch"), clonePreset("omniumCyclingCarnivalLunch"));
  const leftoverFill = computeResidualCoverage(teamSprintCycling.scenario).secondary;
  assert.ok(leftoverFill);
  assert.equal(leftoverFill.merchant, "Hall Team-sprint Pickup");
  const leftoverOffer = teamSprintCycling.scenario.offers.find((offer) => offer.id === leftoverFill.offerId);
  assert.equal(leftoverOffer.fulfillment, "pickup");
  assert.equal(leftoverOffer.minimumUnits, 43);
  assert.notEqual(leftoverOffer.unitPrice, leftoverOffer.minimumUnits);
  assert.equal(leftoverOffer.unitPrice, 40);
  assert.equal(teamSprintCycling.scenario.buyers.find((buyer) => buyer.id === "B03").quantity, 15);
  assert.equal(teamSprintCycling.scenario.buyers.find((buyer) => buyer.id === "B03").maxUnitPrice, 40);
  assert.equal(teamSprintCycling.scenario.buyers.find((buyer) => buyer.id === "B04").maxUnitPrice, 40);
  assert.equal(teamSprintCycling.scenario.buyers.find((buyer) => buyer.id === "B04").quantity, 30);
  assert.equal(evaluateMarket(clonePreset("scratchCyclingCarnivalLunch")).scenario.offers.find((offer) => offer.merchant === "Hall Scratch Pickup").minimumUnits, 38);
  assert.equal(evaluateMarket(clonePreset("pointsRaceCyclingCarnivalLunch")).scenario.offers.find((offer) => offer.merchant === "Hall Points-race Pickup").minimumUnits, 37);
  assert.equal(evaluateMarket(clonePreset("omniumCyclingCarnivalLunch")).scenario.offers.find((offer) => offer.merchant === "Hall Omnium Pickup").minimumUnits, 36);
  assert.equal(madisonCycling.scenario.offers.find((offer) => offer.merchant === "Hall Madison Pickup").minimumUnits, 35);
  assert.equal(keirinCycling.scenario.offers.find((offer) => offer.merchant === "Hall Keirin Pickup").minimumUnits, 34);
  assert.equal(hillClimbCycling.scenario.offers.find((offer) => offer.merchant === "Hall Hill Climb Pickup").minimumUnits, 33);
  assert.equal(timeTrialCycling.scenario.offers.find((offer) => offer.merchant === "Hall Time Trial Pickup").minimumUnits, 32);
  assert.equal(criteriumCycling.scenario.offers.find((offer) => offer.merchant === "Hall Criterium Pickup").minimumUnits, 31);
  assert.equal(roadCycling.scenario.offers.find((offer) => offer.merchant === "Hall Road Cycling Pickup").minimumUnits, 30);
  assert.equal(gravelCycling.scenario.offers.find((offer) => offer.merchant === "Hall Gravel Cycling Pickup").minimumUnits, 29);
  assert.equal(trackCycling.scenario.offers.find((offer) => offer.merchant === "Hall Track Cycling Pickup").minimumUnits, 28);
  assert.equal(cycloCross.scenario.offers.find((offer) => offer.merchant === "Hall Cyclo-cross Pickup").minimumUnits, 27);
  assert.equal(bmx.scenario.offers.find((offer) => offer.merchant === "Hall BMX Pickup").minimumUnits, 26);
  assert.equal(mountainBike.scenario.offers.find((offer) => offer.merchant === "Hall Mountain Bike Pickup").minimumUnits, 25);
  assert.equal(cycling.scenario.offers.find((offer) => offer.merchant === "Hall Cycling Pickup").minimumUnits, 24);
  assert.equal(triathlon.scenario.offers.find((offer) => offer.merchant === "Hall Triathlon Pickup").minimumUnits, 23);
  assert.equal(surf.scenario.offers.find((offer) => offer.merchant === "Hall Surf Pickup").minimumUnits, 22);
  assert.equal(dragonBoat.scenario.offers.find((offer) => offer.merchant === "Hall Dragon Boat Pickup").minimumUnits, 21);
  assert.equal(kayaking.scenario.offers.find((offer) => offer.merchant === "Hall Kayaking Pickup").minimumUnits, 20);
  assert.equal(canoeing.scenario.offers.find((offer) => offer.merchant === "Hall Canoeing Pickup").minimumUnits, 19);
  assert.equal(sailing.scenario.offers.find((offer) => offer.merchant === "Hall Sailing Pickup").minimumUnits, 18);
  assert.equal(rowing.scenario.offers.find((offer) => offer.merchant === "Hall Rowing Pickup").minimumUnits, 17);
  assert.equal(soccer.scenario.offers.find((offer) => offer.merchant === "Hall Soccer Pickup").minimumUnits, 11);
  assert.equal(rugby.scenario.offers.find((offer) => offer.merchant === "Hall Rugby Pickup").minimumUnits, 12);
  assert.equal(hockey.scenario.offers.find((offer) => offer.merchant === "Hall Hockey Pickup").minimumUnits, 13);
  assert.equal(baseball.scenario.offers.find((offer) => offer.merchant === "Hall Baseball Pickup").minimumUnits, 14);
  assert.equal(softball.scenario.offers.find((offer) => offer.merchant === "Hall Softball Pickup").minimumUnits, 15);
  assert.equal(waterPolo.scenario.offers.find((offer) => offer.merchant === "Hall Water Polo Pickup").minimumUnits, 16);
  assert.equal(criteriumCycling.winner.offer.merchant, "Court-side Criterium Delivery");
  assert.equal(evaluateMarket(clonePreset("omniumCyclingCarnivalLunch")).winner.offer.merchant, "Court-side Omnium Delivery");
  assert.equal(madisonCycling.winner.offer.merchant, "Court-side Madison Delivery");
  assert.equal(keirinCycling.winner.offer.merchant, "Court-side Keirin Delivery");
  assert.equal(scratchCycling.winner.offer.merchant, "Court-side Scratch Delivery");
  assert.equal(leftoverOffer.capacity, 64);
  assert.equal(leftoverFill.fulfilledUnits, 45);
  assert.equal(leftoverOffer.capacity - leftoverFill.fulfilledUnits, 19);
  assert.notEqual(leftoverFill.fulfilledUnits, leftoverOffer.unitPrice);
  assert.notEqual(leftoverFill.fulfilledUnits, leftoverOffer.minimumUnits);
  assert.notEqual(leftoverFill.fulfilledUnits, leftoverOffer.capacity - leftoverFill.fulfilledUnits);
  assert.notEqual(leftoverOffer.capacity, leftoverFill.fulfilledUnits);
  assert.notEqual(leftoverOffer.capacity, leftoverOffer.capacity - leftoverFill.fulfilledUnits);
  assert.notEqual(leftoverOffer.unitPrice, leftoverOffer.capacity - leftoverFill.fulfilledUnits);
  assert.notEqual(leftoverOffer.unitPrice, leftoverFill.fulfilledUnits);
  assert.notEqual(leftoverFill.fulfilledUnits, 43);
  assert.notEqual(leftoverFill.fulfilledUnits, 41);
  assert.notEqual(leftoverFill.fulfilledUnits, 39);
  assert.notEqual(leftoverFill.fulfilledUnits, 38);
  assert.notEqual(leftoverFill.fulfilledUnits, 37);
  assert.notEqual(leftoverFill.fulfilledUnits, 36);
  assert.notEqual(leftoverFill.fulfilledUnits, 35);
  assert.notEqual(leftoverFill.fulfilledUnits, 34);
  assert.notEqual(leftoverFill.fulfilledUnits, 33);
  assert.notEqual(leftoverFill.fulfilledUnits, 32);
  assert.notEqual(leftoverFill.fulfilledUnits, 31);
  assert.notEqual(leftoverFill.fulfilledUnits, 30);
  assert.deepEqual(leftoverFill.selectedBuyerIds, ["B03", "B04"]);
  assert.equal(computeResidualCoverage(teamSprintCycling.scenario).tertiary, null);
  assert.deepEqual(computeResidualCoverage(teamSprintCycling.scenario).leftoverBuyerIds.filter((id) => !(leftoverFill.selectedBuyerIds ?? []).includes(id)), ["B06"]);
  assert.ok(teamSprintCycling.results.some((result) => result.offer.merchant === "Court-side Team-sprint Delivery" && result.offer.fulfillment === "shipping"));
  assert.ok(teamSprintCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Team-sprint pie")));
  assert.equal(teamSprintCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Team-pursuit pie")), false);
  assert.equal(teamSprintCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Scratch pie")), false);
  assert.equal(teamSprintCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Points-race pie")), false);
  assert.equal(teamSprintCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Omnium pie")), false);
  assert.equal(teamSprintCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Bell salad")), false);
  assert.equal(teamSprintCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Prime water")), false);
  assert.equal(teamSprintCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Tempo salad")), false);
  assert.equal(teamSprintCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Tempo water")), false);
  assert.ok(teamSprintCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Relay salad")));
  assert.ok(teamSprintCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Trio water")));
  assert.equal(teamSprintCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Changeover salad")), false);
  assert.equal(teamSprintCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Quartet water")), false);
  assert.equal(teamSprintCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Catch salad")), false);
  assert.equal(teamSprintCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Pursuit water")), false);
  assert.equal(teamSprintCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Individual-pursuit pie")), false);
  assert.equal(teamSprintCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Bunch salad")), false);
  assert.equal(teamSprintCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Chip water")), false);
  assert.equal(teamSprintCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Madison pie")), false);
  assert.equal(teamSprintCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Handsling salad")), false);
  assert.equal(teamSprintCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Handsling water")), false);
  assert.equal(teamSprintCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Keirin pie")), false);
  assert.equal(teamSprintCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Derny salad")), false);
  assert.equal(teamSprintCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Derny water")), false);
  assert.equal(teamSprintCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Hill-climb pie")), false);
  assert.equal(teamSprintCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Hairpin salad")), false);
  assert.equal(teamSprintCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Hairpin water")), false);
  assert.equal(teamSprintCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Criterium pie")), false);
  assert.equal(teamSprintCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Circuit salad")), false);
  assert.equal(teamSprintCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Circuit water")), false);
  const payload = JSON.stringify(clonePreset("teamSprintCyclingCarnivalLunch"));
  assert.equal(payload.includes("start-ramp"), false);
  assert.equal(payload.includes("timing-hut"), false);
  assert.equal(payload.includes("time-check"), false);
  assert.equal(payload.includes("pit-lane"), false);
  assert.equal(payload.includes("circuit"), false);
  assert.equal(payload.includes("sealed-road"), false);
  assert.equal(payload.includes("feed-zone"), false);
  assert.equal(payload.includes("banked-boards"), false);
  assert.equal(payload.includes("summit-marshal"), false);
  assert.equal(payload.includes("hairpin"), false);
  assert.equal(payload.includes("climb-chip"), false);
  assert.equal(payload.includes("derny-pacer"), false);
  assert.equal(payload.includes("keirin-chip"), false);
  assert.equal(payload.includes("keirin-gate"), false);
  assert.equal(payload.includes("madison-pair"), false);
  assert.equal(payload.includes("handsling"), false);
  assert.equal(payload.includes("omnium-board"), false);
  assert.equal(payload.includes("flying-200"), false);
  assert.equal(payload.includes("elimination-heat"), false);
  assert.equal(payload.includes("tempo-lap"), false);
  assert.equal(payload.includes("points-race"), false);
  assert.equal(payload.includes("points-sprint"), false);
  assert.equal(payload.includes("bell-lap"), false);
  assert.equal(payload.includes("prime-board"), false);
  assert.equal(payload.includes("scratch-bunch"), false);
  assert.equal(payload.includes("bunch-finish"), false);
  assert.equal(payload.includes("scratch-chip"), false);
  assert.equal(payload.includes("pursuit-line"), false);
  assert.equal(payload.includes("catch-bell"), false);
  assert.equal(payload.includes("pursuit-chip"), false);
  assert.equal(payload.includes("changeover-bell"), false);
  assert.equal(payload.includes("quartet-board"), false);
  assert.equal(payload.includes("team-pursuit-rota"), false);
  assert.match(payload, /Start-gate crate/);
  assert.match(payload, /Flying-relay bench/);
  assert.match(payload, /Trio-board hamper/);
  const first = evaluateMarket(clonePreset("teamSprintCyclingCarnivalLunch"));
  const second = evaluateMarket(validateScenario(clonePreset("teamSprintCyclingCarnivalLunch")));
  assert.equal(first.winner.offer.id, second.winner.offer.id);
  assert.equal(first.winner.fulfilledUnits, second.winner.fulfilledUnits);
  assert.equal(first.winner.fulfilledUnits, 50);
});

test("the example bar includes team-sprint cycling carnival lunch next to team-pursuit cycling carnival lunch", async () => {
  const html = await readFile(new URL("../index.html", import.meta.url), "utf8");
  assert.match(html, /data-preset="teamSprintCyclingCarnivalLunch"/u);
  assert.match(html, /Team-sprint cycling carnival/u);
  assert.match(html, /data-preset="scratchCyclingCarnivalLunch"/u);
  assert.match(html, /data-preset="pointsRaceCyclingCarnivalLunch"/u);
  assert.ok(html.indexOf('data-preset="pointsRaceCyclingCarnivalLunch"') < html.indexOf('data-preset="scratchCyclingCarnivalLunch"'));
  assert.match(html, /data-preset="individualPursuitCyclingCarnivalLunch"/u);
  assert.ok(html.indexOf('data-preset="scratchCyclingCarnivalLunch"') < html.indexOf('data-preset="individualPursuitCyclingCarnivalLunch"'));
  assert.ok(html.indexOf('data-preset="individualPursuitCyclingCarnivalLunch"') < html.indexOf('data-preset="teamPursuitCyclingCarnivalLunch"'));
  assert.ok(html.indexOf('data-preset="teamPursuitCyclingCarnivalLunch"') < html.indexOf('data-preset="teamSprintCyclingCarnivalLunch"'));
  assert.ok(html.indexOf('data-preset="teamSprintCyclingCarnivalLunch"') < html.indexOf('data-preset="tiers"'));
});
