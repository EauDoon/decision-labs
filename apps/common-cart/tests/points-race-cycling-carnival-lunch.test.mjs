import test from "node:test";
import assert from "node:assert/strict";
import { readFile } from "node:fs/promises";
import { clonePreset, computeResidualCoverage, evaluateMarket, validateScenario } from "../src/model.js";

test("points-race cycling carnival lunch leftover fill is hall pickup and stays distinct from omnium cycling carnival lunch", () => {
  const pointsRaceCycling = evaluateMarket(clonePreset("pointsRaceCyclingCarnivalLunch"));
  const madisonCycling = evaluateMarket(clonePreset("madisonCyclingCarnivalLunch"));
  const keirinCycling = evaluateMarket(clonePreset("keirinCyclingCarnivalLunch"));
  const hillClimbCycling = evaluateMarket(clonePreset("hillClimbCyclingCarnivalLunch"));
  const timeTrialCycling = evaluateMarket(clonePreset("timeTrialCyclingCarnivalLunch"));
  const criteriumCycling = evaluateMarket(clonePreset("criteriumCyclingCarnivalLunch"));
  const roadCycling = evaluateMarket(clonePreset("roadCyclingCarnivalLunch"));
  const gravelCycling = evaluateMarket(clonePreset("gravelCyclingCarnivalLunch"));
  const trackCycling = evaluateMarket(clonePreset("trackCyclingCarnivalLunch"));
  const cycloCross = evaluateMarket(clonePreset("cycloCrossCarnivalLunch"));
  const bmx = evaluateMarket(clonePreset("bmxCarnivalLunch"));
  const mountainBike = evaluateMarket(clonePreset("mountainBikeCarnivalLunch"));
  const cycling = evaluateMarket(clonePreset("cyclingCarnivalLunch"));
  const triathlon = evaluateMarket(clonePreset("triathlonCarnivalLunch"));
  const surf = evaluateMarket(clonePreset("surfCarnivalLunch"));
  const dragonBoat = evaluateMarket(clonePreset("dragonBoatCarnivalLunch"));
  const kayaking = evaluateMarket(clonePreset("kayakingCarnivalLunch"));
  const canoeing = evaluateMarket(clonePreset("canoeingCarnivalLunch"));
  const sailing = evaluateMarket(clonePreset("sailingCarnivalLunch"));
  const rowing = evaluateMarket(clonePreset("rowingCarnivalLunch"));
  const waterPolo = evaluateMarket(clonePreset("waterPoloCarnivalLunch"));
  const softball = evaluateMarket(clonePreset("softballCarnivalLunch"));
  const baseball = evaluateMarket(clonePreset("baseballCarnivalLunch"));
  const hockey = evaluateMarket(clonePreset("hockeyCarnivalLunch"));
  const rugby = evaluateMarket(clonePreset("rugbyCarnivalLunch"));
  const soccer = evaluateMarket(clonePreset("soccerCarnivalLunch"));
  assert.ok(pointsRaceCycling.winner);
  assert.equal(pointsRaceCycling.winner.offer.merchant, "Court-side Points-race Delivery");
  assert.equal(pointsRaceCycling.winner.offer.fulfillment, "shipping");
  assert.equal(pointsRaceCycling.winner.fulfilledUnits, 50);
  assert.deepEqual(pointsRaceCycling.winner.selectedBuyerIds, ["B01", "B02", "B05"]);
  assert.equal(pointsRaceCycling.scenario.title, "Points-race cycling carnival lunch");
  assert.notEqual(pointsRaceCycling.scenario.title, clonePreset("omniumCyclingCarnivalLunch").title);
  assert.notEqual(pointsRaceCycling.scenario.title, clonePreset("madisonCyclingCarnivalLunch").title);
  assert.notEqual(pointsRaceCycling.scenario.title, keirinCycling.scenario.title);
  assert.notEqual(pointsRaceCycling.scenario.title, hillClimbCycling.scenario.title);
  assert.notEqual(pointsRaceCycling.scenario.title, timeTrialCycling.scenario.title);
  assert.notEqual(pointsRaceCycling.scenario.title, criteriumCycling.scenario.title);
  assert.notEqual(pointsRaceCycling.scenario.title, roadCycling.scenario.title);
  assert.notEqual(pointsRaceCycling.scenario.title, gravelCycling.scenario.title);
  assert.notEqual(pointsRaceCycling.scenario.title, trackCycling.scenario.title);
  assert.notEqual(pointsRaceCycling.scenario.title, cycloCross.scenario.title);
  assert.notEqual(pointsRaceCycling.scenario.title, bmx.scenario.title);
  assert.notEqual(pointsRaceCycling.scenario.title, mountainBike.scenario.title);
  assert.notEqual(pointsRaceCycling.scenario.title, cycling.scenario.title);
  assert.notEqual(pointsRaceCycling.scenario.title, triathlon.scenario.title);
  assert.notEqual(pointsRaceCycling.scenario.title, surf.scenario.title);
  assert.notEqual(pointsRaceCycling.scenario.title, dragonBoat.scenario.title);
  assert.notEqual(pointsRaceCycling.scenario.title, kayaking.scenario.title);
  assert.notEqual(pointsRaceCycling.scenario.title, canoeing.scenario.title);
  assert.notEqual(pointsRaceCycling.scenario.title, sailing.scenario.title);
  assert.notDeepEqual(clonePreset("pointsRaceCyclingCarnivalLunch"), clonePreset("madisonCyclingCarnivalLunch"));
  assert.notDeepEqual(clonePreset("pointsRaceCyclingCarnivalLunch"), clonePreset("keirinCyclingCarnivalLunch"));
  assert.notDeepEqual(clonePreset("pointsRaceCyclingCarnivalLunch"), clonePreset("hillClimbCyclingCarnivalLunch"));
  assert.notDeepEqual(clonePreset("pointsRaceCyclingCarnivalLunch"), clonePreset("timeTrialCyclingCarnivalLunch"));
  assert.notDeepEqual(clonePreset("pointsRaceCyclingCarnivalLunch"), clonePreset("criteriumCyclingCarnivalLunch"));
  assert.notDeepEqual(clonePreset("pointsRaceCyclingCarnivalLunch"), clonePreset("roadCyclingCarnivalLunch"));
  assert.notDeepEqual(clonePreset("pointsRaceCyclingCarnivalLunch"), clonePreset("gravelCyclingCarnivalLunch"));
  assert.notDeepEqual(clonePreset("pointsRaceCyclingCarnivalLunch"), clonePreset("trackCyclingCarnivalLunch"));
  assert.notDeepEqual(clonePreset("pointsRaceCyclingCarnivalLunch"), clonePreset("cycloCrossCarnivalLunch"));
  assert.notDeepEqual(clonePreset("pointsRaceCyclingCarnivalLunch"), clonePreset("bmxCarnivalLunch"));
  assert.notDeepEqual(clonePreset("pointsRaceCyclingCarnivalLunch"), clonePreset("mountainBikeCarnivalLunch"));
  assert.notDeepEqual(clonePreset("pointsRaceCyclingCarnivalLunch"), clonePreset("cyclingCarnivalLunch"));
  assert.notDeepEqual(clonePreset("pointsRaceCyclingCarnivalLunch"), clonePreset("triathlonCarnivalLunch"));
  assert.notDeepEqual(clonePreset("pointsRaceCyclingCarnivalLunch"), clonePreset("surfCarnivalLunch"));
  assert.notDeepEqual(clonePreset("pointsRaceCyclingCarnivalLunch"), clonePreset("dragonBoatCarnivalLunch"));
  assert.notDeepEqual(clonePreset("pointsRaceCyclingCarnivalLunch"), clonePreset("kayakingCarnivalLunch"));
  assert.notDeepEqual(clonePreset("pointsRaceCyclingCarnivalLunch"), clonePreset("canoeingCarnivalLunch"));
  assert.notDeepEqual(clonePreset("pointsRaceCyclingCarnivalLunch"), clonePreset("sailingCarnivalLunch"));
  assert.notDeepEqual(clonePreset("pointsRaceCyclingCarnivalLunch"), clonePreset("rowingCarnivalLunch"));
  assert.notDeepEqual(clonePreset("pointsRaceCyclingCarnivalLunch"), clonePreset("waterPoloCarnivalLunch"));
  assert.notDeepEqual(clonePreset("pointsRaceCyclingCarnivalLunch"), clonePreset("softballCarnivalLunch"));
  assert.notDeepEqual(clonePreset("pointsRaceCyclingCarnivalLunch"), clonePreset("baseballCarnivalLunch"));
  assert.notDeepEqual(clonePreset("pointsRaceCyclingCarnivalLunch"), clonePreset("hockeyCarnivalLunch"));
  assert.notDeepEqual(clonePreset("pointsRaceCyclingCarnivalLunch"), clonePreset("rugbyCarnivalLunch"));
  assert.notDeepEqual(clonePreset("pointsRaceCyclingCarnivalLunch"), clonePreset("soccerCarnivalLunch"));
  assert.notDeepEqual(clonePreset("pointsRaceCyclingCarnivalLunch"), clonePreset("omniumCyclingCarnivalLunch"));
  assert.notDeepEqual(clonePreset("pointsRaceCyclingCarnivalLunch"), clonePreset("madisonCyclingCarnivalLunch"));
  const leftoverFill = computeResidualCoverage(pointsRaceCycling.scenario).secondary;
  assert.ok(leftoverFill);
  assert.equal(leftoverFill.merchant, "Hall Points-race Pickup");
  const leftoverOffer = pointsRaceCycling.scenario.offers.find((offer) => offer.id === leftoverFill.offerId);
  assert.equal(leftoverOffer.fulfillment, "pickup");
  assert.equal(leftoverOffer.minimumUnits, 37);
  assert.notEqual(leftoverOffer.unitPrice, leftoverOffer.minimumUnits);
  assert.equal(leftoverOffer.unitPrice, 33);
  assert.equal(pointsRaceCycling.scenario.buyers.find((buyer) => buyer.id === "B03").quantity, 15);
  assert.equal(pointsRaceCycling.scenario.buyers.find((buyer) => buyer.id === "B03").maxUnitPrice, 33);
  assert.equal(pointsRaceCycling.scenario.buyers.find((buyer) => buyer.id === "B04").maxUnitPrice, 33);
  assert.equal(pointsRaceCycling.scenario.buyers.find((buyer) => buyer.id === "B04").quantity, 23);
  assert.equal(evaluateMarket(clonePreset("omniumCyclingCarnivalLunch")).scenario.offers.find((offer) => offer.merchant === "Hall Omnium Pickup").minimumUnits, 36);
  assert.equal(madisonCycling.scenario.offers.find((offer) => offer.merchant === "Hall Madison Pickup").minimumUnits, 35);
  assert.equal(keirinCycling.scenario.offers.find((offer) => offer.merchant === "Hall Keirin Pickup").minimumUnits, 34);
  assert.equal(hillClimbCycling.scenario.offers.find((offer) => offer.merchant === "Hall Hill Climb Pickup").minimumUnits, 33);
  assert.equal(timeTrialCycling.scenario.offers.find((offer) => offer.merchant === "Hall Time Trial Pickup").minimumUnits, 32);
  assert.equal(criteriumCycling.scenario.offers.find((offer) => offer.merchant === "Hall Criterium Pickup").minimumUnits, 31);
  assert.equal(roadCycling.scenario.offers.find((offer) => offer.merchant === "Hall Road Cycling Pickup").minimumUnits, 30);
  assert.equal(gravelCycling.scenario.offers.find((offer) => offer.merchant === "Hall Gravel Cycling Pickup").minimumUnits, 29);
  assert.equal(trackCycling.scenario.offers.find((offer) => offer.merchant === "Hall Track Cycling Pickup").minimumUnits, 28);
  assert.equal(cycloCross.scenario.offers.find((offer) => offer.merchant === "Hall Cyclo-cross Pickup").minimumUnits, 27);
  assert.equal(bmx.scenario.offers.find((offer) => offer.merchant === "Hall BMX Pickup").minimumUnits, 26);
  assert.equal(mountainBike.scenario.offers.find((offer) => offer.merchant === "Hall Mountain Bike Pickup").minimumUnits, 25);
  assert.equal(cycling.scenario.offers.find((offer) => offer.merchant === "Hall Cycling Pickup").minimumUnits, 24);
  assert.equal(triathlon.scenario.offers.find((offer) => offer.merchant === "Hall Triathlon Pickup").minimumUnits, 23);
  assert.equal(surf.scenario.offers.find((offer) => offer.merchant === "Hall Surf Pickup").minimumUnits, 22);
  assert.equal(dragonBoat.scenario.offers.find((offer) => offer.merchant === "Hall Dragon Boat Pickup").minimumUnits, 21);
  assert.equal(kayaking.scenario.offers.find((offer) => offer.merchant === "Hall Kayaking Pickup").minimumUnits, 20);
  assert.equal(canoeing.scenario.offers.find((offer) => offer.merchant === "Hall Canoeing Pickup").minimumUnits, 19);
  assert.equal(sailing.scenario.offers.find((offer) => offer.merchant === "Hall Sailing Pickup").minimumUnits, 18);
  assert.equal(rowing.scenario.offers.find((offer) => offer.merchant === "Hall Rowing Pickup").minimumUnits, 17);
  assert.equal(soccer.scenario.offers.find((offer) => offer.merchant === "Hall Soccer Pickup").minimumUnits, 11);
  assert.equal(rugby.scenario.offers.find((offer) => offer.merchant === "Hall Rugby Pickup").minimumUnits, 12);
  assert.equal(hockey.scenario.offers.find((offer) => offer.merchant === "Hall Hockey Pickup").minimumUnits, 13);
  assert.equal(baseball.scenario.offers.find((offer) => offer.merchant === "Hall Baseball Pickup").minimumUnits, 14);
  assert.equal(softball.scenario.offers.find((offer) => offer.merchant === "Hall Softball Pickup").minimumUnits, 15);
  assert.equal(waterPolo.scenario.offers.find((offer) => offer.merchant === "Hall Water Polo Pickup").minimumUnits, 16);
  assert.equal(criteriumCycling.winner.offer.merchant, "Court-side Criterium Delivery");
  assert.equal(evaluateMarket(clonePreset("omniumCyclingCarnivalLunch")).winner.offer.merchant, "Court-side Omnium Delivery");
  assert.equal(madisonCycling.winner.offer.merchant, "Court-side Madison Delivery");
  assert.equal(keirinCycling.winner.offer.merchant, "Court-side Keirin Delivery");
  assert.equal(leftoverOffer.capacity, 64);
  assert.equal(leftoverFill.fulfilledUnits, 38);
  assert.equal(leftoverOffer.capacity - leftoverFill.fulfilledUnits, 26);
  assert.notEqual(leftoverFill.fulfilledUnits, leftoverOffer.unitPrice);
  assert.notEqual(leftoverFill.fulfilledUnits, leftoverOffer.minimumUnits);
  assert.notEqual(leftoverFill.fulfilledUnits, leftoverOffer.capacity - leftoverFill.fulfilledUnits);
  assert.notEqual(leftoverOffer.capacity, leftoverFill.fulfilledUnits);
  assert.notEqual(leftoverOffer.capacity, leftoverOffer.capacity - leftoverFill.fulfilledUnits);
  assert.notEqual(leftoverOffer.unitPrice, leftoverOffer.capacity - leftoverFill.fulfilledUnits);
  assert.notEqual(leftoverOffer.unitPrice, leftoverFill.fulfilledUnits);
  assert.notEqual(leftoverFill.fulfilledUnits, 37);
  assert.notEqual(leftoverFill.fulfilledUnits, 36);
  assert.notEqual(leftoverFill.fulfilledUnits, 35);
  assert.notEqual(leftoverFill.fulfilledUnits, 34);
  assert.notEqual(leftoverFill.fulfilledUnits, 33);
  assert.notEqual(leftoverFill.fulfilledUnits, 32);
  assert.notEqual(leftoverFill.fulfilledUnits, 31);
  assert.notEqual(leftoverFill.fulfilledUnits, 30);
  assert.deepEqual(leftoverFill.selectedBuyerIds, ["B03", "B04"]);
  assert.equal(computeResidualCoverage(pointsRaceCycling.scenario).tertiary, null);
  assert.deepEqual(computeResidualCoverage(pointsRaceCycling.scenario).leftoverBuyerIds.filter((id) => !(leftoverFill.selectedBuyerIds ?? []).includes(id)), ["B06"]);
  assert.ok(pointsRaceCycling.results.some((result) => result.offer.merchant === "Court-side Points-race Delivery" && result.offer.fulfillment === "shipping"));
  assert.ok(pointsRaceCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Points-race pie")));
  assert.equal(pointsRaceCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Omnium pie")), false);
  assert.equal(pointsRaceCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Tempo salad")), false);
  assert.equal(pointsRaceCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Tempo water")), false);
  assert.ok(pointsRaceCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Bell salad")));
  assert.ok(pointsRaceCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Prime water")));
  assert.equal(pointsRaceCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Madison pie")), false);
  assert.equal(pointsRaceCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Handsling salad")), false);
  assert.equal(pointsRaceCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Handsling water")), false);
  assert.equal(pointsRaceCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Keirin pie")), false);
  assert.equal(pointsRaceCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Derny salad")), false);
  assert.equal(pointsRaceCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Derny water")), false);
  assert.equal(pointsRaceCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Hill-climb pie")), false);
  assert.equal(pointsRaceCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Hairpin salad")), false);
  assert.equal(pointsRaceCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Hairpin water")), false);
  assert.equal(pointsRaceCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Criterium pie")), false);
  assert.equal(pointsRaceCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Circuit salad")), false);
  assert.equal(pointsRaceCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Circuit water")), false);
  const payload = JSON.stringify(clonePreset("pointsRaceCyclingCarnivalLunch"));
  assert.equal(payload.includes("start-ramp"), false);
  assert.equal(payload.includes("timing-hut"), false);
  assert.equal(payload.includes("time-check"), false);
  assert.equal(payload.includes("pit-lane"), false);
  assert.equal(payload.includes("circuit"), false);
  assert.equal(payload.includes("sealed-road"), false);
  assert.equal(payload.includes("feed-zone"), false);
  assert.equal(payload.includes("banked-boards"), false);
  assert.equal(payload.includes("summit-marshal"), false);
  assert.equal(payload.includes("hairpin"), false);
  assert.equal(payload.includes("climb-chip"), false);
  assert.equal(payload.includes("derny-pacer"), false);
  assert.equal(payload.includes("keirin-chip"), false);
  assert.equal(payload.includes("keirin-gate"), false);
  assert.equal(payload.includes("madison-pair"), false);
  assert.equal(payload.includes("handsling"), false);
  assert.equal(payload.includes("omnium-board"), false);
  assert.equal(payload.includes("flying-200"), false);
  assert.equal(payload.includes("elimination-heat"), false);
  assert.equal(payload.includes("tempo-lap"), false);
  assert.match(payload, /Points-sprint crate/);
  assert.match(payload, /Bell-lap bench/);
  assert.match(payload, /Prime-board hamper/);
  const first = evaluateMarket(clonePreset("pointsRaceCyclingCarnivalLunch"));
  const second = evaluateMarket(validateScenario(clonePreset("pointsRaceCyclingCarnivalLunch")));
  assert.equal(first.winner.offer.id, second.winner.offer.id);
  assert.equal(first.winner.fulfilledUnits, second.winner.fulfilledUnits);
  assert.equal(first.winner.fulfilledUnits, 50);
});

test("the example bar includes points-race cycling carnival lunch next to omnium cycling carnival lunch", async () => {
  const html = await readFile(new URL("../index.html", import.meta.url), "utf8");
  assert.match(html, /data-preset="pointsRaceCyclingCarnivalLunch"/u);
  assert.match(html, /Points-race cycling carnival/u);
  assert.match(html, /data-preset="omniumCyclingCarnivalLunch"/u);
  assert.match(html, /data-preset="madisonCyclingCarnivalLunch"/u);
  assert.ok(html.indexOf('data-preset="madisonCyclingCarnivalLunch"') < html.indexOf('data-preset="omniumCyclingCarnivalLunch"'));
  assert.ok(html.indexOf('data-preset="omniumCyclingCarnivalLunch"') < html.indexOf('data-preset="pointsRaceCyclingCarnivalLunch"'));
  assert.ok(html.indexOf('data-preset="pointsRaceCyclingCarnivalLunch"') < html.indexOf('data-preset="tiers"'));
});
