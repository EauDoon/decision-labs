import test from "node:test";
import assert from "node:assert/strict";
import { readFile } from "node:fs/promises";
import { clonePreset, computeResidualCoverage, evaluateMarket, validateScenario } from "../src/model.js";

test("bottle-hand cycling carnival lunch leftover fill is hall pickup and stays distinct from feed-zone cycling carnival lunch", () => {
  const bottleHandCycling = evaluateMarket(clonePreset("bottleHandCyclingCarnivalLunch"));
  const firstAidCycling = evaluateMarket(clonePreset("firstAidCyclingCarnivalLunch"));
  const teamSprintCycling = evaluateMarket(clonePreset("teamSprintCyclingCarnivalLunch"));
  const pursuitCycling = evaluateMarket(clonePreset("individualPursuitCyclingCarnivalLunch"));
  const scratchCycling = evaluateMarket(clonePreset("scratchCyclingCarnivalLunch"));
  const pointsRaceCycling = evaluateMarket(clonePreset("pointsRaceCyclingCarnivalLunch"));
  const madisonCycling = evaluateMarket(clonePreset("madisonCyclingCarnivalLunch"));
  const keirinCycling = evaluateMarket(clonePreset("keirinCyclingCarnivalLunch"));
  const hillClimbCycling = evaluateMarket(clonePreset("hillClimbCyclingCarnivalLunch"));
  const timeTrialCycling = evaluateMarket(clonePreset("timeTrialCyclingCarnivalLunch"));
  const criteriumCycling = evaluateMarket(clonePreset("criteriumCyclingCarnivalLunch"));
  const roadCycling = evaluateMarket(clonePreset("roadCyclingCarnivalLunch"));
  const gravelCycling = evaluateMarket(clonePreset("gravelCyclingCarnivalLunch"));
  const trackCycling = evaluateMarket(clonePreset("trackCyclingCarnivalLunch"));
  const cycloCross = evaluateMarket(clonePreset("cycloCrossCarnivalLunch"));
  const bmx = evaluateMarket(clonePreset("bmxCarnivalLunch"));
  const mountainBike = evaluateMarket(clonePreset("mountainBikeCarnivalLunch"));
  const cycling = evaluateMarket(clonePreset("cyclingCarnivalLunch"));
  const triathlon = evaluateMarket(clonePreset("triathlonCarnivalLunch"));
  const surf = evaluateMarket(clonePreset("surfCarnivalLunch"));
  const dragonBoat = evaluateMarket(clonePreset("dragonBoatCarnivalLunch"));
  const kayaking = evaluateMarket(clonePreset("kayakingCarnivalLunch"));
  const canoeing = evaluateMarket(clonePreset("canoeingCarnivalLunch"));
  const sailing = evaluateMarket(clonePreset("sailingCarnivalLunch"));
  const rowing = evaluateMarket(clonePreset("rowingCarnivalLunch"));
  const waterPolo = evaluateMarket(clonePreset("waterPoloCarnivalLunch"));
  const softball = evaluateMarket(clonePreset("softballCarnivalLunch"));
  const baseball = evaluateMarket(clonePreset("baseballCarnivalLunch"));
  const hockey = evaluateMarket(clonePreset("hockeyCarnivalLunch"));
  const rugby = evaluateMarket(clonePreset("rugbyCarnivalLunch"));
  const soccer = evaluateMarket(clonePreset("soccerCarnivalLunch"));
  assert.ok(bottleHandCycling.winner);
  assert.equal(bottleHandCycling.winner.offer.merchant, "Court-side Bottle-hand Delivery");
  assert.equal(bottleHandCycling.winner.offer.fulfillment, "shipping");
  assert.equal(bottleHandCycling.winner.fulfilledUnits, 54);
  assert.deepEqual(bottleHandCycling.winner.selectedBuyerIds, ["B01", "B02", "B05"]);
  assert.equal(bottleHandCycling.scenario.title, "Bottle-hand cycling carnival lunch");
  assert.notEqual(bottleHandCycling.scenario.title, clonePreset("domestiqueCyclingCarnivalLunch").title);
  assert.notEqual(bottleHandCycling.scenario.title, clonePreset("soigneurCyclingCarnivalLunch").title);
  assert.notEqual(bottleHandCycling.scenario.title, clonePreset("leadOutCyclingCarnivalLunch").title);
  assert.notEqual(bottleHandCycling.scenario.title, clonePreset("firstAidCyclingCarnivalLunch").title);
  assert.notEqual(bottleHandCycling.scenario.title, clonePreset("feedZoneCyclingCarnivalLunch").title);
  assert.notEqual(bottleHandCycling.scenario.title, clonePreset("teamSprintCyclingCarnivalLunch").title);
  assert.notEqual(bottleHandCycling.scenario.title, clonePreset("teamPursuitCyclingCarnivalLunch").title);
  assert.notEqual(bottleHandCycling.scenario.title, clonePreset("individualPursuitCyclingCarnivalLunch").title);
  assert.notEqual(bottleHandCycling.scenario.title, clonePreset("scratchCyclingCarnivalLunch").title);
  assert.notEqual(bottleHandCycling.scenario.title, clonePreset("pointsRaceCyclingCarnivalLunch").title);
  assert.notEqual(bottleHandCycling.scenario.title, clonePreset("omniumCyclingCarnivalLunch").title);
  assert.notEqual(bottleHandCycling.scenario.title, clonePreset("madisonCyclingCarnivalLunch").title);
  assert.notEqual(bottleHandCycling.scenario.title, keirinCycling.scenario.title);
  assert.notEqual(bottleHandCycling.scenario.title, hillClimbCycling.scenario.title);
  assert.notEqual(bottleHandCycling.scenario.title, timeTrialCycling.scenario.title);
  assert.notEqual(bottleHandCycling.scenario.title, criteriumCycling.scenario.title);
  assert.notEqual(bottleHandCycling.scenario.title, roadCycling.scenario.title);
  assert.notEqual(bottleHandCycling.scenario.title, gravelCycling.scenario.title);
  assert.notEqual(bottleHandCycling.scenario.title, trackCycling.scenario.title);
  assert.notEqual(bottleHandCycling.scenario.title, cycloCross.scenario.title);
  assert.notEqual(bottleHandCycling.scenario.title, bmx.scenario.title);
  assert.notEqual(bottleHandCycling.scenario.title, mountainBike.scenario.title);
  assert.notEqual(bottleHandCycling.scenario.title, cycling.scenario.title);
  assert.notEqual(bottleHandCycling.scenario.title, triathlon.scenario.title);
  assert.notEqual(bottleHandCycling.scenario.title, surf.scenario.title);
  assert.notEqual(bottleHandCycling.scenario.title, dragonBoat.scenario.title);
  assert.notEqual(bottleHandCycling.scenario.title, kayaking.scenario.title);
  assert.notEqual(bottleHandCycling.scenario.title, canoeing.scenario.title);
  assert.notEqual(bottleHandCycling.scenario.title, sailing.scenario.title);
  assert.notDeepEqual(clonePreset("bottleHandCyclingCarnivalLunch"), clonePreset("leadOutCyclingCarnivalLunch"));
  assert.notDeepEqual(clonePreset("bottleHandCyclingCarnivalLunch"), clonePreset("domestiqueCyclingCarnivalLunch"));
  assert.notDeepEqual(clonePreset("bottleHandCyclingCarnivalLunch"), clonePreset("soigneurCyclingCarnivalLunch"));
  assert.notDeepEqual(clonePreset("bottleHandCyclingCarnivalLunch"), clonePreset("firstAidCyclingCarnivalLunch"));
  assert.notDeepEqual(clonePreset("bottleHandCyclingCarnivalLunch"), clonePreset("feedZoneCyclingCarnivalLunch"));
  assert.notDeepEqual(clonePreset("bottleHandCyclingCarnivalLunch"), clonePreset("teamSprintCyclingCarnivalLunch"));
  assert.notDeepEqual(clonePreset("bottleHandCyclingCarnivalLunch"), clonePreset("teamPursuitCyclingCarnivalLunch"));
  assert.notDeepEqual(clonePreset("bottleHandCyclingCarnivalLunch"), clonePreset("individualPursuitCyclingCarnivalLunch"));
  assert.notDeepEqual(clonePreset("bottleHandCyclingCarnivalLunch"), clonePreset("scratchCyclingCarnivalLunch"));
  assert.notDeepEqual(clonePreset("bottleHandCyclingCarnivalLunch"), clonePreset("madisonCyclingCarnivalLunch"));
  assert.notDeepEqual(clonePreset("bottleHandCyclingCarnivalLunch"), clonePreset("keirinCyclingCarnivalLunch"));
  assert.notDeepEqual(clonePreset("bottleHandCyclingCarnivalLunch"), clonePreset("hillClimbCyclingCarnivalLunch"));
  assert.notDeepEqual(clonePreset("bottleHandCyclingCarnivalLunch"), clonePreset("timeTrialCyclingCarnivalLunch"));
  assert.notDeepEqual(clonePreset("bottleHandCyclingCarnivalLunch"), clonePreset("criteriumCyclingCarnivalLunch"));
  assert.notDeepEqual(clonePreset("bottleHandCyclingCarnivalLunch"), clonePreset("roadCyclingCarnivalLunch"));
  assert.notDeepEqual(clonePreset("bottleHandCyclingCarnivalLunch"), clonePreset("gravelCyclingCarnivalLunch"));
  assert.notDeepEqual(clonePreset("bottleHandCyclingCarnivalLunch"), clonePreset("trackCyclingCarnivalLunch"));
  assert.notDeepEqual(clonePreset("bottleHandCyclingCarnivalLunch"), clonePreset("cycloCrossCarnivalLunch"));
  assert.notDeepEqual(clonePreset("bottleHandCyclingCarnivalLunch"), clonePreset("bmxCarnivalLunch"));
  assert.notDeepEqual(clonePreset("bottleHandCyclingCarnivalLunch"), clonePreset("mountainBikeCarnivalLunch"));
  assert.notDeepEqual(clonePreset("bottleHandCyclingCarnivalLunch"), clonePreset("cyclingCarnivalLunch"));
  assert.notDeepEqual(clonePreset("bottleHandCyclingCarnivalLunch"), clonePreset("triathlonCarnivalLunch"));
  assert.notDeepEqual(clonePreset("bottleHandCyclingCarnivalLunch"), clonePreset("surfCarnivalLunch"));
  assert.notDeepEqual(clonePreset("bottleHandCyclingCarnivalLunch"), clonePreset("dragonBoatCarnivalLunch"));
  assert.notDeepEqual(clonePreset("bottleHandCyclingCarnivalLunch"), clonePreset("kayakingCarnivalLunch"));
  assert.notDeepEqual(clonePreset("bottleHandCyclingCarnivalLunch"), clonePreset("canoeingCarnivalLunch"));
  assert.notDeepEqual(clonePreset("bottleHandCyclingCarnivalLunch"), clonePreset("sailingCarnivalLunch"));
  assert.notDeepEqual(clonePreset("bottleHandCyclingCarnivalLunch"), clonePreset("rowingCarnivalLunch"));
  assert.notDeepEqual(clonePreset("bottleHandCyclingCarnivalLunch"), clonePreset("waterPoloCarnivalLunch"));
  assert.notDeepEqual(clonePreset("bottleHandCyclingCarnivalLunch"), clonePreset("softballCarnivalLunch"));
  assert.notDeepEqual(clonePreset("bottleHandCyclingCarnivalLunch"), clonePreset("baseballCarnivalLunch"));
  assert.notDeepEqual(clonePreset("bottleHandCyclingCarnivalLunch"), clonePreset("hockeyCarnivalLunch"));
  assert.notDeepEqual(clonePreset("bottleHandCyclingCarnivalLunch"), clonePreset("rugbyCarnivalLunch"));
  assert.notDeepEqual(clonePreset("bottleHandCyclingCarnivalLunch"), clonePreset("soccerCarnivalLunch"));
  assert.notDeepEqual(clonePreset("bottleHandCyclingCarnivalLunch"), clonePreset("pointsRaceCyclingCarnivalLunch"));
  assert.notDeepEqual(clonePreset("bottleHandCyclingCarnivalLunch"), clonePreset("omniumCyclingCarnivalLunch"));
  const leftoverFill = computeResidualCoverage(bottleHandCycling.scenario).secondary;
  assert.ok(leftoverFill);
  assert.equal(leftoverFill.merchant, "Hall Bottle-hand Pickup");
  const leftoverOffer = bottleHandCycling.scenario.offers.find((offer) => offer.id === leftoverFill.offerId);
  assert.equal(leftoverOffer.fulfillment, "pickup");
  assert.equal(leftoverOffer.minimumUnits, 53);
  assert.notEqual(leftoverOffer.unitPrice, leftoverOffer.minimumUnits);
  assert.equal(leftoverOffer.unitPrice, 48);
  assert.equal(bottleHandCycling.scenario.buyers.find((buyer) => buyer.id === "B03").quantity, 15);
  assert.equal(bottleHandCycling.scenario.buyers.find((buyer) => buyer.id === "B03").maxUnitPrice, 48);
  assert.equal(bottleHandCycling.scenario.buyers.find((buyer) => buyer.id === "B04").maxUnitPrice, 48);
  assert.equal(bottleHandCycling.scenario.buyers.find((buyer) => buyer.id === "B04").quantity, 39);
  assert.equal(evaluateMarket(clonePreset("scratchCyclingCarnivalLunch")).scenario.offers.find((offer) => offer.merchant === "Hall Scratch Pickup").minimumUnits, 38);
  assert.equal(evaluateMarket(clonePreset("pointsRaceCyclingCarnivalLunch")).scenario.offers.find((offer) => offer.merchant === "Hall Points-race Pickup").minimumUnits, 37);
  assert.equal(evaluateMarket(clonePreset("omniumCyclingCarnivalLunch")).scenario.offers.find((offer) => offer.merchant === "Hall Omnium Pickup").minimumUnits, 36);
  assert.equal(madisonCycling.scenario.offers.find((offer) => offer.merchant === "Hall Madison Pickup").minimumUnits, 35);
  assert.equal(keirinCycling.scenario.offers.find((offer) => offer.merchant === "Hall Keirin Pickup").minimumUnits, 34);
  assert.equal(hillClimbCycling.scenario.offers.find((offer) => offer.merchant === "Hall Hill Climb Pickup").minimumUnits, 33);
  assert.equal(timeTrialCycling.scenario.offers.find((offer) => offer.merchant === "Hall Time Trial Pickup").minimumUnits, 32);
  assert.equal(criteriumCycling.scenario.offers.find((offer) => offer.merchant === "Hall Criterium Pickup").minimumUnits, 31);
  assert.equal(roadCycling.scenario.offers.find((offer) => offer.merchant === "Hall Road Cycling Pickup").minimumUnits, 30);
  assert.equal(gravelCycling.scenario.offers.find((offer) => offer.merchant === "Hall Gravel Cycling Pickup").minimumUnits, 29);
  assert.equal(trackCycling.scenario.offers.find((offer) => offer.merchant === "Hall Track Cycling Pickup").minimumUnits, 28);
  assert.equal(cycloCross.scenario.offers.find((offer) => offer.merchant === "Hall Cyclo-cross Pickup").minimumUnits, 27);
  assert.equal(bmx.scenario.offers.find((offer) => offer.merchant === "Hall BMX Pickup").minimumUnits, 26);
  assert.equal(mountainBike.scenario.offers.find((offer) => offer.merchant === "Hall Mountain Bike Pickup").minimumUnits, 25);
  assert.equal(cycling.scenario.offers.find((offer) => offer.merchant === "Hall Cycling Pickup").minimumUnits, 24);
  assert.equal(triathlon.scenario.offers.find((offer) => offer.merchant === "Hall Triathlon Pickup").minimumUnits, 23);
  assert.equal(surf.scenario.offers.find((offer) => offer.merchant === "Hall Surf Pickup").minimumUnits, 22);
  assert.equal(dragonBoat.scenario.offers.find((offer) => offer.merchant === "Hall Dragon Boat Pickup").minimumUnits, 21);
  assert.equal(kayaking.scenario.offers.find((offer) => offer.merchant === "Hall Kayaking Pickup").minimumUnits, 20);
  assert.equal(canoeing.scenario.offers.find((offer) => offer.merchant === "Hall Canoeing Pickup").minimumUnits, 19);
  assert.equal(sailing.scenario.offers.find((offer) => offer.merchant === "Hall Sailing Pickup").minimumUnits, 18);
  assert.equal(rowing.scenario.offers.find((offer) => offer.merchant === "Hall Rowing Pickup").minimumUnits, 17);
  assert.equal(soccer.scenario.offers.find((offer) => offer.merchant === "Hall Soccer Pickup").minimumUnits, 11);
  assert.equal(rugby.scenario.offers.find((offer) => offer.merchant === "Hall Rugby Pickup").minimumUnits, 12);
  assert.equal(hockey.scenario.offers.find((offer) => offer.merchant === "Hall Hockey Pickup").minimumUnits, 13);
  assert.equal(baseball.scenario.offers.find((offer) => offer.merchant === "Hall Baseball Pickup").minimumUnits, 14);
  assert.equal(softball.scenario.offers.find((offer) => offer.merchant === "Hall Softball Pickup").minimumUnits, 15);
  assert.equal(waterPolo.scenario.offers.find((offer) => offer.merchant === "Hall Water Polo Pickup").minimumUnits, 16);
  assert.equal(criteriumCycling.winner.offer.merchant, "Court-side Criterium Delivery");
  assert.equal(evaluateMarket(clonePreset("omniumCyclingCarnivalLunch")).winner.offer.merchant, "Court-side Omnium Delivery");
  assert.equal(madisonCycling.winner.offer.merchant, "Court-side Madison Delivery");
  assert.equal(keirinCycling.winner.offer.merchant, "Court-side Keirin Delivery");
  assert.equal(scratchCycling.winner.offer.merchant, "Court-side Scratch Delivery");
  assert.equal(leftoverOffer.capacity, 64);
  assert.equal(leftoverFill.fulfilledUnits, 54);
  assert.equal(computeResidualCoverage(firstAidCycling.scenario).secondary.fulfilledUnits, 47);
  assert.equal(computeResidualCoverage(evaluateMarket(clonePreset("soigneurCyclingCarnivalLunch")).scenario).secondary.fulfilledUnits, 52);
  assert.equal(computeResidualCoverage(evaluateMarket(clonePreset("leadOutCyclingCarnivalLunch")).scenario).secondary.fulfilledUnits, 51);
  assert.equal(computeResidualCoverage(evaluateMarket(clonePreset("feedZoneCyclingCarnivalLunch")).scenario).secondary.fulfilledUnits, 49);
  assert.equal(computeResidualCoverage(teamSprintCycling.scenario).secondary.fulfilledUnits, 45);
  assert.equal(leftoverOffer.capacity - leftoverFill.fulfilledUnits, 10);
  assert.notEqual(leftoverFill.fulfilledUnits, leftoverOffer.unitPrice);
  assert.notEqual(leftoverFill.fulfilledUnits, leftoverOffer.minimumUnits);
  assert.notEqual(leftoverFill.fulfilledUnits, leftoverOffer.capacity - leftoverFill.fulfilledUnits);
  assert.notEqual(leftoverOffer.capacity, leftoverFill.fulfilledUnits);
  assert.notEqual(leftoverOffer.capacity, leftoverOffer.capacity - leftoverFill.fulfilledUnits);
  assert.notEqual(leftoverOffer.unitPrice, leftoverOffer.capacity - leftoverFill.fulfilledUnits);
  assert.notEqual(leftoverOffer.unitPrice, leftoverFill.fulfilledUnits);
  assert.notEqual(leftoverFill.fulfilledUnits, 53);
  assert.notEqual(leftoverFill.fulfilledUnits, 52);
  assert.notEqual(leftoverFill.fulfilledUnits, 51);
  assert.notEqual(leftoverFill.fulfilledUnits, 49);
  assert.notEqual(leftoverFill.fulfilledUnits, 47);
  assert.notEqual(leftoverFill.fulfilledUnits, 45);
  assert.notEqual(leftoverFill.fulfilledUnits, 43);
  assert.notEqual(leftoverFill.fulfilledUnits, 41);
  assert.notEqual(leftoverFill.fulfilledUnits, 39);
  assert.notEqual(leftoverFill.fulfilledUnits, 38);
  assert.notEqual(leftoverFill.fulfilledUnits, 37);
  assert.notEqual(leftoverFill.fulfilledUnits, 36);
  assert.notEqual(leftoverFill.fulfilledUnits, 35);
  assert.notEqual(leftoverFill.fulfilledUnits, 34);
  assert.notEqual(leftoverFill.fulfilledUnits, 33);
  assert.notEqual(leftoverFill.fulfilledUnits, 32);
  assert.notEqual(leftoverFill.fulfilledUnits, 31);
  assert.notEqual(leftoverFill.fulfilledUnits, 30);
  assert.deepEqual(leftoverFill.selectedBuyerIds, ["B03", "B04"]);
  assert.equal(computeResidualCoverage(bottleHandCycling.scenario).tertiary, null);
  assert.deepEqual(computeResidualCoverage(bottleHandCycling.scenario).leftoverBuyerIds.filter((id) => !(leftoverFill.selectedBuyerIds ?? []).includes(id)), ["B06"]);
  assert.ok(bottleHandCycling.results.some((result) => result.offer.merchant === "Court-side Bottle-hand Delivery" && result.offer.fulfillment === "shipping"));
  assert.ok(bottleHandCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Bottle-hand pie")));
  assert.equal(bottleHandCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Soigneur pie")), false);
  assert.equal(bottleHandCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Station salad")), false);
  assert.equal(bottleHandCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Cake water")), false);
  assert.equal(bottleHandCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Lead-out pie")), false);
  assert.equal(bottleHandCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Train salad")), false);
  assert.equal(bottleHandCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Launch water")), false);
  assert.equal(bottleHandCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Feed-zone pie")), false);
  assert.equal(bottleHandCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("First-aid pie")), false);
  assert.equal(bottleHandCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Tent salad")), false);
  assert.equal(bottleHandCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Triage water")), false);
  assert.equal(bottleHandCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Team-sprint pie")), false);
  assert.equal(bottleHandCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Team-pursuit pie")), false);
  assert.equal(bottleHandCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Scratch pie")), false);
  assert.equal(bottleHandCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Points-race pie")), false);
  assert.equal(bottleHandCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Omnium pie")), false);
  assert.equal(bottleHandCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Bell salad")), false);
  assert.equal(bottleHandCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Prime water")), false);
  assert.equal(bottleHandCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Tempo salad")), false);
  assert.equal(bottleHandCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Tempo water")), false);
  assert.ok(bottleHandCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Bidon salad")));
  assert.ok(bottleHandCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Crate water")));
  assert.equal(bottleHandCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Relay salad")), false);
  assert.equal(bottleHandCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Trio water")), false);
  assert.equal(bottleHandCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Changeover salad")), false);
  assert.equal(bottleHandCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Quartet water")), false);
  assert.equal(bottleHandCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Catch salad")), false);
  assert.equal(bottleHandCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Pursuit water")), false);
  assert.equal(bottleHandCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Individual-pursuit pie")), false);
  assert.equal(bottleHandCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Bunch salad")), false);
  assert.equal(bottleHandCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Chip water")), false);
  assert.equal(bottleHandCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Madison pie")), false);
  assert.equal(bottleHandCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Handsling salad")), false);
  assert.equal(bottleHandCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Handsling water")), false);
  assert.equal(bottleHandCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Keirin pie")), false);
  assert.equal(bottleHandCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Derny salad")), false);
  assert.equal(bottleHandCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Derny water")), false);
  assert.equal(bottleHandCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Hill-climb pie")), false);
  assert.equal(bottleHandCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Hairpin salad")), false);
  assert.equal(bottleHandCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Hairpin water")), false);
  assert.equal(bottleHandCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Criterium pie")), false);
  assert.equal(bottleHandCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Circuit salad")), false);
  assert.equal(bottleHandCycling.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Circuit water")), false);
  const payload = JSON.stringify(clonePreset("bottleHandCyclingCarnivalLunch"));
  assert.equal(payload.includes("start-ramp"), false);
  assert.equal(payload.includes("timing-hut"), false);
  assert.equal(payload.includes("time-check"), false);
  assert.equal(payload.includes("pit-lane"), false);
  assert.equal(payload.includes("circuit"), false);
  assert.equal(payload.includes("sealed-road"), false);
  assert.equal(payload.includes("treatment-tent"), false);
  assert.equal(payload.includes("ice-pack-rota"), false);
  assert.equal(payload.includes("triage-board"), false);
  assert.equal(payload.includes("banked-boards"), false);
  assert.equal(payload.includes("summit-marshal"), false);
  assert.equal(payload.includes("hairpin"), false);
  assert.equal(payload.includes("climb-chip"), false);
  assert.equal(payload.includes("derny-pacer"), false);
  assert.equal(payload.includes("keirin-chip"), false);
  assert.equal(payload.includes("keirin-gate"), false);
  assert.equal(payload.includes("madison-pair"), false);
  assert.equal(payload.includes("handsling"), false);
  assert.equal(payload.includes("omnium-board"), false);
  assert.equal(payload.includes("flying-200"), false);
  assert.equal(payload.includes("elimination-heat"), false);
  assert.equal(payload.includes("tempo-lap"), false);
  assert.equal(payload.includes("points-race"), false);
  assert.equal(payload.includes("points-sprint"), false);
  assert.equal(payload.includes("bell-lap"), false);
  assert.equal(payload.includes("prime-board"), false);
  assert.equal(payload.includes("scratch-bunch"), false);
  assert.equal(payload.includes("bunch-finish"), false);
  assert.equal(payload.includes("scratch-chip"), false);
  assert.equal(payload.includes("pursuit-line"), false);
  assert.equal(payload.includes("catch-bell"), false);
  assert.equal(payload.includes("pursuit-chip"), false);
  assert.equal(payload.includes("changeover-bell"), false);
  assert.equal(payload.includes("quartet-board"), false);
  assert.equal(payload.includes("team-pursuit-rota"), false);
  assert.equal(payload.includes("start-gate"), false);
  assert.equal(payload.includes("flying-relay"), false);
  assert.equal(payload.includes("trio-board"), false);
  assert.equal(payload.includes("Start-gate"), false);
  assert.equal(payload.includes("Flying-relay"), false);
  assert.equal(payload.includes("Trio-board"), false);
  assert.equal(payload.includes("musette-line"), false);
  assert.equal(payload.includes("sticky-bottle"), false);
  assert.equal(payload.includes("bottle-hand-up"), false);
  assert.equal(payload.includes("Musette-line"), false);
  assert.equal(payload.includes("Feed-zone pie"), false);
  assert.equal(payload.includes("Lead-out pie"), false);
  assert.equal(payload.includes("wheel-change"), false);
  assert.equal(payload.includes("neutral-service"), false);
  assert.equal(payload.includes("team-car"), false);
  assert.equal(payload.includes("Wheel-change crate"), false);
  assert.equal(payload.includes("Domestique pie"), false);
  assert.equal(payload.includes("soigneur-station"), false);
  assert.equal(payload.includes("cape-bag"), false);
  assert.equal(payload.includes("rice-cake"), false);
  assert.equal(payload.includes("Soigneur-station crate"), false);
  assert.equal(payload.includes("Cape-bag bench"), false);
  assert.equal(payload.includes("Rice-cake hamper"), false);
  assert.equal(payload.includes("Soigneur pie"), false);
  assert.equal(payload.includes("train-line"), false);
  assert.equal(payload.includes("last-rider"), false);
  assert.equal(payload.includes("sprinter-launch"), false);
  assert.equal(payload.includes("Train-line crate"), false);
  assert.equal(payload.includes("Last-rider bench"), false);
  assert.equal(payload.includes("Sprinter-launch hamper"), false);
  assert.match(payload, /Bottle-hand/);
  assert.match(payload, /Bidon-pass crate/);
  assert.match(payload, /Car-window bench/);
  assert.match(payload, /Crate-lock hamper/);
  assert.match(payload, /Car-window bench/);
  assert.match(payload, /Crate-lock hamper/);
  const first = evaluateMarket(clonePreset("bottleHandCyclingCarnivalLunch"));
  const second = evaluateMarket(validateScenario(clonePreset("bottleHandCyclingCarnivalLunch")));
  assert.equal(first.winner.offer.id, second.winner.offer.id);
  assert.equal(first.winner.fulfilledUnits, second.winner.fulfilledUnits);
  assert.equal(first.winner.fulfilledUnits, 54);
});

test("the example bar includes bottle-hand cycling carnival lunch next to domestique cycling carnival lunch", async () => {
  const html = await readFile(new URL("../index.html", import.meta.url), "utf8");
  assert.match(html, /data-preset="bottleHandCyclingCarnivalLunch"/u);
  assert.match(html, /Bottle-hand cycling carnival/u);
  assert.match(html, /data-preset="domestiqueCyclingCarnivalLunch"/u);
  assert.match(html, /Domestique cycling carnival/u);
  assert.match(html, /data-preset="soigneurCyclingCarnivalLunch"/u);
  assert.match(html, /Soigneur cycling carnival/u);
  assert.match(html, /data-preset="leadOutCyclingCarnivalLunch"/u);
  assert.match(html, /Lead-out cycling carnival/u);
  assert.match(html, /data-preset="feedZoneCyclingCarnivalLunch"/u);
  assert.match(html, /Feed-zone cycling carnival/u);
  assert.match(html, /data-preset="firstAidCyclingCarnivalLunch"/u);
  assert.match(html, /First-aid cycling carnival/u);
  assert.match(html, /data-preset="teamSprintCyclingCarnivalLunch"/u);
  assert.match(html, /Team-sprint cycling carnival/u);
  assert.match(html, /data-preset="scratchCyclingCarnivalLunch"/u);
  assert.match(html, /data-preset="pointsRaceCyclingCarnivalLunch"/u);
  assert.ok(html.indexOf('data-preset="pointsRaceCyclingCarnivalLunch"') < html.indexOf('data-preset="scratchCyclingCarnivalLunch"'));
  assert.match(html, /data-preset="individualPursuitCyclingCarnivalLunch"/u);
  assert.ok(html.indexOf('data-preset="scratchCyclingCarnivalLunch"') < html.indexOf('data-preset="individualPursuitCyclingCarnivalLunch"'));
  assert.ok(html.indexOf('data-preset="individualPursuitCyclingCarnivalLunch"') < html.indexOf('data-preset="teamPursuitCyclingCarnivalLunch"'));
  assert.ok(html.indexOf('data-preset="teamPursuitCyclingCarnivalLunch"') < html.indexOf('data-preset="teamSprintCyclingCarnivalLunch"'));
  assert.ok(html.indexOf('data-preset="teamSprintCyclingCarnivalLunch"') < html.indexOf('data-preset="firstAidCyclingCarnivalLunch"'));
  assert.ok(html.indexOf('data-preset="firstAidCyclingCarnivalLunch"') < html.indexOf('data-preset="feedZoneCyclingCarnivalLunch"'));
  assert.ok(html.indexOf('data-preset="feedZoneCyclingCarnivalLunch"') < html.indexOf('data-preset="leadOutCyclingCarnivalLunch"'));
  assert.ok(html.indexOf('data-preset="leadOutCyclingCarnivalLunch"') < html.indexOf('data-preset="soigneurCyclingCarnivalLunch"'));
  assert.ok(html.indexOf('data-preset="soigneurCyclingCarnivalLunch"') < html.indexOf('data-preset="domestiqueCyclingCarnivalLunch"'));
  assert.ok(html.indexOf('data-preset="domestiqueCyclingCarnivalLunch"') < html.indexOf('data-preset="bottleHandCyclingCarnivalLunch"'));
  assert.ok(html.indexOf('data-preset="bottleHandCyclingCarnivalLunch"') < html.indexOf('data-preset="tiers"'));
});
