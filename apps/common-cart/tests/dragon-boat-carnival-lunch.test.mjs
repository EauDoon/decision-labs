import test from "node:test";
import assert from "node:assert/strict";
import { readFile } from "node:fs/promises";
import { clonePreset, computeResidualCoverage, evaluateMarket, validateScenario } from "../src/model.js";

test("dragon boat carnival lunch leftover fill is hall pickup and stays distinct from kayaking carnival lunch", () => {
  const dragonBoat = evaluateMarket(clonePreset("dragonBoatCarnivalLunch"));
  const kayaking = evaluateMarket(clonePreset("kayakingCarnivalLunch"));
  const canoeing = evaluateMarket(clonePreset("canoeingCarnivalLunch"));
  const sailing = evaluateMarket(clonePreset("sailingCarnivalLunch"));
  const rowing = evaluateMarket(clonePreset("rowingCarnivalLunch"));
  const waterPolo = evaluateMarket(clonePreset("waterPoloCarnivalLunch"));
  const softball = evaluateMarket(clonePreset("softballCarnivalLunch"));
  const baseball = evaluateMarket(clonePreset("baseballCarnivalLunch"));
  const hockey = evaluateMarket(clonePreset("hockeyCarnivalLunch"));
  const rugby = evaluateMarket(clonePreset("rugbyCarnivalLunch"));
  const soccer = evaluateMarket(clonePreset("soccerCarnivalLunch"));
  assert.ok(dragonBoat.winner);
  assert.equal(dragonBoat.winner.offer.merchant, "Court-side Dragon Boat Delivery");
  assert.equal(dragonBoat.winner.offer.fulfillment, "shipping");
  assert.equal(dragonBoat.winner.fulfilledUnits, 50);
  assert.deepEqual(dragonBoat.winner.selectedBuyerIds, ["B01", "B02", "B05"]);
  assert.equal(dragonBoat.scenario.title, "Dragon boat carnival lunch");
  assert.notEqual(dragonBoat.scenario.title, kayaking.scenario.title);
  assert.notEqual(dragonBoat.scenario.title, canoeing.scenario.title);
  assert.notEqual(dragonBoat.scenario.title, sailing.scenario.title);
  assert.notDeepEqual(clonePreset("dragonBoatCarnivalLunch"), clonePreset("kayakingCarnivalLunch"));
  assert.notDeepEqual(clonePreset("dragonBoatCarnivalLunch"), clonePreset("canoeingCarnivalLunch"));
  assert.notDeepEqual(clonePreset("dragonBoatCarnivalLunch"), clonePreset("sailingCarnivalLunch"));
  assert.notDeepEqual(clonePreset("dragonBoatCarnivalLunch"), clonePreset("rowingCarnivalLunch"));
  assert.notDeepEqual(clonePreset("dragonBoatCarnivalLunch"), clonePreset("waterPoloCarnivalLunch"));
  assert.notDeepEqual(clonePreset("dragonBoatCarnivalLunch"), clonePreset("softballCarnivalLunch"));
  assert.notDeepEqual(clonePreset("dragonBoatCarnivalLunch"), clonePreset("baseballCarnivalLunch"));
  assert.notDeepEqual(clonePreset("dragonBoatCarnivalLunch"), clonePreset("hockeyCarnivalLunch"));
  assert.notDeepEqual(clonePreset("dragonBoatCarnivalLunch"), clonePreset("rugbyCarnivalLunch"));
  assert.notDeepEqual(clonePreset("dragonBoatCarnivalLunch"), clonePreset("soccerCarnivalLunch"));
  const leftoverFill = computeResidualCoverage(dragonBoat.scenario).secondary;
  assert.ok(leftoverFill);
  assert.equal(leftoverFill.merchant, "Hall Dragon Boat Pickup");
  const leftoverOffer = dragonBoat.scenario.offers.find((offer) => offer.id === leftoverFill.offerId);
  assert.equal(leftoverOffer.fulfillment, "pickup");
  assert.equal(leftoverOffer.minimumUnits, 21);
  assert.notEqual(leftoverOffer.unitPrice, leftoverOffer.minimumUnits);
  assert.equal(leftoverOffer.unitPrice, 17);
  assert.equal(dragonBoat.scenario.buyers.find((buyer) => buyer.id === "B04").maxUnitPrice, 17);
  assert.equal(kayaking.scenario.offers.find((offer) => offer.merchant === "Hall Kayaking Pickup").minimumUnits, 20);
  assert.equal(canoeing.scenario.offers.find((offer) => offer.merchant === "Hall Canoeing Pickup").minimumUnits, 19);
  assert.equal(sailing.scenario.offers.find((offer) => offer.merchant === "Hall Sailing Pickup").minimumUnits, 18);
  assert.equal(rowing.scenario.offers.find((offer) => offer.merchant === "Hall Rowing Pickup").minimumUnits, 17);
  assert.equal(soccer.scenario.offers.find((offer) => offer.merchant === "Hall Soccer Pickup").minimumUnits, 11);
  assert.equal(rugby.scenario.offers.find((offer) => offer.merchant === "Hall Rugby Pickup").minimumUnits, 12);
  assert.equal(hockey.scenario.offers.find((offer) => offer.merchant === "Hall Hockey Pickup").minimumUnits, 13);
  assert.equal(baseball.scenario.offers.find((offer) => offer.merchant === "Hall Baseball Pickup").minimumUnits, 14);
  assert.equal(softball.scenario.offers.find((offer) => offer.merchant === "Hall Softball Pickup").minimumUnits, 15);
  assert.equal(waterPolo.scenario.offers.find((offer) => offer.merchant === "Hall Water Polo Pickup").minimumUnits, 16);
  assert.equal(kayaking.winner.offer.merchant, "Court-side Kayaking Delivery");
  assert.equal(sailing.winner.offer.merchant, "Court-side Sailing Delivery");
  assert.equal(canoeing.winner.offer.merchant, "Court-side Canoeing Delivery");
  assert.equal(rowing.winner.offer.merchant, "Court-side Rowing Delivery");
  assert.equal(soccer.winner.offer.merchant, "Court-side Soccer Delivery");
  assert.equal(leftoverOffer.capacity, 44);
  assert.equal(leftoverFill.fulfilledUnits, 23);
  assert.equal(leftoverOffer.capacity - leftoverFill.fulfilledUnits, 21);
  assert.notEqual(leftoverOffer.capacity - leftoverFill.fulfilledUnits, leftoverOffer.unitPrice);
  assert.deepEqual(leftoverFill.selectedBuyerIds, ["B03", "B04"]);
  assert.equal(computeResidualCoverage(dragonBoat.scenario).tertiary, null);
  assert.deepEqual(computeResidualCoverage(dragonBoat.scenario).leftoverBuyerIds.filter((id) => !(leftoverFill.selectedBuyerIds ?? []).includes(id)), ["B06"]);
  assert.ok(dragonBoat.results.some((result) => result.offer.merchant === "Court-side Dragon Boat Delivery" && result.offer.fulfillment === "shipping"));
  assert.ok(dragonBoat.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Dragon boat pie")));
  assert.ok(dragonBoat.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Drum salad")));
  assert.ok(dragonBoat.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Drum water")));
  assert.equal(dragonBoat.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Kayaking pie")), false);
  assert.equal(dragonBoat.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Boathouse water")), false);
  assert.equal(dragonBoat.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Boathouse salad")), false);
  assert.equal(dragonBoat.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Canoeing pie")), false);
  assert.equal(dragonBoat.scenario.buyers.some((buyer) => buyer.allowedVariants.includes("Clubhouse water")), false);
  const first = evaluateMarket(clonePreset("dragonBoatCarnivalLunch"));
  const second = evaluateMarket(validateScenario(clonePreset("dragonBoatCarnivalLunch")));
  assert.equal(first.winner.offer.id, second.winner.offer.id);
  assert.equal(first.winner.fulfilledUnits, second.winner.fulfilledUnits);
  assert.equal(first.winner.fulfilledUnits, 50);
});

test("the example bar includes dragon boat carnival lunch next to kayaking carnival lunch", async () => {
  const html = await readFile(new URL("../index.html", import.meta.url), "utf8");
  assert.match(html, /data-preset="dragonBoatCarnivalLunch"/u);
  assert.match(html, /Dragon boat carnival/u);
  assert.match(html, /data-preset="kayakingCarnivalLunch"/u);
});
