import {DEFAULT_SCENARIO} from '../src/model.js';
export const fixture=()=>({...DEFAULT_SCENARIO,name:'Synthetic hourly ledger',nominalLiquidityAud:10000,reserveCashAud:72,redemptionDemandAud:72,issuerThroughputAudPerHour:100,fxDepthAudPerHour:100,payoutThroughputAudPerHour:100,issuerOpenStartHour:0,issuerOpenEndHour:24,bankOpenStartHour:0,bankOpenEndHour:24,payoutOpenStartHour:0,payoutOpenEndHour:24});
export function oracle(s){let queue=0,reserve=s.reserveCashAud,settled=0;const rows=[];for(let h=0;h<72;h++){const totalHour=15+h,day=(5+Math.floor(totalHour/24))%7,hour=totalHour%24;const open=day>=1&&day<=5&&!(day===1&&s.mondayHoliday)&&['issuer','bank','payout'].every(g=>hour>=s[g+'OpenStartHour']&&hour<s[g+'OpenEndHour']);const arrival=s.redemptionDemandAud/72;queue+=arrival;const paid=open?Math.min(queue,reserve,s.issuerThroughputAudPerHour,s.fxDepthAudPerHour,s.payoutThroughputAudPerHour):0;queue-=paid;reserve-=paid;settled+=paid;rows.push({h,queue,settled,paid,arrival,open});}return rows;}
