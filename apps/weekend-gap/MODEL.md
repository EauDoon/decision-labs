# Weekend Gap model notes

## Purpose

This document describes a synthetic, deterministic model of AUD stablecoin redemption operations from Friday 15:00 through Monday 15:00 local time. It is designed to make operating-window constraints visible. It is not a valuation model, market forecast, reserve assessment or representation of a real issuer.

## Reserve planner and comparison

The planner uses the same hourly queue and settlement recurrence as the
timeline. Its target is a percentage of total 72-hour demand, not just demand
arrived by the deadline. Hour 1 is the checkpoint after the first hourly step;
hour 72 is Monday 15:00.

Only starting reserve changes. Candidate reserves are integer cents between zero
and nominal liquidity rounded down to cents. Settlement is nondecreasing in
starting reserve under the fixed inputs, so a binary search finds the first
passing cent in at most 39 iterations at the model's A$5 billion bound. A
scale-aware tolerance (128 times machine epsilon times the larger target or
settled amount, below A$0.000143 at the model cap) absorbs floating-point
summation without accepting zero settlement for a positive target. Tests compare planner
outputs with the full simulation and require the preceding cent to fail.

If the maximum allowed reserve cannot meet the target, the result is unreachable.
It must not be read as a recommendation to add reserve. Demand arrival, closed
windows, throughput, and nominal liquidity may each prevent the target.

Comparison deltas are current minus baseline. Baselines are detached canonical
scenarios saved with the local workspace. A decrease in queue is not necessarily an improvement
if demand or other assumptions changed. The analysis JSON report binds both
input scenarios, changed fields, summaries, planner target and deadline, and
hourly queue values. It has no timestamp, making identical inputs reproducible.

## Units and editable assumptions

All money values are Australian dollars (AUD). Throughputs and FX depth are AUD per hour. Operating hours use a local 24-hour clock and apply on business days only. The editable scenario contains:

- Nominal stablecoin liquidity: the modeled outstanding nominal base used in the liquidity-ratio denominator.
- AUD reserve and cash capacity: the maximum cash pool that can be paid in the 72-hour simulation.
- Issuer redemption throughput and business-day operating window.
- Bank settlement operating window.
- FX depth, weekday spread and weekend multiplier.
- Australian AUD payout or off-ramp throughput and business-day operating window.
- Total 72-hour redemption demand.

Demand timing is explicitly selected: flat, Friday burst, Saturday burst, Sunday burst, or Monday rush. Total demand is the only demand-volume input; each profile redistributes it deterministically. Adjacent earlier and later steps move Friday burst, flat, then Monday rush without randomness. Saturday burst is an extra synthetic arrival profile used by the Saturday market burst preset; Sunday burst is an extra synthetic arrival profile used by the Sunday stall close preset; the three-row timing experiment still compares flat, Friday burst and Monday rush. Presets change only visible scenario fields. The Long-weekend Friday start (synthetic) preset turns on Saturday and Monday holidays and keeps Normal Friday operating windows. The Compressed Friday close (synthetic) preset uses Friday burst arrivals and 16:00 operating closes without holiday flags. The Payday Friday burst (synthetic) preset keeps Normal Friday operating windows and uses Friday burst arrivals with a larger total demand. The Public-holiday Monday (synthetic) preset keeps the Normal Friday start and operating windows and turns on the Monday holiday so Monday banking windows stay closed. The Saturday market burst (synthetic) preset keeps the Normal Friday 72-hour calendar and uses Saturday-weighted arrivals. The Sunday stall close (synthetic) preset keeps the Normal Friday 72-hour calendar, uses late-Sunday-weighted arrivals, and closes payouts one hour earlier. The Thin Saturday FX (synthetic) preset keeps the Normal Friday 72-hour calendar, uses Saturday-weighted arrivals, and tightens Saturday FX hours. The Early Monday bank open (synthetic) preset keeps the Normal Friday 72-hour calendar and opens the Monday bank window one hour earlier. The Friday late FX close (synthetic) preset keeps the Normal Friday 72-hour calendar and delays weekend FX thinning by one hour so Saturday 00:00 keeps weekday FX depth. The Monday late issuer open (synthetic) preset keeps the Normal Friday 72-hour calendar and opens the Monday issuer window one hour later. The Saturday early FX open (synthetic) preset keeps the Normal Friday 72-hour calendar and opens weekday FX depth on Saturday morning from 06:00 to 11:00, earlier than Thin Saturday FX, Friday late FX close, Monday late issuer open, and Early Monday bank open. The Sunday late bank close (synthetic) preset keeps the Normal Friday 72-hour calendar and treats the bank gate as open on Sunday from 16:00 to 18:00 even though Sunday is not a business day. It is distinct from Sunday stall close, Saturday early FX open, Friday late FX close, Monday late issuer open, Early Monday bank open, Thin Saturday FX, Payday Friday, Public-holiday Monday, Saturday market burst, Long-weekend, and Compressed Friday. The Sunday late payout close (synthetic) preset keeps the Normal Friday 72-hour calendar and treats the payout gate as open on Sunday from 16:00 to 18:00 even though Sunday is not a business day. It is distinct from Sunday late bank close, Sunday stall close, Saturday early FX open, Friday late FX close, Monday late issuer open, Early Monday bank open, Thin Saturday FX, Payday Friday, Public-holiday Monday, Saturday market burst, Long-weekend Friday start, and Compressed Friday close. The Saturday early payout open (synthetic) preset keeps the Normal Friday 72-hour calendar and treats the payout gate as open on Saturday morning from 07:00 to 09:00, earlier than Sunday late payout close. It is distinct from Sunday late payout close, Sunday late bank close, Saturday early FX open, Friday late FX close, Monday late issuer open, Early Monday bank open, Thin Saturday FX, Payday Friday, Public-holiday Monday, Saturday market burst, Sunday stall close, Long-weekend Friday start, and Compressed Friday close. Synthetic. Not a bank feed. The Friday early payout open (synthetic) preset keeps the Normal Friday 72-hour calendar and treats the payout gate as open on Friday evening from 18:00 to 20:00 after the ordinary Friday close. It is distinct from Saturday early payout open, Saturday late payout open, Sunday late payout close, Sunday late bank close, Saturday early FX open, Friday late FX close, Monday late issuer open, Early Monday bank open, Thin Saturday FX, Payday Friday, Public-holiday Monday, Saturday market burst, Sunday stall close, Long-weekend Friday start, and Compressed Friday close. Synthetic. Not a bank feed. The Saturday late payout open (synthetic) preset keeps the Normal Friday 72-hour calendar and treats the payout gate as open on Saturday evening from 18:00 to 20:00. It is distinct from Friday early payout open, Saturday early payout open, Sunday late payout close, Sunday late bank close, Saturday early FX open, Friday late FX close, Monday late issuer open, Early Monday bank open, Thin Saturday FX, Payday Friday, Public-holiday Monday, Saturday market burst, Sunday stall close, Long-weekend Friday start, and Compressed Friday close. Synthetic. Not a bank feed. The Sunday early payout open (synthetic) preset keeps the Normal Friday 72-hour calendar and treats the payout gate as open on Sunday morning from 08:00 to 10:00. It is distinct from Saturday late payout open, Friday early payout open, Saturday early payout open, Sunday late payout close, Sunday late bank close, Saturday early FX open, Friday late FX close, Monday late issuer open, Early Monday bank open, Thin Saturday FX, Payday Friday, Public-holiday Monday, Saturday market burst, Sunday stall close, Long-weekend Friday start, and Compressed Friday close. Synthetic. Not a bank feed. The Sunday late issuer close (synthetic) preset keeps the Normal Friday 72-hour calendar and treats the issuer gate as open on Sunday from 16:00 to 18:00 even though Sunday is not a business day. It is distinct from Sunday late bank close, Sunday late payout close, Sunday early payout open, Saturday late payout open, Friday early payout open, Saturday early payout open, Saturday early FX open, Friday late FX close, Monday late issuer open, Early Monday bank open, Thin Saturday FX, Payday Friday, Public-holiday Monday, Saturday market burst, Sunday stall close, Long-weekend Friday start, and Compressed Friday close. Synthetic. Not a bank feed. The Sunday early issuer open (synthetic) preset keeps the Normal Friday 72-hour calendar and treats the issuer gate as open on Sunday from 08:00 to 10:00 even though Sunday is not a business day. It is distinct from Sunday late issuer close, Sunday late bank close, Sunday late payout close, Sunday early payout open, Saturday late payout open, Friday early payout open, Saturday early payout open, Saturday early FX open, Friday late FX close, Monday late issuer open, Early Monday bank open, Thin Saturday FX, Payday Friday, Public-holiday Monday, Saturday market burst, Sunday stall close, Long-weekend Friday start, and Compressed Friday close. Synthetic. Not a bank feed. The Saturday early issuer open (synthetic) preset keeps the Normal Friday 72-hour calendar and treats the issuer gate as open on Saturday from 08:00 to 10:00 even though Saturday is not a business day. It is distinct from Sunday early issuer open, Sunday late issuer close, Sunday late bank close, Sunday late payout close, Sunday early payout open, Saturday late payout open, Friday early payout open, Saturday early payout open, Saturday early FX open, Friday late FX close, Monday late issuer open, Early Monday bank open, Thin Saturday FX, Payday Friday, Public-holiday Monday, Saturday market burst, Sunday stall close, Long-weekend Friday start, and Compressed Friday close. Synthetic. Not a bank feed. Optional issuer, bank, payout and FX display labels default to Issuer, Bank, Payout and FX. Older files without a demand profile default to flat. Older files without display labels restore those generic names.

## Timeline and gates

The timeline has 72 one-hour events beginning Friday 15:00, plus an initial state at hour zero. For each event `t`, demand enters the queue. An issuer redemption gate, bank settlement gate and Australian AUD payout gate must all be open for settlement to occur. Each gate is available only on Monday through Friday and only inside its editable local-hour window. An optional `mondayHoliday` flag, default false, treats Monday as a non-business day like Sunday: issuer, bank and payout windows stay closed, and the weekend FX multiplier applies. Older scenario JSON without the field keeps a weekday Monday. An optional `saturdayHoliday` flag, default false, treats Saturday as a non-business day like Sunday. Saturday is already a weekend in the model; the flag records an explicit Sunday-like public holiday and labels Saturday accordingly. When the flag is on, the simulator notices that Saturday holiday and Sunday-style close overlap, so both weekend days are treated as closed. When Monday holiday and Saturday holiday are both on, a separate notice says Saturday, Sunday and Monday stay closed under Sunday-style rules. Older files without the Saturday field keep the existing weekend Saturday.

An optional `fridayFxLateClose` flag, default false, keeps weekday FX depth at Saturday 00:00, one hour after Friday. Issuer, bank and payout windows stay closed that hour. Older files without the field start weekend FX thinning at Saturday 00:00.

An optional `saturdayEarlyFxOpen` flag, default false, keeps weekday FX depth on Saturday from 06:00 to 11:00. Issuer, bank and payout windows stay closed those hours. Older files without the field keep weekend FX thinning on Saturday morning. This is a synthetic calendar, not a bank feed.

An optional `sundayLateBankClose` flag, default false, treats the bank gate as open on Sunday from 16:00 to 18:00 even though Sunday is not a business day. Issuer, payout and FX stay on their Sunday rules those hours. Older files without the field keep Sunday bank closed. This is a synthetic calendar, not a bank feed.

An optional `sundayLatePayoutClose` flag, default false, treats the payout gate as open on Sunday from 16:00 to 18:00 even though Sunday is not a business day. Issuer, bank and FX stay on their Sunday rules those hours. Older files without the field keep Sunday payout closed. This is a synthetic calendar, not a bank feed.

An optional `saturdayEarlyPayoutOpen` flag, default false, treats the payout gate as open on Saturday from 07:00 to 09:00 even though Saturday is not a business day. Issuer, bank and FX stay on their Saturday rules those hours. Older files without the field keep Saturday payout closed. This is a synthetic calendar, not a bank feed.

An optional `fridayEarlyPayoutOpen` flag, default false, treats the payout gate as open on Friday from 18:00 to 20:00 after the ordinary Friday close. Issuer, bank and FX stay on their Friday evening rules those hours. Older files without the field keep Friday evening payout closed. This is a synthetic calendar, not a bank feed.

An optional `saturdayLatePayoutOpen` flag, default false, treats the payout gate as open on Saturday from 18:00 to 20:00 even though Saturday is not a business day. Issuer, bank and FX stay on their Saturday evening rules those hours. Older files without the field keep Saturday evening payout closed. This is a synthetic calendar, not a bank feed.

An optional `sundayEarlyPayoutOpen` flag, default false, treats the payout gate as open on Sunday from 08:00 to 10:00 even though Sunday is not a business day. Issuer, bank and FX stay on their Sunday morning rules those hours. Older files without the field keep Sunday morning payout closed. This is a synthetic calendar, not a bank feed.

An optional `sundayLateIssuerClose` flag, default false, treats the issuer gate as open on Sunday from 16:00 to 18:00 even though Sunday is not a business day. Bank, payout and FX stay on their Sunday rules those hours. Older files without the field keep Sunday issuer closed. This is a synthetic calendar, not a bank feed.

An optional `sundayEarlyIssuerOpen` flag, default false, treats the issuer gate as open on Sunday from 08:00 to 10:00 even though Sunday is not a business day. Bank, payout and FX stay on their Sunday morning rules those hours. Older files without the field keep Sunday morning issuer closed. This is a synthetic calendar, not a bank feed.

An optional `saturdayEarlyIssuerOpen` flag, default false, treats the issuer gate as open on Saturday from 08:00 to 10:00 even though Saturday is not a business day. Bank, payout and FX stay on their Saturday morning rules those hours. Older files without the field keep Saturday morning issuer closed. This is a synthetic calendar, not a bank feed.

An optional `fridayEarlyIssuerOpen` flag, default false, treats the issuer gate as open on Friday from 08:00 to 10:00 even when the ordinary issuer window starts later. Bank, payout and FX stay on their Friday morning rules those hours. The 72-hour workshop clock starts Friday 15:00, so this window is before the modeled start. Older files without the field keep Friday morning on the ordinary issuer window. This is a synthetic calendar, not a bank feed.

An optional `saturdayEarlyBankOpen` flag, default false, treats the bank gate as open on Saturday from 08:00 to 10:00 even though Saturday is not a business day. Issuer, payout and FX stay on their Saturday morning rules those hours. Older files without the field keep Saturday morning bank closed. This is a synthetic calendar, not a bank feed.

An optional `fridayEarlyBankOpen` flag, default false, treats the bank gate as open on Friday from 08:00 to 10:00 even when the ordinary bank window starts later. Issuer, payout and FX stay on their Friday morning rules those hours. The 72-hour workshop clock starts Friday 15:00, so this window is before the modeled start. Older files without the field keep Friday morning on the ordinary bank window. This is a synthetic calendar, not a bank feed.

An optional `saturdayLateBankOpen` flag, default false, treats the bank gate as open on Saturday from 16:00 to 18:00 even though Saturday is not a business day. Issuer, payout and FX stay on their Saturday evening rules those hours. Older files without the field keep Saturday evening bank closed. This is a synthetic calendar, not a bank feed.

An optional `fridayLateBankOpen` flag, default false, treats the bank gate as open on Friday from 16:00 to 18:00 even when the ordinary bank window ends earlier. Issuer, payout and FX stay on their Friday evening rules those hours. The 72-hour workshop clock starts Friday 15:00, so this window is inside the modeled start. Older files without the field keep Friday evening on the ordinary bank window. This is a synthetic calendar, not a bank feed.

An optional `fridayLateFxOpen` flag, default false, treats FX as weekday-depth / open on Friday from 16:00 to 18:00. It is ORed into FX open / weekday FX depth (`fxWeekday`) through `isFridayLateFxOpenHour` and is distinct from `fridayLateBankOpen` (bank gate), Friday late FX close (`fridayFxLateClose` / `fridayLateFxClose`, which keeps Saturday 00:00 as delayed Friday FX close), Saturday late bank open, Saturday early bank open, and Saturday early FX open. Issuer, bank and payout stay on their Friday evening rules those hours. The 72-hour workshop clock starts Friday 15:00, so this window is inside the modeled start. Older files without the field keep Friday evening on ordinary weekday FX depth. This is a synthetic calendar, not an FX feed.

An optional `saturdayLateFxOpen` flag, default false, treats FX as weekday-depth / open on Saturday from 16:00 to 18:00 even though Saturday is not a business day. It is ORed into FX open / weekday FX depth (`fxWeekday`) through `isSaturdayLateFxOpenHour` and is distinct from `saturdayLateBankOpen` (bank gate, same clock window), `saturdayLatePayoutOpen` (18:00-20:00 payout), `saturdayEarlyFxOpen` (Saturday 06:00-11:00), and `fridayLateFxOpen` (Friday 16:00-18:00). Issuer, bank and payout stay on their Saturday evening rules those hours. The 72-hour workshop clock starts Friday 15:00, so this window is inside the modeled start. Older files without the field keep Saturday evening on ordinary weekend FX depth. This is a synthetic calendar, not an FX feed.

An optional `sundayLateFxOpen` flag, default false, treats FX as weekday-depth / open on Sunday from 16:00 to 18:00 even though Sunday is not a business day. It is ORed into FX open / weekday FX depth (`fxWeekday`) through `isSundayLateFxOpenHour` and is distinct from `saturdayLateFxOpen` (Saturday 16:00-18:00), `fridayLateFxOpen` (Friday 16:00-18:00), `saturdayLateBankOpen` (bank gate), `saturdayLatePayoutOpen` (payout), `sundayLateIssuerClose` (issuer gate, same clock window), `sundayLatePayoutClose` (payout, same clock window), and `sundayLateBankClose` (bank gate, same clock window). Issuer, bank and payout stay on their Sunday evening rules those hours. The 72-hour workshop clock starts Friday 15:00, so this window is inside the modeled start. Older files without the field keep Sunday evening on ordinary weekend FX depth. This is a synthetic calendar, not an FX feed.

An optional `sundayEarlyFxOpen` flag, default false, treats FX as weekday-depth / open on Sunday from 08:00 to 10:00 even though Sunday is not a business day. It is ORed into FX open / weekday FX depth (`fxWeekday`) through `isSundayEarlyFxOpenHour` and is distinct from `sundayLateFxOpen` (Sunday 16:00-18:00), `saturdayLateFxOpen` (Saturday 16:00-18:00), `fridayLateFxOpen` (Friday 16:00-18:00), `sundayEarlyPayoutOpen` (payout gate, same clock window), and `sundayEarlyIssuerOpen` (issuer gate, same clock window). Issuer, bank and payout stay on their Sunday morning rules those hours. The 72-hour workshop clock starts Friday 15:00, so this window is inside the modeled start. Older files without the field keep Sunday morning on ordinary weekend FX depth. This is a synthetic calendar, not an FX feed.

An optional `mondayEarlyFxOpen` flag, default false, treats FX as weekday-depth / open on Monday from 08:00 to 10:00. It is ORed into FX open / weekday FX depth (`fxWeekday`) through `isMondayEarlyFxOpenHour` so Monday early FX open is a real weekday-FX-open case, including when a Monday public holiday would otherwise thin FX. It is distinct from `sundayEarlyFxOpen` (Sunday 08:00-10:00), `fridayLateFxOpen` (Friday 16:00-18:00), `sundayLateFxOpen` (Sunday 16:00-18:00), `saturdayLateFxOpen` (Saturday 16:00-18:00), `mondayLateFxOpen` (Monday 16:00-18:00), and Early Monday bank open (`bankOpenStartHour`). Issuer, bank and payout stay on their Monday morning rules those hours. The 72-hour workshop clock starts Friday 15:00, so this window is inside the modeled start. Older files without the field keep Monday morning on ordinary weekday or holiday FX depth. This is a synthetic calendar, not an FX feed.

An optional `mondayLateFxOpen` flag, default false, treats FX as weekday-depth / open on Monday from 16:00 to 18:00. It is ORed into FX open / weekday FX depth (`fxWeekday`) through `isMondayLateFxOpenHour` so Monday late FX open is a real weekday-FX-open case, including when a Monday public holiday would otherwise thin FX. It is distinct from `mondayEarlyFxOpen` (Monday 08:00-10:00), `fridayLateFxOpen` (Friday 16:00-18:00), `saturdayLateFxOpen` (Saturday 16:00-18:00), and `sundayEarlyFxOpen` (Sunday 08:00-10:00). Issuer, bank and payout stay on their Monday evening rules those hours. Older files without the field keep Monday evening on ordinary weekday or holiday FX depth. This is a synthetic calendar, not an FX feed.

An optional `tuesdayEarlyFxOpen` flag, default false, treats FX as weekday-depth / open on Tuesday from 08:00 to 10:00. It is ORed into FX open / weekday FX depth (`fxWeekday`) through `isTuesdayEarlyFxOpenHour` so Tuesday early FX open is a real weekday-FX-open case. It is distinct from `mondayEarlyFxOpen` (Monday 08:00-10:00), `mondayLateFxOpen` (Monday 16:00-18:00), `fridayLateFxOpen` (Friday 16:00-18:00), `sundayEarlyFxOpen` (Sunday 08:00-10:00), `saturdayLateFxOpen` (Saturday 16:00-18:00), and `tuesdayLateFxOpen` (Tuesday 16:00-18:00). Issuer, bank and payout stay on their Tuesday morning rules those hours. Older files without the field keep Tuesday morning on ordinary weekday FX depth. This is a synthetic calendar, not an FX feed.

An optional `tuesdayLateFxOpen` flag, default false, treats FX as weekday-depth / open on Tuesday from 16:00 to 18:00. It is ORed into FX open / weekday FX depth (`fxWeekday`) through `isTuesdayLateFxOpenHour` so Tuesday late FX open is a real weekday-FX-open case. It is distinct from `tuesdayEarlyFxOpen` (Tuesday 08:00-10:00), `mondayLateFxOpen` (Monday 16:00-18:00), `mondayEarlyFxOpen` (Monday 08:00-10:00), `fridayLateFxOpen` (Friday 16:00-18:00), `sundayEarlyFxOpen` (Sunday 08:00-10:00), `saturdayLateFxOpen` (Saturday 16:00-18:00), `wednesdayEarlyFxOpen` (Wednesday 08:00-10:00), `wednesdayLateFxOpen` (Wednesday 16:00-18:00), `thursdayEarlyFxOpen` (Thursday 08:00-10:00), and `thursdayLateFxOpen` (Thursday 16:00-18:00). Issuer, bank and payout stay on their Tuesday afternoon rules those hours. Older files without the field keep Tuesday afternoon on ordinary weekday FX depth. This is a synthetic calendar, not an FX feed.

An optional `wednesdayEarlyFxOpen` flag, default false, treats FX as weekday-depth / open on Wednesday from 08:00 to 10:00. It is ORed into FX open / weekday FX depth (`fxWeekday`) through `isWednesdayEarlyFxOpenHour` so Wednesday early FX open is a real weekday-FX-open case. It is distinct from `tuesdayLateFxOpen` (Tuesday 16:00-18:00), `tuesdayEarlyFxOpen` (Tuesday 08:00-10:00), `mondayLateFxOpen` (Monday 16:00-18:00), `mondayEarlyFxOpen` (Monday 08:00-10:00), `fridayLateFxOpen` (Friday 16:00-18:00), `sundayEarlyFxOpen` (Sunday 08:00-10:00), `saturdayLateFxOpen` (Saturday 16:00-18:00), `wednesdayLateFxOpen` (Wednesday 16:00-18:00), `thursdayEarlyFxOpen` (Thursday 08:00-10:00), and `thursdayLateFxOpen` (Thursday 16:00-18:00). Issuer, bank and payout stay on their Wednesday morning rules those hours. Older files without the field keep Wednesday morning on ordinary weekday FX depth. This is a synthetic calendar, not an FX feed.

An optional `wednesdayLateFxOpen` flag, default false, treats FX as weekday-depth / open on Wednesday from 16:00 to 18:00. It is ORed into FX open / weekday FX depth (`fxWeekday`) through `isWednesdayLateFxOpenHour` so Wednesday late FX open is a real weekday-FX-open case. It is distinct from `wednesdayEarlyFxOpen` (Wednesday 08:00-10:00), `tuesdayLateFxOpen` (Tuesday 16:00-18:00), `tuesdayEarlyFxOpen` (Tuesday 08:00-10:00), `mondayLateFxOpen` (Monday 16:00-18:00), `mondayEarlyFxOpen` (Monday 08:00-10:00), `fridayLateFxOpen` (Friday 16:00-18:00), `sundayEarlyFxOpen` (Sunday 08:00-10:00), `saturdayLateFxOpen` (Saturday 16:00-18:00), `thursdayEarlyFxOpen` (Thursday 08:00-10:00), and `thursdayLateFxOpen` (Thursday 16:00-18:00). Issuer, bank and payout stay on their Wednesday afternoon rules those hours. Older files without the field keep Wednesday afternoon on ordinary weekday FX depth. This is a synthetic calendar, not an FX feed.

An optional `thursdayEarlyFxOpen` flag, default false, treats FX as weekday-depth / open on Thursday from 08:00 to 10:00. It is ORed into FX open / weekday FX depth (`fxWeekday`) through `isThursdayEarlyFxOpenHour` so Thursday early FX open is a real weekday-FX-open case. It is distinct from `thursdayLateFxOpen` (Thursday 16:00-18:00), `wednesdayLateFxOpen` (Wednesday 16:00-18:00), `wednesdayEarlyFxOpen` (Wednesday 08:00-10:00), `tuesdayLateFxOpen` (Tuesday 16:00-18:00), `tuesdayEarlyFxOpen` (Tuesday 08:00-10:00), `mondayLateFxOpen` (Monday 16:00-18:00), `mondayEarlyFxOpen` (Monday 08:00-10:00), `fridayLateFxOpen` (Friday 16:00-18:00), `sundayEarlyFxOpen` (Sunday 08:00-10:00), and `saturdayLateFxOpen` (Saturday 16:00-18:00). Issuer, bank and payout stay on their Thursday morning rules those hours. Older files without the field keep Thursday morning on ordinary weekday FX depth. This is a synthetic calendar, not an FX feed.

An optional `thursdayLateFxOpen` flag, default false, treats FX as weekday-depth / open on Thursday from 16:00 to 18:00. It is ORed into FX open / weekday FX depth (`fxWeekday`) through `isThursdayLateFxOpenHour` so Thursday late FX open is a real weekday-FX-open case. It is distinct from `thursdayEarlyFxOpen` (Thursday 08:00-10:00), `wednesdayLateFxOpen` (Wednesday 16:00-18:00), `wednesdayEarlyFxOpen` (Wednesday 08:00-10:00), `tuesdayLateFxOpen` (Tuesday 16:00-18:00), `tuesdayEarlyFxOpen` (Tuesday 08:00-10:00), `mondayLateFxOpen` (Monday 16:00-18:00), `mondayEarlyFxOpen` (Monday 08:00-10:00), `fridayLateFxOpen` (Friday 16:00-18:00), `fridayEarlyFxOpen` (Friday 08:00-10:00), `sundayEarlyFxOpen` (Sunday 08:00-10:00), and `saturdayLateFxOpen` (Saturday 16:00-18:00). Issuer, bank and payout stay on their Thursday afternoon rules those hours. Older files without the field keep Thursday afternoon on ordinary weekday FX depth. This is a synthetic calendar, not an FX feed.

An optional `fridayEarlyFxOpen` flag, default false, treats FX as weekday-depth / open on Friday from 08:00 to 10:00. It is ORed into FX open / weekday FX depth (`fxWeekday`) through `isFridayEarlyFxOpenHour` so Friday early FX open is a real weekday-FX-open case. It is distinct from `fridayLateFxOpen` (Friday 16:00-18:00), Friday late FX close (`fridayFxLateClose` / `isFridayLateFxHour`, Saturday 00:00 delayed Friday FX close), `thursdayLateFxOpen` (Thursday 16:00-18:00), `thursdayEarlyFxOpen` (Thursday 08:00-10:00), `fridayEarlyIssuerOpen` (issuer gate, same clock window), `fridayEarlyBankOpen` (bank gate, same clock window), and `fridayEarlyPayoutOpen` (payout). Issuer, bank and payout stay on their Friday morning rules those hours. Older files without the field keep Friday morning on ordinary weekday FX depth. This is a synthetic calendar, not an FX feed.

An optional `saturdayMiddayFxOpen` flag, default false, treats FX as weekday-depth / open on Saturday from 12:00 to 14:00 even though Saturday is not a business day. It is ORed into FX open / weekday FX depth (`fxWeekday`) through `isSaturdayMiddayFxOpenHour` so Saturday midday FX open is a real weekday-FX-open case. It is distinct from `saturdayAfternoonFxOpen` (Saturday 14:00-16:00), `saturdayEarlyFxOpen` (Saturday 06:00-11:00), `saturdayLateFxOpen` (Saturday 16:00-18:00), `fridayEarlyFxOpen` (Friday 08:00-10:00), `fridayLateFxOpen` (Friday 16:00-18:00), `sundayEarlyFxOpen` (Sunday 08:00-10:00), `saturdayEarlyIssuerOpen` (issuer gate), `saturdayEarlyBankOpen` (bank gate), and `saturdayEarlyPayoutOpen` (payout). Issuer, bank and payout stay on their Saturday midday rules those hours. Older files without the field keep Saturday midday on ordinary weekend FX depth. This is a synthetic calendar, not an FX feed.

An optional `sundayMiddayFxOpen` flag, default false, treats FX as weekday-depth / open on Sunday from 12:00 to 14:00 even though Sunday is not a business day. It is ORed into FX open / weekday FX depth (`fxWeekday`) through `isSundayMiddayFxOpenHour` so Sunday midday FX open is a real weekday-FX-open case. It is distinct from `saturdayAfternoonFxOpen` (Saturday 14:00-16:00), `saturdayMiddayFxOpen` (Saturday 12:00-14:00), `sundayEarlyFxOpen` (Sunday 08:00-10:00), `sundayLateFxOpen` (Sunday 16:00-18:00), `saturdayEarlyFxOpen` (Saturday 06:00-11:00), `saturdayLateFxOpen` (Saturday 16:00-18:00), `fridayEarlyFxOpen` (Friday 08:00-10:00), and `fridayLateFxOpen` (Friday 16:00-18:00). Issuer, bank and payout stay on their Sunday midday rules those hours. Older files without the field keep Sunday midday on ordinary weekend FX depth. This is a synthetic calendar, not an FX feed.

An optional `saturdayAfternoonFxOpen` flag, default false, treats FX as weekday-depth / open on Saturday from 14:00 to 16:00 even though Saturday is not a business day. It is ORed into FX open / weekday FX depth (`fxWeekday`) through `isSaturdayAfternoonFxOpenHour` so Saturday afternoon FX open is a real weekday-FX-open case. It is distinct from `saturdayMiddayFxOpen` (Saturday 12:00-14:00), `saturdayLateFxOpen` (Saturday 16:00-18:00), `saturdayEarlyFxOpen` (Saturday 06:00-11:00), `sundayMiddayFxOpen` (Sunday 12:00-14:00), `sundayEarlyFxOpen` (Sunday 08:00-10:00), `sundayLateFxOpen` (Sunday 16:00-18:00), `fridayEarlyFxOpen` (Friday 08:00-10:00), and `fridayLateFxOpen` (Friday 16:00-18:00). Issuer, bank and payout stay on their Saturday afternoon rules those hours. Older files without the field keep Saturday afternoon on ordinary weekend FX depth. This is a synthetic calendar, not an FX feed.

The FX weekend multiplier applies on Saturday and Sunday, except Saturday 00:00 when `fridayFxLateClose` is true, Saturday 06:00-11:00 when `saturdayEarlyFxOpen` is true, Saturday 12:00-14:00 when `saturdayMiddayFxOpen` is true, Sunday 12:00-14:00 when `sundayMiddayFxOpen` is true, Saturday 14:00-16:00 when `saturdayAfternoonFxOpen` is true, Saturday 16:00-18:00 when `saturdayLateFxOpen` is true, Sunday 16:00-18:00 when `sundayLateFxOpen` is true, Sunday 08:00-10:00 when `sundayEarlyFxOpen` is true, Monday 08:00-10:00 when `mondayEarlyFxOpen` is true and Monday is treated as a non-business day, Monday 16:00-18:00 when `mondayLateFxOpen` is true and Monday is treated as a non-business day, and Tuesday 08:00-10:00 when `tuesdayEarlyFxOpen` is true and Tuesday is treated as a non-business day, and Tuesday 16:00-18:00 when `tuesdayLateFxOpen` is true and Tuesday is treated as a non-business day, and Wednesday 08:00-10:00 when `wednesdayEarlyFxOpen` is true and Wednesday is treated as a non-business day, and Wednesday 16:00-18:00 when `wednesdayLateFxOpen` is true and Wednesday is treated as a non-business day, and Thursday 08:00-10:00 when `thursdayEarlyFxOpen` is true and Thursday is treated as a non-business day, and Thursday 16:00-18:00 when `thursdayLateFxOpen` is true and Thursday is treated as a non-business day, and Friday 08:00-10:00 when `fridayEarlyFxOpen` is true and Friday is treated as a non-business day:

```text
weekend FX depth = weekday FX depth / weekend multiplier
weekend FX spread = weekday FX spread × weekend multiplier
```

The model still requires issuer, bank and payout gates to settle. FX weakening therefore changes the synthetic spread and the capacity available in any hypothetical weekend overlap, but does not reopen a closed payout chain.

## Core formulas

For each hour, let `Q` be queued demand, `D` total redemption demand, `R` reserve remaining, `I` issuer hourly throughput, `F` available FX depth, and `P` payout hourly throughput.

```text
demand_this_hour = D * weight_this_hour / sum_of_all_72_weights
Q_before_settlement = Q_previous + demand_this_hour

capacity = 0                                      if any gate is closed
capacity = min(I, F, P, R)                        if all gates are open

settled_this_hour = min(Q_before_settlement, capacity)
Q_next = Q_before_settlement - settled_this_hour
R_next = R - settled_this_hour
```

The interface defines immediately redeemable AUD as the current hourly `capacity`, not the total reserve. This distinguishes a cash pool from the amount that can complete the full issuer-to-AUD payout chain now.

```text
effective_liquidity_ratio = immediate_redeemable_AUD / nominal_stablecoin_liquidity

queue_pressure = queued_AUD / total_redemption_demand
estimated_discount_bps = clamp(
  current_FX_spread_bps
  + 550 × (1 - effective_liquidity_ratio)
  + 400 × queue_pressure,
  0,
  10,000
)
```

When total redemption demand is zero, queue pressure is defined as zero. The discount or slippage value is only a synthetic stress indicator. It is not a market quote, executable price, token price forecast or loss estimate.

## Validation and failure states

The model exports pure functions. It sanitizes untrusted scenario data, clamps numeric values to bounded ranges, caps reserve capacity at nominal liquidity, enforces an operating window of at least one hour, and rejects malformed JSON and URL hashes without executing them. Scenario envelopes must carry the supported `weekend-gap-scenario` format and version; raw scenario objects remain importable, while malformed, unsupported, and analysis-report envelopes are rejected. It has no random input and no network access.

Explicit failure states include a closed issuer, bank or payout gate, zero reserve, zero throughput, zero FX depth, no overlapping payout time in the next seven days, malformed import JSON and malformed shared URLs. A failed import or hash keeps the current or default scenario instead of partially applying data.

## Non-goals and limitations

- The model cannot prove the actual liquidity, reserves, redemption rights, compliance status or operating hours of any issuer or financial institution.
- It does not represent a real bank, FX desk, off-ramp, blockchain, exchange, legal jurisdiction or payment scheme.
- It assumes one aggregate queue and one settlement route. It does not simulate priority, partial fills across venues, credit lines, fees, counterparty default, holidays, queue cancellation, token transfers, market makers or arbitrage.
- Business-day hours are illustrative local hours. The only holidays in the model are the optional synthetic Monday holiday and the optional Saturday holiday flag. Other public holidays and daylight-saving transitions remain outside the model.
- A cash reserve is treated as immediately available once all operating gates are open. This is an assumption, not a claim about custody or settlement finality.

Use the simulator to compare assumptions and reason about dependencies, not to make a trading, redemption, investment, legal or operational decision.

## Demand timing

Flat arrivals remain the default for existing scenario files. Friday burst gives each of the first nine hours weight 8, all other hours weight 1. Saturday burst gives hours 9 to 32 weight 8. Sunday burst gives hours 48 to 56 weight 8. Monday rush gives hours 57 to 71 weight 8. Each hourly demand is total demand times its weight divided by the sum of weights. This conserves total demand subject to floating-point rounding. The reserve planner uses exactly the same arrival schedule.

## Diagnostic semantics

There are 72 intervals and 73 checkpoints. A checkpoint's arrived and settled values describe the interval that just ended. Immediate capacity describes the next interval at that checkpoint. The initial checkpoint has no preceding interval. Queue exposure is the sum of all 72 end-of-interval queue balances times one hour, measured in AUD-hours. It is an exposure proxy, not a customer wait-time estimate. The longest backlog run counts consecutive intervals with a positive closing queue.

Blocker counts include every closed gate and exhausted reserve observed in an interval with a closing backlog. Counts can overlap and must not be added or interpreted as causal attribution. With no closed gate, the smallest available capacity is reported. Equal capacity ties use the model's existing deterministic order. A constrained resource may have no marginal effect if another gate is closed.

Sensitivity holds other assumptions fixed and scales one supported field by 50%, 75%, 100%, 125% and 150%. Effective values are sanitized and any cap is labeled. Starting from zero gives five zero cases. Increasing reserve cannot repair non-overlapping windows or throughput limits. The pinned baseline is separate from the sensitivity experiment's current-scenario reference.

Hours to first settlement is the 0-based hour offset of the first interval with positive settlement. The dashboard reports "No settlement in 72h" when none of the 72 intervals settle. Comparison deltas for this field are numeric only when both runs settle; a mixed null and number is stored as null rather than coerced through zero.

Hours to clear the queue is the first checkpoint hour where queued AUD is 0 after a checkpoint with a positive queue. The dashboard reports "queue remains" when the queue never returns to zero, and "No queue in 72h" when it never goes positive. Comparison deltas follow the same numeric-or-null rule as hours to first settlement.

Hourly limiting-gate attribution counts `limitingGate` at the start of each of the 72 intervals. Closed issuer, bank or payout gates are named before throughput or reserve. The count is an observation, not the marginal effect of changing one assumption. It is separate from overlapping backlog-blocker counts in the diagnostics list.

A window-shift preview adds whole hours to one gate's start and end, then clamps with the existing one-hour minimum window and re-runs the simulation. Peak queue and settled total deltas are relative to the current scenario. Apply is a separate action and is one scenario-history step, so Undo scenario edit restores the previous window.

Demand-profile comparison reuses every other current assumption and runs flat, Friday burst and Monday rush. Earlier and later step buttons preview one adjacent profile, then apply it as a separate history step. Saved-experiment comparison accepts two or three library copies only. Two scenario JSON files can be compared for queue and settlement diffs; mixed settlement or queue-clear hours stay null. Three scenario JSON files labeled baseline, current and imported can be summarised side by side; hours stay null when a run never queues or never settles, and the open scenario is not replaced. None of these comparisons is a forecast.

The gate Gantt plots 72 hours of issuer, bank and payout open/closed state plus weekday versus weekend FX. Closed and weekend hours use hatched fills so open versus closed is not color-only. A text table remains the required equivalent. The selected-hour marker follows the timeline. The first-payout marker is `nextPayoutTime` from hour zero given starting reserve. A paired-row comparison Gantt draws current above baseline for each gate. Its table lists hours where those calendars differ and always includes the selected hour. A display filter can hide hours that are open on every gate; the table and chart are a local drawing and the model still contains 72 hours. A second display filter can show only Issuer, Bank, Payout or FX, with All gates restore; simulation results stay unchanged. A third display filter can show only hours where issuer, bank, payout and FX are all closed; uncheck to restore all hours. A fourth display filter can show only Saturday and Sunday hours; uncheck to restore all hours. A fifth display filter can hide Saturday and Sunday hours; uncheck to restore all hours. A sixth display filter can hide hours that are closed on every gate, the inverse of hiding hours open on every gate; uncheck to restore all hours. A seventh display filter can hide hours whose synthetic queue is zero; uncheck to restore all hours. An eighth display filter can hide hours where the bank gate is closed; uncheck to restore all hours. A ninth display filter can hide hours where the issuer gate is closed; uncheck to restore all hours. A tenth display filter can hide hours where the payout gate is closed; uncheck to restore all hours. An eleventh display filter can hide hours where the FX gate is closed; uncheck to restore all hours. A twelfth display filter can hide hours where the payout gate is open; uncheck to restore all hours. A thirteenth display filter can hide hours where FX is weekday-depth / open; uncheck to restore all hours. A fourteenth display filter can hide hours where the bank gate is open; uncheck to restore all hours. A fifteenth display filter can hide hours where the issuer gate is open; uncheck to restore all hours. A sixteenth display filter can hide hours that are weekend and issuer-open; uncheck to restore all hours. A seventeenth display filter can hide hours that are weekend and issuer-closed; uncheck to restore all hours. An eighteenth display filter can hide hours that are weekend and bank-closed; uncheck to restore all hours. A nineteenth display filter can hide hours that are weekend and bank-open; uncheck to restore all hours. A twentieth display filter can hide hours that are weekend and payout-open; uncheck to restore all hours. A twenty-first display filter can hide hours that are weekend and FX-open (`fxWeekday` true on a weekend hour); uncheck to restore all hours. A twenty-second display filter can hide hours that are weekend and payout-closed; uncheck to restore all hours. A twenty-third display filter can hide hours that are weekend and FX-closed (`ganttHourWeekendFxClosed` = weekend and FX-closed); uncheck to restore all hours. A twenty-fourth display filter can hide hours that are weekday and FX-closed (`ganttHourWeekdayFxClosed` = weekday and FX-closed); uncheck to restore all hours. A twenty-fifth display filter can hide hours that are weekday and FX-open (`ganttHourWeekdayFxOpen` = weekday and FX-open); uncheck to restore all hours. The selected hour stays visible if that filter would otherwise hide it. Display filters do not change the 72-hour model. Hatched Gantt rows are a local drawing. They do not connect to a bank or a live redemption queue. Keyboard `f` jumps to the first chart hour where the bank is closed, or the first closed issuer, bank or payout hour if the bank never closes. Keyboard `y` jumps to the hours-to-first-settlement dashboard line, or the dashboard heading if that line is missing. Keyboard `,` copies the same hours-to-first-settlement Markdown as the hours-to-first-settlement copy control. Keyboard `;` copies the same hours-to-clear Markdown as the hours-to-clear copy control. Keyboard `[` jumps to the hours-to-clear copy control, or the dashboard heading if that control is missing. Keyboard `]` jumps to Print, or the print / one-pager heading if that control is missing. Keyboard `.` jumps to the first-closed-FX-hour copy control, or the FX Gantt row or dashboard heading if that control is missing. Keyboard `/` without Shift jumps to the remaining-reserve copy control, or the dashboard heading if that control is missing. Shift+/ stays help. Keyboard `'` copies the same first-closed-bank Markdown as the first-closed-bank copy control. Keyboard `:` copies the same first-closed-issuer Markdown as the first-closed-issuer copy control. Keyboard `<` jumps to the first-closed-bank-hour copy control, or the Gantt heading if that control is missing. Keyboard `-` jumps to the first-closed-issuer-hour copy control, or the Gantt heading if that control is missing. Keyboard `>` jumps to the hide-zero-queue Gantt filter, or the Gantt heading if that control is missing. Keyboard `=` jumps to the hide-bank-closed Gantt filter, or the Gantt heading if that control is missing. Keyboard `"` copies the same first-closed-payout Markdown as the first-closed-payout copy control. Keyboard `_` jumps to the first-closed-payout-hour copy control, or the Gantt heading if that control is missing. Keyboard `{` jumps to the hide-issuer-closed Gantt filter, or the Gantt heading if that control is missing. Keyboard `}` copies the same first-closed-FX Markdown as the first-closed-FX copy control. Keyboard `+` jumps to the first-closed-FX-hour copy control, or the Gantt heading if that control is missing. Keyboard `|` jumps to the hide-payout-closed Gantt filter, or the Gantt heading if that control is missing. Keyboard `~` copies the same first-open-payout Markdown as the first-open-payout copy control. Keyboard `!` jumps to the first-open-payout-hour copy control, or the Gantt heading if that control is missing. Keyboard `@` jumps to the hide-FX-closed Gantt filter, or the Gantt heading if that control is missing. Keyboard `(` copies the same first-open-FX Markdown as the first-open-FX copy control. Keyboard `)` jumps to the first-open-FX-hour copy control, or the Gantt heading if that control is missing. Keyboard `#` jumps to the hide-payout-open Gantt filter, or the Gantt heading if that control is missing. Keyboard `*` copies the same first-open-bank Markdown as the first-open-bank copy control. Keyboard `&` jumps to the first-open-bank-hour copy control, or the Gantt heading if that control is missing. Keyboard `%` jumps to the hide-FX-open Gantt filter, or the Gantt heading if that control is missing. Keyboard `$` copies the same first-open-issuer Markdown as the first-open-issuer copy control. Keyboard `^` jumps to the first-open-issuer-hour copy control, or the Gantt heading if that control is missing. Keyboard backtick jumps to the hide-issuer-open Gantt filter, or the Gantt heading if that control is missing. Keyboard `5` copies the same last-open-issuer Markdown as the last-open-issuer copy control. Keyboard `6` jumps to the last-open-issuer-hour copy control, or the Gantt heading if that control is missing. Keyboard `7` jumps to the hide-weekend-issuer-open Gantt filter, or the Gantt heading if that control is missing. Keyboard `8` copies the same last-closed-issuer Markdown as the last-closed-issuer copy control. Keyboard `9` jumps to the last-closed-issuer-hour copy control, or the Gantt heading if that control is missing. Keyboard `0` jumps to the hide-weekend-issuer-closed Gantt filter, or the Gantt heading if that control is missing. Keyboard `1` copies the same last-closed-bank Markdown as the last-closed-bank copy control. Keyboard `2` jumps to the last-closed-bank-hour copy control, or the Gantt heading if that control is missing. Keyboard `3` jumps to the hide-weekend-bank-closed Gantt filter, or the Gantt heading if that control is missing. Keyboard `4` copies the same last-open-bank Markdown as the last-open-bank copy control. Keyboard `Home` jumps to the last-open-bank-hour copy control, or the Gantt heading if that control is missing. Keyboard `End` jumps to the hide-weekend-bank-open Gantt filter, or the Gantt heading if that control is missing. Keyboard `PageUp` copies the same last-open-payout Markdown as the last-open-payout copy control. Keyboard `PageDown` jumps to the last-open-payout-hour copy control, or the Gantt heading if that control is missing. Keyboard `ArrowUp` jumps to the hide-weekend-payout-open Gantt filter, or the Gantt heading if that control is missing. Keyboard `Insert` copies the same last-open-FX Markdown as the last-open-FX copy control. Keyboard `ArrowDown` jumps to the last-open-FX-hour copy control, or the Gantt heading if that control is missing. Keyboard `ArrowLeft` jumps to the hide-weekend-FX-open Gantt filter, or the Gantt heading if that control is missing. Keyboard `Delete` copies the same last-closed-FX Markdown as the last-closed-FX copy control. Keyboard `F2` jumps to the last-closed-FX-hour copy control, or the Gantt heading if that control is missing. Keyboard `ArrowRight` jumps to the hide-weekend-payout-closed Gantt filter, or the Gantt heading if that control is missing. Keyboard `F3` copies the same last-closed-payout Markdown as the last-closed-payout copy control. Keyboard `F4` jumps to the last-closed-payout-hour copy control, or the Gantt heading if that control is missing. Keyboard `Backspace` jumps to the hide-weekend-FX-closed Gantt filter, or the Gantt heading if that control is missing. Keyboard `F7` copies the same last-weekend-FX-closed Markdown as the last-weekend-FX-closed copy control. Keyboard `F8` jumps to the last-weekend-FX-closed-hour copy control, or the Gantt heading if that control is missing. Keyboard `F9` jumps to the hide-weekday-FX-closed Gantt filter, or the Gantt heading if that control is missing. Keyboard `F10` copies the same last-weekday-FX-closed Markdown as the last-weekday-FX-closed copy control. Keyboard `Shift+F10` copies the same first-weekend-FX-open Markdown as the first-weekend-FX-open copy control. Keyboard `F11` jumps to the last-weekday-FX-closed-hour copy control, or the Gantt heading if that control is missing. Keyboard `Shift+F11` jumps to the first-weekend-FX-open-hour copy control, or the Gantt heading if that control is missing. Keyboard `F12` jumps to the hide-weekend-FX-open Gantt filter, or the Gantt heading if that control is missing. Keyboard `Shift+F12` jumps to the hide-weekday-FX-closed Gantt filter, or the Gantt heading if that control is missing. Keyboard `Backspace` also jumps to the hide-weekend-FX-closed Gantt filter. Keyboard `z` copies the same remaining-reserve Markdown as the remaining-reserve copy control. Keyboard `c` copies the same selected-hour Markdown as the copy button. Keyboard `x` copies the same closed-hours Markdown as the copy control. Keyboard `h` jumps to the selected Gantt hour table. Keyboard `k` jumps to the hours-to-clear line. Keyboard `n` jumps to the first-payout Gantt marker, or the Gantt heading if none exists. Keyboard `a` jumps to analysis and export controls. Keyboard `b` jumps to the Bank Gantt row, or the Gantt heading if that row is filtered away. Keyboard `w` jumps to the FX Gantt row, or the Gantt heading if that row is filtered away. Keyboard `m` jumps to the paired compare Gantt heading if present, otherwise the gate Gantt. Keyboard `t` jumps to the timing review heading. A Gantt CSV exports open or closed state for the same 72 chart hours. Selected-hour Markdown copies issuer, bank, payout and FX state for the timeline hour with a synthetic-not-live notice. Remaining-reserve Markdown copies remaining reserve and queued AUD at that hour as one synthetic line, distinct from hours-to-clear copy and selected-hour copy. Peak-queue hour Markdown copies the peak checkpoint hour, queued AUD and gate state, and is distinct from selected-hour copy. Closed-hours Markdown lists each chart hour and which gates are closed; it is a local drawing, not a bank feed. FX-hours Markdown lists weekday depth versus weekend thinning for the same 72 chart hours; it is a local drawing, not a bank feed. Weekend FX hour-count Markdown copies open versus closed counts for Saturday and Sunday hours only; it is a count of modeled hours, not a bank calendar. First-closed-FX Markdown copies the first weekend-thinned FX hour label, or none when no modeled hour is closed; it is a count of modeled hours, not a bank calendar. First-closed-bank Markdown copies the first bank-closed hour label, or none when no modeled hour is closed; it is distinct from first-closed-FX copy and is a count of modeled hours, not a bank calendar. First-closed-issuer Markdown copies the first issuer-closed hour label, or none when no modeled hour is closed; it is distinct from first-closed-FX copy and first-closed-bank copy and is a synthetic label, not live issuer data. First-closed-payout Markdown copies the first payout-closed hour label, or none when no modeled hour is closed; it is distinct from first-closed-FX, first-closed-bank and first-closed-issuer copy and is a synthetic label, not live payout data. First-open-payout Markdown copies the first payout-open hour label, or none when no modeled hour is open; it is distinct from first-closed-payout, first-closed-FX, first-closed-issuer and first-closed-bank copy and is a synthetic label, not live payout data. First-open-FX Markdown copies the first weekday-depth FX hour label, or none when no modeled hour is open; it is distinct from first-open-payout, first-closed-FX and first-closed-payout copy and is a local drawing, not a live FX feed. First-open-bank Markdown copies the first bank-open hour label, or none when no modeled hour is open; it is distinct from first-open-FX copy and is a local drawing, not a live bank feed. First-open-issuer Markdown copies the first issuer-open hour label, or none when no modeled hour is open; it is distinct from first-open-bank copy and first-closed-issuer copy and is a synthetic label, not live issuer data. Last-open-issuer Markdown copies the last issuer-open hour label, or none when no modeled hour is open; it is distinct from first-open-issuer copy and is a synthetic label, not live issuer data. Last-open-bank Markdown copies the last bank-open hour label, or none when no modeled hour is open; it is distinct from last-closed-bank copy and last-open-issuer copy and is a count of modeled hours, not a bank calendar. Last-open-payout Markdown copies the last payout-open hour label, or none when no modeled hour is open; it is distinct from last-open-bank copy, last-open-issuer copy and first-open-payout copy and is a count of modeled hours, not a payout calendar. Last-open-FX Markdown copies the last weekday-depth FX hour label, or none when no modeled hour is open; it is distinct from last-open-bank copy, last-open-payout copy and first-open-FX copy and is a count of modeled hours, not an FX calendar. Last-closed-issuer Markdown copies the last issuer-closed hour label, or none when no modeled hour is closed; it is distinct from last-open-issuer copy and first-closed-issuer copy and is a synthetic label, not live issuer data. Last-closed-bank Markdown copies the last bank-closed hour label, or none when no modeled hour is closed; it is distinct from last-closed-issuer copy and first-closed-bank copy and is a synthetic label, not live bank data. Last-closed-FX Markdown copies the last weekend-thinned FX hour label, or none when no modeled hour is closed; it is distinct from last-open-FX copy, last-open-payout copy and first-closed-FX copy and is a count of modeled hours, not an FX calendar. Last-closed-payout Markdown copies the last payout-closed hour label, or none when no modeled hour is closed; it is distinct from last-closed-FX copy, last-closed-bank copy, last-closed-issuer copy and last-open-payout copy and is a count of modeled hours, not a payout calendar. Last-weekend-FX-closed Markdown copies the last weekend-and-FX-closed hour label, or none when no modeled hour matches; it is distinct from last-closed-FX copy, last-closed-payout copy, last-closed-bank copy and last-closed-issuer copy and is a count of modeled hours, not an FX calendar. Last-weekday-FX-closed Markdown copies the last weekday-and-FX-closed hour label, or none when no modeled hour matches; it is distinct from last-weekend-FX-closed copy, last-closed-FX copy and last-closed-payout copy and is a count of modeled hours, not an FX calendar. Last-weekend-FX-open Markdown copies the last weekend-and-FX-open hour label, or none when no modeled hour matches; it is distinct from last-weekday-FX-closed copy, last-weekend-FX-closed copy and last-closed-FX copy and is a count of modeled hours, not an FX calendar. Last-weekday-FX-open Markdown copies the last weekday-and-FX-open hour label, or none when no modeled hour matches; it is distinct from last-weekend-FX-open copy, last-weekday-FX-closed copy and last-weekend-FX-closed copy and is a count of modeled hours, not an FX calendar. First-weekday-FX-open Markdown copies the first weekday-and-FX-open hour label, or none when no modeled hour matches; it is distinct from last-weekday-FX-open copy, last-weekend-FX-open copy and last-weekday-FX-closed copy and is a count of modeled hours, not an FX calendar. First-weekday-FX-closed Markdown copies the first weekday-and-FX-closed hour label, or none when no modeled hour matches; it is distinct from last-weekday-FX-closed copy, first-weekday-FX-open copy, last-weekday-FX-open copy and last-weekend-FX-closed copy and is a count of modeled hours, not an FX calendar. First-weekend-FX-open Markdown copies the first weekend-and-FX-open hour label, or none when no modeled hour matches; it is distinct from first-weekday-FX-closed copy, first-weekday-FX-open copy, last-weekday-FX-open copy, last-weekend-FX-open copy, last-weekday-FX-closed copy, last-weekend-FX-closed copy and last-closed-FX copy and is a count of modeled hours, not an FX calendar. First-weekend-FX-closed Markdown copies the first weekend-and-FX-closed hour label, or none when no modeled hour matches; it is distinct from first-weekend-FX-open copy, last-weekend-FX-closed copy, last-weekday-FX-closed copy, first-weekday-FX-closed copy, first-weekday-FX-open copy and last-closed-FX copy and is a count of modeled hours, not an FX calendar. Hours-to-clear Markdown copies one synthetic line for the queue-clear checkpoint. Arrival-cohort Markdown copies cohort window, arrivals and remaining amounts and is not a forecast. A formula-safe queue CSV exports the hour label and queue size at every checkpoint. A one-row dashboard CSV exports hours to clear, the peak hour label and hours to first settlement; empty cells mean those events never occurred. Printable queue and sensitivity bar SVGs are light-background paths and can be downloaded as files. Reports may inline the Gantt SVG, the comparison Gantt, the queue path and the hourly limiting-gate table. A Markdown report copies hours to clear the queue and the peak queue hour. Dashboard Markdown copies hours to clear, peak queue hour and hours to first settlement. Limiting-gate Markdown copies the 72-hour observation counts and is not a causal ranking. The hourly table highlights the peak queue checkpoint in text as well as style and can hide hours with no backlog without changing dashboard counts. Print and print redacted include the hours-to-clear line when a queue exists, with an honest empty when none, one peak-queue hour line, the selected Gantt hour label, remaining reserve at the selected hour, hours to first settlement, the first closed FX hour as one line, with an honest empty when none, the first closed bank hour as one line, with an honest empty when none, the first closed issuer hour as one line, with an honest empty when none, and the first closed payout hour as one line, with an honest empty when none, and the first open payout hour as one line, with an honest empty when none, and the first open FX hour as one line, with an honest empty when none, and the first open bank hour as one line, with an honest empty when none. Hours to clear, the first closed FX hour, the first closed bank hour, the first closed issuer hour, the first closed payout hour, the first open payout hour, the first open FX hour and the first open bank hour are counts of modeled hours, not a bank calendar. Print redacted replaces custom gate display labels with generic Issuer, Bank, Payout and FX and does not change the saved scenario. None of these drawings or reports contain timestamps.

## Recovery and output boundaries

Workspace v1 stores canonical current and baseline scenarios, bounded notes (4000 characters), target, deadline, selected hour, Gantt hour index (`ganttHourIndex`, integer 0 to 72), Gantt table density (`snapshots`, `all` or `open`), selected chart (`queue` or `gantt`), `ganttClosedOnly` (boolean), `ganttGateFilter` (`all`, `issuer`, `bank`, `payout` or `fx`), `queueBacklogOnly` (boolean), `ganttEveryGateClosed` (boolean), `hideWeekdayGanttHours` (boolean), `hideWeekendGanttHours` (boolean), `hideOpenGanttHours` (boolean), `hideClosedGanttHours` (boolean), `hideZeroQueueGanttHours` (boolean), `hideBankClosedGanttHours` (boolean), `hideIssuerClosedGanttHours` (boolean), `hidePayoutClosedGanttHours` (boolean), `hideFxClosedGanttHours` (boolean), `hidePayoutOpenGanttHours` (boolean), `hideFxOpenGanttHours` (boolean), `hideBankOpenGanttHours` (boolean), `hideIssuerOpenGanttHours` (boolean), `hideWeekendIssuerOpenGanttHours` (boolean), `hideWeekendIssuerClosedGanttHours` (boolean), `hideWeekendBankClosedGanttHours` (boolean), `hideWeekendBankOpenGanttHours` (boolean), `hideWeekendPayoutOpenGanttHours` (boolean), `hideWeekendFxOpenGanttHours` (boolean), `hideWeekendPayoutClosedGanttHours` (boolean), `hideWeekendFxClosedGanttHours` (boolean), `hideWeekdayFxClosedGanttHours` (boolean) and `hideWeekdayFxOpenGanttHours` (boolean). Older workspace files without density restore to six-hour snapshots. Older files without selected hour restore hour zero. Older files without Gantt hour index restore hour zero or the selected hour. Older files without selected chart restore the queue chart. Older files without `ganttClosedOnly` restore all hours. Older files without `ganttGateFilter` restore all gates. Older files without `queueBacklogOnly` restore all queue-table hours. Older files without `ganttEveryGateClosed` restore all Gantt hours. Older files without `hideWeekdayGanttHours` restore all Gantt hours. Older files without `hideWeekendGanttHours` restore all Gantt hours. Older files without `hideOpenGanttHours` restore all Gantt hours. Older files without `hideClosedGanttHours` restore all Gantt hours. Older files without `hideZeroQueueGanttHours` restore all Gantt hours. Older files without `hideBankClosedGanttHours` restore all Gantt hours. Older files without `hideIssuerClosedGanttHours` restore all Gantt hours. Older files without `hidePayoutClosedGanttHours` restore all Gantt hours. Older files without `hideFxClosedGanttHours` restore all Gantt hours. Older files without `hidePayoutOpenGanttHours` restore all Gantt hours. Older files without `hideFxOpenGanttHours` restore all Gantt hours. Older files without `hideBankOpenGanttHours` restore all Gantt hours. Older files without `hideIssuerOpenGanttHours` restore all Gantt hours. Older files without `hideWeekendIssuerOpenGanttHours` restore all Gantt hours. Older files without `hideWeekendIssuerClosedGanttHours` restore all Gantt hours. Older files without `hideWeekendBankClosedGanttHours` restore all Gantt hours. Older files without `hideWeekendBankOpenGanttHours` restore all Gantt hours. Older files without `hideWeekendPayoutOpenGanttHours` restore all Gantt hours. Older files without `hideWeekendFxOpenGanttHours` restore all Gantt hours. Older files without `hideWeekendPayoutClosedGanttHours` restore all Gantt hours. Older files without `hideWeekendFxClosedGanttHours` restore all Gantt hours. Older files without `hideWeekdayFxClosedGanttHours` restore all Gantt hours. Older files without `hideWeekdayFxOpenGanttHours` restore all Gantt hours. Unknown workspace keys and unknown field values are rejected. Imports are size-bounded and atomically decoded before replacing state; computed results are regenerated. The library contains at most 12 canonical scenarios. Scenario undo keeps at most 40 snapshots in memory. Portable reports are static escaped HTML with a restrictive content security policy and may inline the current gate Gantt, the baseline-versus-current Gantt, queue path and limiting-gate counts. Formula-safe queue, Gantt and dashboard CSVs quote cells and prefix formula-like text. The hourly ledger CSV contains only fixed headers, model-generated time labels and numeric values, so scenario names cannot inject spreadsheet formulas. Analysis JSON has no timestamp.

### Queue exposure by day

The optional timing review can decompose end-of-hour queue exposure and demand across the four partial calendar days. It preserves the current scenario and states timing conventions and numerical limits in each table. This is synthetic analysis, not a promise of operational service.

### Arrival-cohort waiting ledger

The optional timing review can attribute hourly settlement to earliest arrivals using an explicit FIFO assumption. It preserves the current scenario and states timing conventions and numerical limits in each table. This is synthetic analysis, not a promise of operational service.

### Settlement checkpoints

The optional timing review can compare service progress against both arrived and full-horizon demand. It preserves the current scenario and states timing conventions and numerical limits in each table. This is synthetic analysis, not a promise of operational service.

### Complete-chain closure spells

The optional timing review can locate continuous intervals when operating windows prevent any complete payout chain. It preserves the current scenario and states timing conventions and numerical limits in each table. This is synthetic analysis, not a promise of operational service.

### Operating-window overlap

The optional timing review can show individually open hours that cannot form a complete chain. It preserves the current scenario and states timing conventions and numerical limits in each table. This is synthetic analysis, not a promise of operational service.

### Reserve needed by service target

The optional timing review can compare minimum whole-cent reserves for four full-horizon settlement targets. It preserves the current scenario and states timing conventions and numerical limits in each table. This is synthetic analysis, not a promise of operational service.

### Joint-throughput ladder

The optional timing review can test coordinated throughput increases while retaining reserve, demand and operating windows. It preserves the current scenario and states timing conventions and numerical limits in each table. This is synthetic analysis, not a promise of operational service.

### Holiday assumption comparison

The optional timing review can compare all four Saturday and Monday holiday combinations with other inputs fixed. It preserves the current scenario and states timing conventions and numerical limits in each table. This is synthetic analysis, not a promise of operational service.

### Hourly effect of extra reserve

The optional timing review can locate when an additional reserve increment changes actual modeled settlements. It preserves the current scenario and states timing conventions and numerical limits in each table. This is synthetic analysis, not a promise of operational service.

### Review packet contract

`createWeekendReviewPacket(scenario, tool)` requires a complete already-valid typed scenario. `replayWeekendReviewPacket(packet)` requires format `weekend-review`, version 1, six exact top-level fields, an exact canonical source snapshot and every recomputed output primitive. Serialized packets are bounded to 1 MiB and UI downloads use those same compact bytes. The existing scenario sanitizer remains unchanged. Review input does not silently clamp invalid evidence. FIFO attribution is an additional analysis convention, not a change to the aggregate recurrence; completed waits start at zero for same-step settlement, and unfinished amounts accrue wait through hour 72.
