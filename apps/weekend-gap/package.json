{
  "name": "weekend-gap",
  "version": "1.5.40",
  "private": true,
  "description": "An educational AUD stablecoin weekend liquidity simulator.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "analyze": "node scripts/analyze.mjs",
    "start": "node scripts/dev-server.mjs",
    "launch": "node scripts/launch.mjs",
    "build:standalone": "node scripts/build-standalone.mjs",
    "test": "node --test",
    "check": "node --check scripts/analyze.mjs && node --check src/model.js && node --check src/app.js && node --check scripts/dev-server.mjs && node --check scripts/launch.mjs && node --check scripts/build-standalone.mjs && node scripts/build-standalone.mjs --check"
  },
  "license": "MIT"
}
