# README Quick Start Audit (phase 15, 2026-09-23)

Verdict: surface-fixed.

The `## Quickstart` heading exists in `README.md` near line 25, but the
concrete launch command (`node scripts/serve.mjs`) was buried below the
`## What's new` block inside the `### No install: open the single-file build`
sub-section. Inserted a `npm start` / `node scripts/serve.mjs` code block
directly under the `## Quickstart` heading. No package install is required
because `package.json` declares `private: true` and `node scripts/serve.mjs`
uses only Node 20+ stdlib.
